---
description: 
globs: 
alwaysApply: true
---
# 微信小程序搜索按钮一致性设计规范

## 概述
本规范确保万能查询页面中所有标签页的搜索按钮保持统一的结构、大小和样式，只允许颜色主题差异来体现不同功能模块的特色。

## 核心原则

### 1. 结构统一性
所有搜索按钮必须使用相同的HTML结构，不允许额外的容器或布局差异。

### 2. 样式一致性
所有搜索按钮的尺寸、字体、圆角、内边距等基础样式必须完全一致。

### 3. 颜色主题差异化
通过不同的渐变色主题来区分不同功能模块，体现各自的特色和用途。

## 标准HTML结构

### 统一模板
```xml
<!-- 搜索框 -->
<view class="search-section [页面类型]-search">
  <van-search
    value="{{ searchValue }}"
    placeholder="搜索提示文字..."
    bind:search="onSearch"
    bind:change="onSearchChange"
    bind:clear="onSearchClear"
    use-action-slot
  >
    <view slot="action" bind:tap="onSearch">
      <van-button type="primary" size="small" class="search-action-button">搜索</van-button>
    </view>
  </van-search>
</view>
```

### 关键要求
1. **必须使用** `use-action-slot` 属性
2. **slot="action"** 直接包含按钮，不允许额外容器
3. **按钮属性固定**：`type="primary" size="small" class="search-action-button"`
4. **页面类型标识**：通过 `[页面类型]-search` 类名区分

## 统一CSS样式

### 基础按钮样式
```css
/* 搜索按钮统一基础样式 */
.search-action-button {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
  border: none !important;
  border-radius: 20rpx !important;
  color: #fff !important;
  font-weight: 600 !important;
  font-size: 28rpx !important;          /* 统一字体大小 */
  padding: 16rpx 32rpx !important;      /* 统一内边距 */
  box-shadow: 0 6rpx 20rpx rgba(102, 126, 234, 0.3) !important;
  transition: all 0.3s ease !important;
  min-width: 120rpx !important;         /* 统一最小宽度 */
}

.search-action-button:active {
  transform: translateY(2rpx) !important;
  box-shadow: 0 4rpx 15rpx rgba(102, 126, 234, 0.4) !important;
}
```

### 页面主题色配置

#### 缩写页面 - 蓝紫色渐变
```css
.abbreviations-search .search-action-button {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
  box-shadow: 0 6rpx 20rpx rgba(102, 126, 234, 0.3) !important;
}

.abbreviations-search .search-action-button:active {
  box-shadow: 0 4rpx 15rpx rgba(102, 126, 234, 0.4) !important;
}
```

#### 定义页面 - 绿色渐变
```css
.definitions-search .search-action-button {
  background: linear-gradient(135deg, #52c41a 0%, #389e0d 100%) !important;
  box-shadow: 0 6rpx 20rpx rgba(82, 196, 26, 0.3) !important;
}

.definitions-search .search-action-button:active {
  box-shadow: 0 4rpx 15rpx rgba(82, 196, 26, 0.4) !important;
}
```

#### 机场页面 - 橙红色渐变
```css
.airports-search .search-action-button {
  background: linear-gradient(135deg, #ff9a56 0%, #ff6b6b 100%) !important;
  box-shadow: 0 6rpx 20rpx rgba(255, 154, 86, 0.3) !important;
}

.airports-search .search-action-button:active {
  box-shadow: 0 4rpx 15rpx rgba(255, 154, 86, 0.4) !important;
}
```

#### 通信页面 - 紫色渐变
```css
.communications-search .search-action-button {
  background: linear-gradient(135deg, #722ed1 0%, #531dab 100%) !important;
  box-shadow: 0 6rpx 20rpx rgba(114, 46, 209, 0.3) !important;
}

.communications-search .search-action-button:active {
  box-shadow: 0 4rpx 15rpx rgba(114, 46, 209, 0.4) !important;
}
```

#### 规章页面 - 金色渐变
```css
.normative-search .search-action-button {
  background: linear-gradient(135deg, #faad14 0%, #d48806 100%) !important;
  box-shadow: 0 6rpx 20rpx rgba(250, 173, 20, 0.3) !important;
}

.normative-search .search-action-button:active {
  box-shadow: 0 4rpx 15rpx rgba(250, 173, 20, 0.4) !important;
}
```

## 禁止的做法

### ❌ 错误示例 1：额外容器
```xml
<!-- 错误：不允许额外的容器 -->
<view slot="action" class="search-actions">
  <van-button type="primary" size="small" class="search-action-button">搜索</van-button>
</view>
```

### ❌ 错误示例 2：特殊布局样式
```css
/* 错误：不允许针对某个页面的特殊布局 */
.some-search-actions .search-action-button {
  flex: 1;
  min-width: 100rpx;
}
```

### ❌ 错误示例 3：尺寸差异
```css
/* 错误：不允许不同的尺寸设置 */
.special-search .search-action-button {
  font-size: 32rpx !important;
  padding: 20rpx 40rpx !important;
}
```

## 开发检查清单

在开发或修改搜索功能时，请确保：

- [ ] HTML结构与标准模板完全一致
- [ ] 没有使用额外的容器包装按钮
- [ ] 按钮属性符合规范：`type="primary" size="small" class="search-action-button"`
- [ ] 页面使用正确的类名标识：`[页面类型]-search`
- [ ] CSS只修改颜色主题，不修改尺寸和布局
- [ ] 所有基础样式参数保持一致：
  - `font-size: 28rpx`
  - `padding: 16rpx 32rpx`
  - `min-width: 120rpx`
  - `border-radius: 20rpx`

## 新增页面规范

当添加新的万能查询标签页时：

1. **复制标准HTML结构**，只修改搜索相关的方法名
2. **选择合适的主题色**，定义新的CSS主题样式
3. **添加页面类名**，如 `new-feature-search`
4. **测试按钮一致性**，确保与其他页面视觉效果相同

## 主题色选择指南

### 推荐的主题色方案
- 🔵 **蓝紫色**：技术、专业类功能（如缩写）
- 🟢 **绿色**：定义、解释类功能
- 🟠 **橙红色**：地理、位置类功能（如机场）
- 🟣 **紫色**：通信、交流类功能
- 🟡 **金色**：权威、规范类功能（如规章）
- 🔴 **红色**：警告、紧急类功能
- 🔷 **青色**：工具、实用类功能

### 新主题色CSS模板
```css
.new-feature-search .search-action-button {
  background: linear-gradient(135deg, #起始色 0%, #结束色 100%) !important;
  box-shadow: 0 6rpx 20rpx rgba(阴影色, 0.3) !important;
}

.new-feature-search .search-action-button:active {
  box-shadow: 0 4rpx 15rpx rgba(阴影色, 0.4) !important;
}
```

## 维护要求

### 定期检查
- 每次发布前检查所有搜索按钮的一致性
- 新功能开发时严格遵循此规范
- 发现不一致时立即修正

### 违规处理
如果发现不符合规范的实现：
1. 立即修正HTML结构和CSS样式
2. 确保符合统一标准
3. 更新相关文档

## 总结

遵循此规范可以确保：
- 🎯 **用户体验一致性**：所有搜索按钮外观和交互统一
- 🎨 **视觉识别清晰**：通过颜色区分不同功能模块
- 🛠️ **维护成本降低**：统一的结构便于批量修改和维护
- 📱 **界面专业性**：整体界面更加协调和专业

此规范是万能查询界面设计的重要组成部分，所有开发人员都必须严格遵循。

