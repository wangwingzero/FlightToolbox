# 绕机图片分包加载与缓存修复说明

本说明记录本次“绕机检查”图片分包在部分设备/环境下无法显示的问题定位与修复过程，以及后续验证、清理与排查指引，便于复用与回溯。

## 问题背景与现象
- **现象**：在调试手机上（真机调试/预览/体验/发布等）绕机图片出现空白或 404；`wx.loadSubpackage` 在真机调试模式不可用；清缓存后发布版一度无法恢复。
- **对比**：其他手机显示正常。调试机清过缓存后持续异常。
- **日志典型**：
  - `⚠️ wx.loadSubpackage API 不可用（可能是真机调试模式）`
  - `GET /packageWalkaroundImagesX/imagesX/xxx.png 404 (Not Found)`

## 根因总结
1. 早期代码对 `wx.loadSubpackage` 传入了 `subPackages.root`，而非 `subPackages.name`，导致加载失败（已修正）。
2. 真机调试模式下 `wx.loadSubpackage` 本身不可用，预加载规则对图片分包也无法生效，导致未加载的分包内图片直接 404。
3. 清缓存后本地图片缓存为空，若分包又未真正加载，就无法回退到缓存路径。

## 关键修复点
- **使用正确的分包标识**
  - 在调用 `wx.loadSubpackage` 时，传 `app.json -> subPackages[].name`（例如：`walkaroundImages1Package`），而非 `root`。
  - 已修文件：`miniprogram/utils/walkaround-preload-guide.js`

- **真机调试兜底：占位页导航触发分包加载**
  - 当 `wx.loadSubpackage` 不可用时，通过 `wx.navigateTo('/<packageRoot>/pages/placeholder/index')` 强制触发“按页导航”的分包自动加载，短暂停留后 `navigateBack()` 返回。
  - 已修文件：`miniprogram/packageWalkaround/pages/index/index.js` 的 `ensurePackageLoaded()` 分支（`typeof wx.loadSubpackage !== 'function'`）。
  - 典型成功日志：`packageWalkaroundImages1 placeholder loaded`，随后图片从分包路径或缓存路径加载成功。

- **版本化的缓存 key（隔离不同环境/版本）**
  - 通过 `VersionManager.getVersionedKey(baseKey)` 生成 `debug_*/trial_*/release_*` 前缀的 Key：
    - 预加载状态：`flight_toolbox_walkaround_preload_status`
    - 图片索引：`walkaround_image_cache_index`
  - 变更文件：`miniprogram/utils/version-manager.js`、`miniprogram/utils/image-cache-manager.js`、`miniprogram/utils/walkaround-preload-guide.js`

- **工程与资源校验**
  - `app.json` 正确声明 6 个图片分包（root/name 对应，pages 含 `pages/placeholder/index`）。
  - `project.config.json` 的 `packOptions.include` 显式包含 `packageWalkaroundImages1..6`，确保上传/打包不会遗漏图片分包目录。
  - 图片实际存在于 `packageWalkaroundImagesX/imagesX/` 目录中。

## 验证流程（真机）
1. 以“预览/体验/发布版”进入以下页面各停留 1-2 秒：
   - 绕机检查、日出日落、个人检查单、飞行时间分摊、体检标准、通信规范（这些页面会触发部分分包预加载）。
2. 返回“绕机检查”，点击区域 1 或 5：
   - 若真机调试模式：观察是否自动跳到对应分包的 `placeholder` 页并返回；随后图片显示并开始本地缓存。
   - 预期日志：
     - `✅ 已标记 1-4/5-8 为预加载完成`
     - `📦 图片从分包加载: /packageWalkaroundImagesX/imagesX/...`
     - 首次可能出现 `not node js file system` 的内部告警，随后 `✅ 已缓存图片到本地 wxfile://usr/...` 即表示缓存成功。

## 清理与恢复
- 仅清理“真机调试”版本的绕机相关 Storage Key：
```js
var VersionManager = require('../../miniprogram/utils/version-manager.js');
VersionManager.clearVersionCaches({
  envVersion: 'develop',
  baseKeys: ['flight_toolbox_walkaround_preload_status', 'walkaround_image_cache_index']
});
```
- 如需彻底清空本地图片文件缓存（wxfile 下的缓存文件）：
```js
var ImageCacheManager = require('../../miniprogram/utils/image-cache-manager.js');
ImageCacheManager.clearAllCache().then(() => {
  wx.showToast({ title: '图片文件缓存已清空', icon: 'none' });
});
```
- 清理后重复“验证流程”，首轮会从分包读取并重新写入本地缓存。

## 常见日志判定
- `⚠️ 真机调试模式：wx.loadSubpackage 不可用`：预期行为，依赖“占位页导航”兜底与本地缓存。
- `packageWalkaroundImagesX placeholder loaded`：分包通过导航成功加载。
- `📦 图片从分包加载: /packageWalkaroundImagesX/imagesX/...`：已可以直接读取分包资源。
- `✅ 已缓存图片到本地: wxfile://usr/walkaround-images/...`：图片完成持久化，后续离线也可加载。

## 注意事项
- 在 DevTools 控制台直接输入 `debug_*_xxx` 会被当作变量解析，报 `ReferenceError`，请通过代码调用清理函数。
- `wx.getSystemInfoSync()` 等诊断应在 appservice 上下文或页面生命周期中打印，避免 `undefined` 误判。
- 真机调试模式与预览/发布运行时能力不同：分包 API/预加载在真机调试常受限，务必用“预览/体验/发布”进行最终验证。

## 附录：关键代码片段
- 分包主动加载（正常场景，传 name）：
```js
wx.loadSubpackage({
  name: mapping.packageName, // 来自 app.json subPackages[].name，如 walkaroundImages1Package
  success() { /* 标记预加载成功 */ },
  fail(err) { /* 重试或记录失败 */ }
});
```
- 真机调试兜底（占位页导航触发分包加载）：
```js
if (typeof wx.loadSubpackage !== 'function') {
  const url = '/' + mapping.packageRoot + '/pages/placeholder/index';
  wx.navigateTo({
    url,
    success() {
      setTimeout(() => {
        try { wx.navigateBack({ delta: 1 }); } catch(e) {}
        // 标记预加载成功，等待 READY_DELAY 再进入详情
      }, TimingConfig.SUBPACKAGE_TIMING.READY_DELAY);
    }
  });
  return;
}
```
- 仅清理“真机调试”下的绕机相关 Key：
```js
VersionManager.clearVersionCaches({
  envVersion: 'develop',
  baseKeys: ['flight_toolbox_walkaround_preload_status', 'walkaround_image_cache_index']
});
```

---
最后一次验证结果：占位页兜底 + 缓存系统已在调试手机上恢复图片显示；随后图片从本地缓存稳定加载。

## 航线录音分包与缓存修复说明

### 问题背景与现象
- **现象**：在调试手机上（真机调试）航线录音首次无法播放/无法缓存；`wx.loadSubpackage` 在真机调试模式不可用，导致分包音频文件无法访问。
- **对比**：预览/体验/发布版正常；其他设备正常。

### 根因总结
1. 真机调试模式下 `wx.loadSubpackage` 能力不可用，预加载规则对音频分包也不稳定，导致无法通过 API 主动加载音频分包。
2. 首次进入时本地音频缓存为空，分包未加载则无法回退到本地缓存路径。
3. 早期少量设备上存在旧版本的“预加载状态/缓存索引”残留，可能造成误判（已通过版本化 Key 与可选清理规避）。

### 关键修复点
- **正确的分包标识**
  - `wx.loadSubpackage` 始终传 `app.json -> subPackages[].name`（如：`australiaAudioPackage`）。

- **真机调试兜底（默认纳入流程）**
  - 当 `wx.loadSubpackage` 不可用时，使用页面导航强制触发分包加载：`wx.navigateTo('/<packageRoot>/index')`，短暂停留后 `navigateBack()`。
  - 已实现位置：
    - `miniprogram/utils/audio-package-loader.js`（核心 Loader，统一兜底）
    - `miniprogram/pages/audio-player/index.ts`（调用端在 API 不可用时改走 Loader 兜底）

- **版本化的缓存 Key（隔离不同环境/版本）**
  - 预加载状态（音频）：`flight_toolbox_audio_preload_status`
  - 音频缓存索引：`flight_audio_cache_index`
  - 通过 `VersionManager.getVersionedKey(baseKey)` 生成 `debug_*/trial_*/release_*` 前缀，避免不同版本/环境相互污染。
  - 相关文件：`miniprogram/utils/version-manager.js`、`miniprogram/utils/audio-cache-manager.js`、`miniprogram/utils/audio-preload-guide.js`

### 验证流程（真机）
1. 打开“航线录音”页面并选择某地区（如：澳大利亚）。
2. 预期：自动跳到 `/<packageRoot>/index` 再返回，随后音频可播放；首轮播放时写入本地缓存。
3. 预期日志：
   - `🚀 开始异步加载音频分包: australiaAudioPackage`
   - `✅ 已标记地区 australia 为预加载完成`
   - `🔄 开始缓存音频...` → `✅ 音频缓存成功: wxfile://usr/...`（后续播放直接走本地缓存）

### 清理与恢复（可选，仅 develop 版本）
- 如遇旧状态干扰，可清理“真机调试（develop）”下的音频相关 Key：
```js
var VersionManager = require('../../miniprogram/utils/version-manager.js');
VersionManager.clearVersionCaches({
  envVersion: 'develop',
  baseKeys: ['flight_toolbox_audio_preload_status', 'flight_audio_cache_index']
});
```
- 如需清空本地音频文件缓存：
```js
var AudioCacheManager = require('../../miniprogram/utils/audio-cache-manager.js');
AudioCacheManager.clearAllCache().then(() => {
  wx.showToast({ title: '音频文件缓存已清空', icon: 'none' });
});
```

### 常见日志判定
- `⚠️ 真机调试模式：wx.loadSubpackage 不可用`：预期；依赖“页面导航兜底 + 本地缓存”。
- `/<packageRoot>/index` 导航成功并返回：分包通过导航成功加载。
- `✅ 音频缓存成功: wxfile://usr/...`：完成持久化，后续离线可播。

---

## 分包制作全流程（快速总览）

制作新分包（图片/音频/资料）时，必须把“真机调试兜底”作为默认能力写入：
- **app.json**：为新分包声明 `root` 与 `name`，并保证有可导航的占位页（图片：`pages/placeholder/index`；音频：`index`）。
- **代码映射**：在 Loader/页面中维护 `{ packageName(name), packageRoot(root) }` 映射。
- **加载策略**：
  - 优先 `wx.loadSubpackage({ name })`；
  - 当 API 不可用（真机调试）→ `wx.navigateTo('/<packageRoot>/...')` 触发加载 → `navigateBack()`；
  - 成功后持久化预加载状态（版本化 Key）。
- **缓存策略**：为大资源准备缓存索引（版本化 Key）与清理工具。
- **验证清单**：预览/体验/发布版各进入触发页 1-2 秒 → 回到业务页验证加载与缓存。

详细步骤与模板代码见《分包制作全流程.md》。
