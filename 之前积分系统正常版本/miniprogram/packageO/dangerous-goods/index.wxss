/* 危险品查询页面样式 - Context7最佳实践 */

.container {
  padding: 0 !important;
  margin: 0 !important;
  background: linear-gradient(180deg, #f8fafb 0%, #f0f4f8 100%);
  min-height: 100vh;
  width: 100% !important;
  box-sizing: border-box !important;
  display: block !important;
  position: relative;
}

/* Context7风格背景装饰 */
.container::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: 160rpx;
  background: linear-gradient(135deg, rgba(99, 102, 241, 0.03) 0%, rgba(168, 85, 247, 0.02) 50%, rgba(236, 72, 153, 0.03) 100%);
  z-index: -1;
  pointer-events: none;
}

/* 现代化背景纹理 */
.container::after {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-image: 
    radial-gradient(circle at 20% 50%, rgba(120, 119, 198, 0.02) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(255, 119, 198, 0.02) 0%, transparent 50%),
    radial-gradient(circle at 40% 80%, rgba(120, 200, 255, 0.02) 0%, transparent 50%);
  z-index: -1;
  pointer-events: none;
}

/* Context7风格搜索框 */
.search-section {
  padding: 32rpx 24rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 250, 252, 0.9) 100%);
  margin-bottom: 0;
  backdrop-filter: blur(20rpx);
  position: sticky;
  top: 0;
  z-index: 98;
  border-bottom: 1rpx solid rgba(226, 232, 240, 0.6);
  box-shadow: 0 2rpx 20rpx rgba(0, 0, 0, 0.02);
}

/* 搜索框左侧图标样式 */
.search-left-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-left: 16rpx;
}

.search-placeholder-icon {
  font-size: 32rpx !important;
  color: #999 !important;
  opacity: 0.7;
}

/* 搜索按钮样式优化 */
.van-search__action {
  color: #ff976a !important;
  font-weight: bold !important;
  font-size: 30rpx !important;
  padding: 0 24rpx !important;
}

.van-search__action:active {
  background-color: rgba(255, 151, 106, 0.1) !important;
  border-radius: 8rpx;
}

/* 搜索框整体样式优化 */
.van-search {
  background: rgba(255, 255, 255, 0.8) !important;
  border-radius: 16rpx !important;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05) !important;
  border: 2rpx solid rgba(255, 151, 106, 0.2) !important;
  transition: all 0.3s ease !important;
}

.van-search:focus-within {
  border-color: #ff976a !important;
  box-shadow: 0 4rpx 16rpx rgba(255, 151, 106, 0.2) !important;
}

.van-search__field {
  background: transparent !important;
  color: #333 !important;
  font-size: 30rpx !important;
}

.van-search__field::placeholder {
  color: #999 !important;
}

/* Context7风格加载状态 */
.loading-section {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  height: 400rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(248, 250, 252, 0.6) 100%);
  backdrop-filter: blur(10rpx);
  border-radius: 24rpx;
  margin: 24rpx;
  border: 1rpx solid rgba(226, 232, 240, 0.6);
}

.results-section {
  padding: 32rpx 24rpx;
}

/* Context7风格列表项设计 */
.regulation-cell,
.emergency-cell,
.hidden-cell {
  margin-bottom: 24rpx;
  border-radius: 20rpx;
  overflow: hidden;
  background: linear-gradient(135deg, #ffffff 0%, #fdfdfd 100%);
  box-shadow: 
    0 2rpx 12rpx rgba(0, 0, 0, 0.04),
    0 1rpx 3rpx rgba(0, 0, 0, 0.08),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
  transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
  position: relative;
  border: 1rpx solid rgba(226, 232, 240, 0.6);
}

.regulation-cell:active,
.emergency-cell:active,
.hidden-cell:active {
  transform: translateY(2rpx) scale(0.995);
  box-shadow: 
    0 4rpx 20rpx rgba(0, 0, 0, 0.08),
    0 2rpx 6rpx rgba(0, 0, 0, 0.12);
}

/* 列表项微妙的左边框装饰 */
.regulation-cell::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 6rpx;
  background: linear-gradient(180deg, #07c160 0%, #52c41a 100%);
}

.emergency-cell::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 6rpx;
  background: linear-gradient(180deg, #ff976a 0%, #ee0a24 100%);
}

.hidden-cell::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 6rpx;
  background: linear-gradient(180deg, #1989fa 0%, #667eea 100%);
}

.regulation-status {
  display: flex;
  gap: 12rpx;
  align-items: center;
  flex-wrap: wrap;
}

.emergency-code {
  display: flex;
  align-items: center;
}

/* Context7风格统计信息 */
.stats-section {
  padding: 48rpx 32rpx;
  background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
  margin: 32rpx 24rpx;
  border-radius: 24rpx;
  box-shadow: 
    0 8rpx 32rpx rgba(0, 0, 0, 0.04),
    0 4rpx 16rpx rgba(0, 0, 0, 0.08),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
  border: 1rpx solid rgba(226, 232, 240, 0.6);
  position: relative;
  overflow: hidden;
}

.stats-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 6rpx;
  background: linear-gradient(90deg, #3b82f6 0%, #8b5cf6 50%, #ec4899 100%);
}

.stats-section::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: radial-gradient(circle at 50% 0%, rgba(59, 130, 246, 0.02) 0%, transparent 50%);
  pointer-events: none;
}

.stats-grid {
  display: flex;
  justify-content: space-around;
  align-items: center;
  position: relative;
  z-index: 1;
}

.stats-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
  padding: 24rpx;
  border-radius: 16rpx;
  transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
  position: relative;
  background: rgba(255, 255, 255, 0.4);
  backdrop-filter: blur(10rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.6);
}

.stats-item:active {
  background: rgba(59, 130, 246, 0.08);
  transform: translateY(-4rpx) scale(1.02);
  box-shadow: 0 8rpx 24rpx rgba(59, 130, 246, 0.15);
}

.stats-number {
  font-size: 56rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 50%, #ec4899 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  text-shadow: 0 4rpx 8rpx rgba(59, 130, 246, 0.15);
  line-height: 1.2;
}

.stats-label {
  font-size: 28rpx;
  color: #475569;
  font-weight: 600;
  letter-spacing: 0.5rpx;
  text-align: center;
}

/* Context7风格标签页 */
.van-tabs {
  background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
  border-radius: 0 0 24rpx 24rpx;
  box-shadow: 
    0 4rpx 16rpx rgba(0, 0, 0, 0.04),
    0 2rpx 8rpx rgba(0, 0, 0, 0.08),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
  position: sticky;
  top: 0;
  z-index: 97;
  backdrop-filter: blur(20rpx);
  margin-top: 0;
  border: 1rpx solid rgba(226, 232, 240, 0.6);
  border-top: none;
}

.van-tab {
  font-weight: 600;
  font-size: 32rpx;
  letter-spacing: 0.5rpx;
  padding: 20rpx 16rpx !important;
  color: #64748b !important;
  transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
}

.van-tab--active {
  color: #3b82f6 !important;
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.05) 0%, rgba(147, 197, 253, 0.05) 100%) !important;
  border-radius: 12rpx;
  margin: 8rpx 4rpx;
}

.van-tabs__line {
  background: linear-gradient(90deg, #3b82f6 0%, #8b5cf6 100%) !important;
  height: 4rpx !important;
  border-radius: 2rpx !important;
}

/* Context7风格Cell组件 */
.van-cell-group {
  margin-bottom: 24rpx;
  border-radius: 20rpx;
  overflow: hidden;
  background: linear-gradient(135deg, #ffffff 0%, #fdfdfd 100%);
  box-shadow: 
    0 2rpx 12rpx rgba(0, 0, 0, 0.04),
    0 1rpx 3rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid rgba(226, 232, 240, 0.6);
}

.van-cell {
  border-radius: 0;
  padding: 32rpx 24rpx !important;
  background: transparent !important;
  border-bottom: 1rpx solid rgba(226, 232, 240, 0.3) !important;
  transition: background-color 0.3s ease;
}

.van-cell:last-child {
  border-bottom: none !important;
}

.van-cell:active {
  background-color: rgba(59, 130, 246, 0.04) !important;
}

/* 优化文字排版 */
.van-cell__title {
  font-size: 32rpx !important;
  font-weight: 600 !important;
  color: #1e293b !important;
  line-height: 1.4 !important;
  margin-bottom: 8rpx !important;
}

.van-cell__label {
  font-size: 28rpx !important;
  color: #64748b !important;
  line-height: 1.6 !important;
  margin-top: 8rpx !important;
  padding-right: 16rpx !important;
}

.van-cell__value {
  font-size: 30rpx !important;
  color: #475569 !important;
  line-height: 1.5 !important;
  text-align: right !important;
  padding-right: 8rpx !important;
  word-wrap: break-word !important;
  word-break: break-word !important;
}

/* Context7风格Tag组件 */
.van-tag--success {
  background: linear-gradient(135deg, #10b981 0%, #059669 100%) !important;
  border: none !important;
  color: white !important;
  font-weight: 600 !important;
  padding: 8rpx 16rpx !important;
  border-radius: 12rpx !important;
  font-size: 22rpx !important;
  box-shadow: 0 2rpx 8rpx rgba(16, 185, 129, 0.3) !important;
}

.van-tag--primary {
  background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%) !important;
  border: none !important;
  color: white !important;
  font-weight: 600 !important;
  padding: 8rpx 16rpx !important;
  border-radius: 12rpx !important;
  font-size: 22rpx !important;
  box-shadow: 0 2rpx 8rpx rgba(59, 130, 246, 0.3) !important;
}

.van-tag--danger {
  background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%) !important;
  border: none !important;
  color: white !important;
  font-weight: 600 !important;
  padding: 8rpx 16rpx !important;
  border-radius: 12rpx !important;
  font-size: 22rpx !important;
  box-shadow: 0 2rpx 8rpx rgba(239, 68, 68, 0.3) !important;
}

.van-tag--warning {
  background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%) !important;
  border: none !important;
  color: white !important;
  font-weight: 600 !important;
  padding: 8rpx 16rpx !important;
  border-radius: 12rpx !important;
  font-size: 22rpx !important;
  box-shadow: 0 2rpx 8rpx rgba(245, 158, 11, 0.3) !important;
}

/* Context7风格空状态 */
.van-empty {
  padding: 80rpx 24rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(248, 250, 252, 0.6) 100%);
  backdrop-filter: blur(10rpx);
  border-radius: 24rpx;
  margin: 24rpx;
  border: 1rpx solid rgba(226, 232, 240, 0.6);
}

/* 详情弹窗样式 */
.detail-popup-container {
  display: flex;
  flex-direction: column;
  height: 100%;
  background-color: #f7f8fa;
}

/* 弹窗标题栏 */
.detail-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 40rpx 32rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 70%, #ff976a 100%);
  color: white;
  position: sticky;
  top: 0;
  z-index: 100;
  box-shadow: 0 4rpx 20rpx rgba(102, 126, 234, 0.3);
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.2);
  min-height: 140rpx;
}

.detail-title {
  font-size: 36rpx;
  font-weight: bold;
  color: white;
}

.close-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 64rpx;
  height: 64rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
  backdrop-filter: blur(10rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.3);
  position: relative;
  z-index: 999;
  flex-shrink: 0;
}

.close-btn:active {
  transform: scale(0.9);
  background: rgba(255, 255, 255, 0.3);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.15);
}

.close-icon {
  font-size: 32rpx !important;
  color: white !important;
  opacity: 0.9;
}

/* 滚动内容区域 */
.detail-content {
  flex: 1;
  padding: 24rpx;
  height: calc(85vh - 140rpx - 120rpx); /* 减去标题栏和底部栏高度 */
  overflow-y: auto;
  overflow-x: hidden;
}

.detail-section {
  margin-bottom: 32rpx;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

/* 携带条件网格 */
.conditions-grid {
  display: flex;
  gap: 24rpx;
  padding: 24rpx;
  background-color: white;
}

.condition-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24rpx;
  border-radius: 12rpx;
  font-size: 28rpx;
  font-weight: 500;
}

.condition-item.allowed {
  background-color: #f6ffed;
  color: #52c41a;
  border: 2rpx solid #b7eb8f;
}

.condition-item.forbidden {
  background-color: #fff2f0;
  color: #ff4d4f;
  border: 2rpx solid #ffadd2;
}

.condition-item van-icon {
  margin-bottom: 8rpx;
  font-size: 32rpx;
}

/* 特殊条件 */
.special-conditions {
  padding: 24rpx;
  background-color: white;
}

.special-conditions van-notice-bar {
  margin-bottom: 16rpx;
}

.special-note {
  display: flex;
  align-items: flex-start;
  gap: 16rpx;
  padding: 16rpx;
  background-color: #fffbe6;
  border: 2rpx solid #ffe58f;
  border-radius: 8rpx;
  margin-top: 16rpx;
}

.note-text {
  flex: 1;
  font-size: 28rpx;
  line-height: 1.6;
  color: #d48806;
  padding-right: 16rpx;
}

/* 描述内容 */
.description-content {
  padding: 24rpx 32rpx 24rpx 24rpx;
  background-color: white;
  width: 100%;
  box-sizing: border-box;
  overflow: visible;
}

.description-text {
  font-size: 30rpx;
  line-height: 1.8;
  color: #333;
  word-wrap: break-word;
  word-break: break-word;
  white-space: pre-wrap;
  overflow: visible;
  text-overflow: inherit;
  max-width: calc(100% - 16rpx);
  width: calc(100% - 16rpx);
  box-sizing: border-box;
  display: block;
  padding-right: 16rpx;
}

/* 危险类型 */
.hazard-item {
  padding: 24rpx 32rpx 24rpx 24rpx;
  background-color: white;
  border-bottom: 1rpx solid #f0f0f0;
}

.hazard-item:last-child {
  border-bottom: none;
}

.hazard-title {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 16rpx;
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

.hazard-content {
  font-size: 30rpx;
  line-height: 1.6;
  color: #666;
  padding-left: 44rpx;
  padding-right: 24rpx;
  word-wrap: break-word;
  word-break: break-word;
  white-space: pre-wrap;
  overflow: visible;
  display: block;
}

/* 应急程序卡片样式 */
.procedure-card {
  margin-bottom: 24rpx;
  background: white;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
  border-left: 8rpx solid transparent;
  transition: all 0.3s ease;
}

.procedure-card:hover {
  transform: translateY(-2rpx);
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.12);
}

/* 泄漏处理卡片 */
.spill-card {
  border-left-color: #ff976a;
  background: linear-gradient(135deg, #fff7f0 0%, #ffffff 40%);
}

/* 灭火程序卡片 */
.fire-card {
  border-left-color: #ee0a24;
  background: linear-gradient(135deg, #fff0f0 0%, #ffffff 40%);
}

/* 其他考虑卡片 */
.other-card {
  border-left-color: #1989fa;
  background: linear-gradient(135deg, #f0f7ff 0%, #ffffff 40%);
}

.procedure-header {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 24rpx 24rpx 16rpx;
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(10rpx);
}

.procedure-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  flex: 1;
}

.procedure-content {
  padding: 0 32rpx 24rpx 24rpx;
}

.procedure-text {
  font-size: 30rpx;
  line-height: 1.8;
  color: #555;
  word-wrap: break-word;
  word-break: break-word;
  white-space: pre-wrap;
  overflow: visible;
  display: block;
  width: calc(100% - 16rpx);
  box-sizing: border-box;
  background: rgba(255, 255, 255, 0.6);
  padding: 20rpx 28rpx 20rpx 20rpx;
  border-radius: 12rpx;
  border: 1rpx solid rgba(0, 0, 0, 0.05);
}

/* 可能包含的物品 */
.possible-items {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  padding: 24rpx;
  background-color: white;
}

.item-tag {
  margin: 0 !important;
}

/* 底部操作栏 */
.detail-footer {
  padding: 32rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 250, 252, 0.9) 100%);
  backdrop-filter: blur(20rpx);
  border-top: 1rpx solid rgba(226, 232, 240, 0.6);
  position: sticky;
  bottom: 0;
  z-index: 100;
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.05);
}

.close-button {
  background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%) !important;
  border: none !important;
  border-radius: 16rpx !important;
  height: 96rpx !important;
  font-size: 32rpx !important;
  font-weight: 600 !important;
  box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.3) !important;
  transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1) !important;
}

.close-button:active {
  transform: scale(0.98) !important;
  box-shadow: 0 2rpx 8rpx rgba(59, 130, 246, 0.4) !important;
}

.button-icon {
  margin-right: 12rpx !important;
  font-size: 28rpx !important;
}

/* 原有的列表样式优化 */
.regulation-cell,
.emergency-cell,
.hidden-cell {
  margin-bottom: 16rpx;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
}

.regulation-status,
.emergency-code {
  display: flex;
  gap: 8rpx;
  align-items: center;
}

/* 🎯 基于Context7最佳实践：危险品主题广告样式 - 标签页下方位置 */

.ad-label {
  display: block;
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.75);
  margin-bottom: 12rpx;
  text-align: center;
  font-weight: 500;
  letter-spacing: 0.5rpx;
}

/* 危险品主题的渐变边框效果 */
 