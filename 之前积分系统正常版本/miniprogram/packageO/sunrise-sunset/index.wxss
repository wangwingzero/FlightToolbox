.container {
  padding: 0 16rpx !important;
  margin: 0 !important;
  background-color: #f7f8fa;
  min-height: 100vh;
  width: 100% !important;
  box-sizing: border-box !important;
  display: block !important;
}

.info-text {
  font-size: 28rpx;
  color: #666;
  line-height: 1.6;
}

/* 夜间定义说明样式 */
.night-definition-note {
  display: flex;
  align-items: flex-start;
  padding: 12rpx 0;
  background-color: #f0f9ff;
  border-radius: 8rpx;
  padding: 16rpx;
  margin: 8rpx 0;
}

.note-text {
  font-size: 24rpx;
  color: #1989fa;
  line-height: 1.5;
  margin-left: 8rpx;
  flex: 1;
}

/* 夜间定义说明样式 */
.night-definition-info {
  display: flex;
  align-items: flex-start;
  padding: 16rpx;
  background-color: #fff7f0;
  border-radius: 8rpx;
  margin: 8rpx 0;
  border-left: 4rpx solid #ff6b35;
}

.definition-text {
  font-size: 26rpx;
  color: #ff6b35;
  line-height: 1.5;
  margin-left: 12rpx;
  flex: 1;
  font-weight: 500;
}

.definition-example {
  display: flex;
  align-items: flex-start;
  padding: 12rpx 16rpx;
  background-color: #f8f9fa;
  border-radius: 6rpx;
  margin: 8rpx 0;
}

.example-text {
  font-size: 24rpx;
  color: #666;
  line-height: 1.4;
  margin-left: 8rpx;
  flex: 1;
}

/* 🎯 基于Context7最佳实践：广告相关样式 - 橙色主题 */
.ad-section {
  margin: 20rpx 0;
  padding: 20rpx;
  background: linear-gradient(135deg, #fff3e0 0%, #ffe0b2 100%);
  border-radius: 16rpx;
  border-left: 8rpx solid #ff9800;
  box-shadow: 0 4rpx 12rpx rgba(255, 152, 0, 0.15);
}

.ad-label {
  display: block;
  font-size: 24rpx;
  color: #ef6c00;
  margin-bottom: 12rpx;
  font-weight: 500;
  text-align: center;
  opacity: 0.8;
}

.ad-container {
  border-radius: 12rpx;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.9);
  box-shadow: 0 2rpx 8rpx rgba(255, 152, 0, 0.1);
}

/* 广告加载状态 */
.ad-loading {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 120rpx;
  color: #ff9800;
  font-size: 28rpx;
}

/* 广告错误状态 */
.ad-error {
  display: none;
}

/* 响应式设计 */
@media (max-width: 750rpx) {
  .ad-section {
    margin: 16rpx 0;
    padding: 16rpx;
  }
  
  .ad-label {
    font-size: 22rpx;
    margin-bottom: 10rpx;
  }
}

/* 🎯 基于Context7最佳实践：夜航时间计算核心样式 */

/* 夜航状态卡片 */
.night-flight-status-card {
  background: linear-gradient(135deg, #1e1e2e 0%, #2d1b69 100%);
  border-radius: 20rpx;
  padding: 32rpx;
  margin: 20rpx 0;
  box-shadow: 0 8rpx 24rpx rgba(29, 27, 105, 0.3);
  border: 2rpx solid rgba(255, 255, 255, 0.1);
}

.status-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
}

.flight-avatar {
  position: relative;
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-ring {
  position: absolute;
  width: 88rpx;
  height: 88rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  top: -4rpx;
  left: -4rpx;
  animation: avatarPulse 2s ease-in-out infinite;
}

@keyframes avatarPulse {
  0%, 100% { transform: scale(1); opacity: 0.7; }
  50% { transform: scale(1.05); opacity: 1; }
}

.flight-info {
  flex: 1;
  margin-left: 20rpx;
}

.flight-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #ffffff;
  margin-bottom: 8rpx;
}

.flight-subtitle {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.7);
  line-height: 1.3;
}

/* 🎨 重新设计的时区切换器 */
.timezone-switcher {
  display: flex;
  flex-direction: column;
  align-items: center;
  cursor: pointer;
  transition: all 0.3s ease;
  padding: 12rpx;
  border-radius: 12rpx;
}

.timezone-switcher:active {
  transform: scale(0.98);
  background-color: rgba(255, 255, 255, 0.05);
}

.timezone-label {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.7);
  margin-bottom: 8rpx;
  font-weight: 500;
}

.timezone-toggle {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.toggle-track {
  position: relative;
  width: 80rpx;
  height: 40rpx;
  border-radius: 20rpx;
  background: rgba(255, 255, 255, 0.2);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  border: 2rpx solid rgba(255, 255, 255, 0.3);
}

.timezone-toggle.beijing .toggle-track {
  background: linear-gradient(135deg, #4fc3f7 0%, #29b6f6 100%);
  border-color: #29b6f6;
  box-shadow: 0 4rpx 12rpx rgba(41, 182, 246, 0.3);
}

.timezone-toggle.utc .toggle-track {
  background: linear-gradient(135deg, #9c27b0 0%, #7b1fa2 100%);
  border-color: #7b1fa2;
  box-shadow: 0 4rpx 12rpx rgba(156, 39, 176, 0.3);
}

.toggle-thumb {
  position: absolute;
  top: 4rpx;
  width: 28rpx;
  height: 28rpx;
  border-radius: 50%;
  background: #ffffff;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.2);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  left: 4rpx;
}

.timezone-toggle.beijing .toggle-thumb {
  transform: translateX(40rpx);
}

.timezone-toggle.utc .toggle-thumb {
  transform: translateX(0);
}

.timezone-text {
  font-size: 26rpx;
  font-weight: 600;
  color: #ffffff;
  min-width: 80rpx;
  text-align: center;
  transition: all 0.3s ease;
}

.timezone-toggle.beijing .timezone-text {
  color: #4fc3f7;
  text-shadow: 0 0 8rpx rgba(79, 195, 247, 0.5);
}

.timezone-toggle.utc .timezone-text {
  color: #ce93d8;
  text-shadow: 0 0 8rpx rgba(206, 147, 216, 0.5);
}

.timezone-hint {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.6);
  margin-top: 6rpx;
  opacity: 0.8;
  transition: all 0.3s ease;
  animation: hintPulse 2s ease-in-out infinite;
}

@keyframes hintPulse {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}

.timezone-switcher:active .timezone-hint {
  opacity: 1;
  color: rgba(255, 255, 255, 0.9);
}

/* 夜间定义说明卡片 */
.night-definition-card {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  padding: 20rpx;
  border-left: 4rpx solid #ff9800;
}

.definition-header {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
}

.definition-title {
  font-size: 26rpx;
  font-weight: 500;
  color: #ffffff;
  margin-left: 12rpx;
}

.definition-content {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.definition-text {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.5;
}

.definition-example {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.6);
  line-height: 1.4;
  font-style: italic;
}

/* 机场输入卡片 */
.airport-input-card {
  background: #ffffff;
  border-radius: 16rpx;
  padding: 24rpx;
  margin: 16rpx 0;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid rgba(0, 0, 0, 0.05);
}

.card-header {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
}

.card-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #333333;
  margin-left: 12rpx;
}

.input-section {
  margin-bottom: 20rpx;
}

.input-wrapper {
  position: relative;
  display: flex;
  align-items: center;
}

.airport-input {
  flex: 1;
  height: 88rpx;
  border: 2rpx solid #e8eaed;
  border-radius: 12rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  color: #333333;
  background-color: #f8f9fa;
  transition: all 0.3s ease;
}

.airport-input:focus {
  border-color: #1976d2;
  background-color: #ffffff;
  box-shadow: 0 0 0 4rpx rgba(25, 118, 210, 0.1);
}

.input-clear {
  position: absolute;
  right: 16rpx;
  width: 32rpx;
  height: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
}

.input-hint {
  font-size: 22rpx;
  color: #666666;
  margin-top: 8rpx;
  line-height: 1.3;
}

/* 机场信息显示 */
.airport-info {
  background: linear-gradient(135deg, #e3f2fd 0%, #f3e5f5 100%);
  border-radius: 12rpx;
  padding: 16rpx;
  margin: 12rpx 0;
  border-left: 4rpx solid #2196f3;
}

.info-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 8rpx;
}

.info-item:last-child {
  margin-bottom: 0;
}

.info-label {
  font-size: 24rpx;
  color: #666666;
  font-weight: 500;
}

.info-value {
  font-size: 26rpx;
  color: #333333;
  font-weight: 600;
}

/* 时间选择器 */
.time-selector {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 88rpx;
  border: 2rpx solid #e8eaed;
  border-radius: 12rpx;
  padding: 0 20rpx;
  background-color: #f8f9fa;
  cursor: pointer;
  transition: all 0.3s ease;
}

.time-selector:active {
  background-color: #e3f2fd;
  border-color: #1976d2;
}

.time-label {
  font-size: 26rpx;
  color: #666666;
  font-weight: 500;
}

.time-value {
  font-size: 28rpx;
  color: #333333;
  font-weight: 600;
}

/* 计算按钮 */
.calculate-section {
  margin: 32rpx 0;
}

.calculate-button {
  position: relative;
  height: 96rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(102, 126, 234, 0.4);
  cursor: pointer;
  overflow: hidden;
  transition: all 0.3s ease;
}

.calculate-button:active {
  transform: translateY(2rpx);
  box-shadow: 0 4rpx 16rpx rgba(102, 126, 234, 0.3);
}

.button-text {
  font-size: 32rpx;
  font-weight: 600;
  color: #ffffff;
  margin-left: 12rpx;
}

.button-ripple {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, transparent 70%);
  opacity: 0;
  transition: opacity 0.3s ease;
}

.calculate-button:active .button-ripple {
  opacity: 1;
}

/* 结果卡片 */
.results-card {
  background: #ffffff;
  border-radius: 20rpx;
  padding: 32rpx;
  margin: 24rpx 0;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.1);
  border: 1rpx solid rgba(0, 0, 0, 0.05);
}

.results-header {
  display: flex;
  align-items: center;
  margin-bottom: 24rpx;
  padding-bottom: 16rpx;
  border-bottom: 2rpx solid #f0f0f0;
}

.results-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #333333;
  margin-left: 12rpx;
}

/* 结果网格 */
.results-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.result-item {
  background: #f8f9fa;
  border-radius: 12rpx;
  padding: 20rpx;
  text-align: center;
  border: 1rpx solid #e8eaed;
  transition: all 0.3s ease;
}

.result-item.primary {
  background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
  border-color: #2196f3;
}

.result-item.highlight {
  background: linear-gradient(135deg, #fff3e0 0%, #ffcc02 100%);
  border-color: #ff9800;
}

.result-item.percentage {
  background: linear-gradient(135deg, #f3e5f5 0%, #e1bee7 100%);
  border-color: #9c27b0;
}

.result-label {
  display: block;
  font-size: 22rpx;
  color: #666666;
  margin-bottom: 8rpx;
  font-weight: 500;
}

.result-value {
  display: block;
  font-size: 28rpx;
  color: #333333;
  font-weight: 700;
}

/* 两地日出日落时间 - 大卡片样式 */
.sun-times-section {
  border-top: 2rpx solid #f0f0f0;
  padding-top: 24rpx;
}

.sun-times-header {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
}

.sun-times-title {
  font-size: 26rpx;
  font-weight: 600;
  color: #333333;
  margin-left: 8rpx;
}

/* 地点分组 */
.location-sun-times {
  margin-bottom: 20rpx;
}

.location-title {
  font-size: 24rpx;
  font-weight: 600;
  color: #666666;
  margin-bottom: 12rpx;
  padding-left: 8rpx;
}

/* 日出日落主要结果 - 复用日出日落界面的样式 */
.sun-times-main {
  display: flex;
  gap: 16rpx;
  margin-bottom: 16rpx;
}

.sun-time-card {
  flex: 1;
  border-radius: 16rpx;
  padding: 24rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
  transition: all 0.3s ease;
}

.sun-time-card.sunrise {
  background: linear-gradient(135deg, #fff3e0 0%, #ffcc02 100%);
  border: 2rpx solid #ff8f00;
}

.sun-time-card.sunset {
  background: linear-gradient(135deg, #fce4ec 0%, #f8bbd9 100%);
  border: 2rpx solid #e91e63;
}

.sun-icon {
  font-size: 48rpx;
  line-height: 1;
}

.sun-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.sun-title {
  font-size: 22rpx;
  color: #666666;
  font-weight: 500;
}

.sun-time {
  font-size: 28rpx;
  color: #333333;
  font-weight: 700;
}

.sun-subtitle {
  font-size: 20rpx;
  color: #999999;
  font-weight: 400;
}

/* 通用样式 */
.emoji-icon {
  font-size: 32rpx;
}

.emoji-icon.small {
  font-size: 24rpx;
}

/* 🎯 基于Context7最佳实践：出发地和到达地之间广告样式 - 橙色主题融合 */
.departure-arrival-middle-ad {
  margin: 32rpx 0;
  padding: 0;
  background: linear-gradient(135deg, #fff8e1 0%, #ffecb3 100%);
  border-radius: 16rpx;
  border: 2rpx solid rgba(255, 152, 0, 0.2);
  box-shadow: 0 6rpx 16rpx rgba(255, 152, 0, 0.15);
  position: relative;
  animation: departureArrivalAdSlideIn 0.7s ease-out;
}

@keyframes departureArrivalAdSlideIn {
  0% {
    opacity: 0;
    transform: translateY(20rpx) scale(0.95);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.departure-arrival-middle-ad .ad-label {
  color: #e65100;
  font-weight: 600;
  text-align: center;
  font-size: 26rpx;
  margin-bottom: 16rpx;
  padding-top: 20rpx;
}

/* 出发地和到达地之间广告响应式适配 */
@media (max-width: 375px) {
  .departure-arrival-middle-ad {
    margin: 20rpx 0;
  }
}

/* 出发地和到达地之间广告暗色模式适配 */
@media (prefers-color-scheme: dark) {
  .departure-arrival-middle-ad {
    background: linear-gradient(135deg, #2d2d2d 0%, #3d3d3d 100%);
    border-color: rgba(255, 152, 0, 0.3);
  }
  
  .departure-arrival-middle-ad .ad-label {
    color: #ffb74d;
  }
}

/* 🎯 基于Context7最佳实践：日出日落查询结果底部广告样式 - 橙色主题融合 */
.sunrise-sunset-bottom-ad {
  margin: 32rpx 0 20rpx 0;
  padding: 0;
  background: linear-gradient(135deg, #fff3e0 0%, #ffe0b2 100%);
  border-radius: 16rpx;
  border: 2rpx solid rgba(255, 152, 0, 0.25);
  box-shadow: 0 6rpx 16rpx rgba(255, 152, 0, 0.15);
  position: relative;
  animation: sunriseBottomAdSlideIn 0.8s ease-out;
}

@keyframes sunriseBottomAdSlideIn {
  0% {
    opacity: 0;
    transform: translateY(25rpx) scale(0.95);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.sunrise-sunset-bottom-ad .ad-label {
  color: #e65100;
  font-weight: 600;
  text-align: center;
  font-size: 26rpx;
  margin-bottom: 16rpx;
  padding-top: 20rpx;
}

/* 日出日落底部广告响应式适配 */
@media (max-width: 375px) {
  .sunrise-sunset-bottom-ad {
    margin: 20rpx 0 16rpx 0;
  }
}

/* 日出日落底部广告暗色模式适配 */
@media (prefers-color-scheme: dark) {
  .sunrise-sunset-bottom-ad {
    background: linear-gradient(135deg, #2d2d2d 0%, #3d3d3d 100%);
    border-color: rgba(255, 152, 0, 0.3);
  }
  
  .sunrise-sunset-bottom-ad .ad-label {
    color: #ffb74d;
  }
}

 