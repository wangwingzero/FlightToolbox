# 音频分包命名标准

## 🎯 命名规范

### 统一格式
```
package[CountryName]
```

### 国家名称规则
- 使用**英文国家名称**
- 采用**首字母大写的驼峰命名**
- 避免缩写，使用**完整清晰的名称**

## 📋 已实施的分包命名

### ✅ 已完成迁移（有真实录音）
| 国家 | 旧名称 | 新名称 | 状态 | 录音数量 |
|------|-------|--------|------|----------|
| 🇯🇵 日本 | `packageJ` | `packageJapan` | ✅ 已迁移 | 45个 |
| 🇵🇭 菲律宾 | `packageK` | `packagePhilippines` | ✅ 已迁移 | 32个 |
| 🇰🇷 韩国 | `packageL` | `packageKorean` | ✅ 已迁移 | 20个 |

### 🆕 未来计划分包（使用新格式）
| 国家 | 新名称 | 状态 | 备注 |
|------|--------|------|------|
| 🇩🇪 德国 | `packageGermany` | 📋 规划中 | 法兰克福机场 |
| 🇺🇸 美国 | `packageUSA` | 📋 规划中 | 纽约肯尼迪机场 |
| 🇦🇺 澳大利亚 | `packageAustralia` | 📋 规划中 | 悉尼机场 |
| 🇿🇦 南非 | `packageSouthAfrica` | 📋 规划中 | 开普敦机场 |

## 🔧 特殊情况处理

### 多单词国家名
- **南非**: `packageSouthAfrica` (驼峰命名)
- **新西兰**: `packageNewZealand` (如需添加)
- **英国**: `packageUnitedKingdom` (如需添加)

### 地区/城市分包
如果同一国家有多个主要机场，可按城市细分：
- `packageJapanTokyo` (东京)
- `packageJapanOsaka` (大阪)
- `packageUSANewYork` (纽约)
- `packageUSALosAngeles` (洛杉矶)

## 📁 目录结构示例

```
miniprogram/
├── packageJapan/              # 日本音频分包
│   ├── index.js
│   ├── index.json
│   ├── index.wxml
│   └── *.mp3                  # 音频文件
├── packagePhilippines/        # 菲律宾音频分包
├── packageKorean/             # 韩国音频分包
├── packageGermany/            # 德国音频分包（未来）
├── packageUSA/                # 美国音频分包（未来）
├── packageAustralia/          # 澳大利亚音频分包（未来）
└── packageSouthAfrica/        # 南非音频分包（未来）
```

## ⚙️ 配置文件关联

每个分包需要在以下文件中配置：

### 1. `app.json` 分包声明
```json
{
  "subPackages": [
    {
      "root": "packageJapan",
      "name": "japanAudioPackage",
      "pages": ["index"]
    }
  ]
}
```

### 2. `utils/audio-config.js` 路径配置
```javascript
{
  id: 'japan',
  packageName: 'packageJapan',
  audioPath: '/packageJapan/',
  // ...其他配置
}
```

### 3. `pages/audio-player/index.ts` 路径映射
```javascript
const regionPathMap = {
  'japan': '/packageJapan/',
  // ...其他映射
};
```

## 🚀 新增分包流程

### 1. 创建分包文件夹
```bash
mkdir miniprogram/package[CountryName]
```

### 2. 添加基础文件
```bash
touch miniprogram/package[CountryName]/index.js
touch miniprogram/package[CountryName]/index.json  
touch miniprogram/package[CountryName]/index.wxml
```

### 3. 放置音频文件
```bash
cp *.mp3 miniprogram/package[CountryName]/
```

### 4. 更新配置文件
- [ ] `app.json` - 添加分包声明
- [ ] `utils/audio-config.js` - 添加机场配置
- [ ] `pages/audio-player/index.ts` - 添加路径映射
- [ ] `data/regions/[country].js` - 创建音频数据文件

## 🎉 命名规范的优势

### ✅ 清晰性
- `packageKorean` 比 `packageL` 一目了然
- 新开发者无需查阅文档即可理解

### ✅ 可扩展性  
- 支持无限数量的国家
- 不受字母编号限制（A-Z只有26个）

### ✅ 维护性
- 重构时不易出错
- 配置文件更易理解和维护

### ✅ 协作友好
- 团队成员能快速定位分包
- 降低沟通成本

## 📝 更新历史

- **2024-07-05**: 建立新的命名标准
- **2024-07-05**: 完成日本、菲律宾、韩国分包重命名
- **2024-07-05**: 更新所有相关配置文件

---

**维护者**: FlightToolbox 开发团队  
**最后更新**: 2024-07-05