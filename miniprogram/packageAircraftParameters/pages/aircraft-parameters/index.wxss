/* 🛩️ 飞机参数页面 - iOS Premium Style */

/* 🌟 全局变量与页面容器 */
.page-content {
  min-height: 100vh;
  /* 高级灰背景，带有极淡的蓝色倾向 */
  background: #F5F5F7;
  position: relative;
  padding-bottom: 60rpx;
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, Segoe UI, Arial, Roboto, 'PingFang SC', 'miui', 'Hiragino Sans GB', 'Microsoft Yahei', sans-serif;
  -webkit-font-smoothing: antialiased;
}

/* 顶部氛围光晕 */
.page-content::before {
  content: '';
  position: absolute;
  top: -20vh;
  left: -20vw;
  width: 140vw;
  height: 100vh;
  background: radial-gradient(circle at 50% 50%, rgba(59, 130, 246, 0.08), rgba(255, 255, 255, 0) 70%);
  pointer-events: none;
  z-index: 0;
}

/* 📱 标题区域 */
.section-title {
  font-size: 48rpx; /* iOS Large Title style */
  font-weight: 800;
  color: #1d1d1f;
  margin: 40rpx 40rpx 40rpx;
  letter-spacing: -0.5px;
  position: relative;
  z-index: 1;
}

/* 🏗️ 分类网格系统 */
.category-section {
  padding: 0 32rpx;
  position: relative;
  z-index: 1;
}

.category-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24rpx;
  padding-bottom: 40rpx;
}

/* 💎 重新设计的卡片 - Apple Style */
.category-card {
  /* 使用 CSS 变量 --category-color 控制颜色主题 */
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-radius: 32rpx;
  padding: 40rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  position: relative;
  overflow: hidden;
  box-shadow: 
    0 8rpx 24rpx -6rpx rgba(0, 0, 0, 0.05), 
    0 0 0 1rpx rgba(255, 255, 255, 0.5) inset;
  transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
  height: 320rpx; /* 固定高度，确保整齐 */
  box-sizing: border-box;
}

/* 卡片激活/点击态 */
.category-card:active {
  transform: scale(0.96);
  background: rgba(255, 255, 255, 0.95);
}

/* 卡片背景光晕 - 使用主题色 */
.category-card::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(circle at 50% 0%, var(--category-color), transparent 70%);
  opacity: 0.08; /* 极淡的背景色 */
  z-index: 0;
  transition: opacity 0.3s ease;
}

/* 选中或强调时的光晕增强 */
.category-card:active::after {
  opacity: 0.15;
}

/* 图标包装器 */
.category-icon-wrapper {
  width: 120rpx;
  height: 120rpx;
  background: #ffffff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 24rpx;
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.06);
  position: relative;
  z-index: 1;
}

.category-icon {
  font-size: 56rpx;
}

/* 信息区域 */
.category-info {
  position: relative;
  z-index: 1;
}

.category-name {
  font-size: 34rpx;
  font-weight: 700;
  color: #1d1d1f;
  margin-bottom: 8rpx;
}

.category-count {
  font-size: 24rpx;
  font-weight: 600;
  color: var(--category-color); /* 使用主题色 */
  background: rgba(255, 255, 255, 0.6);
  padding: 6rpx 16rpx;
  border-radius: 100rpx;
  display: inline-block;
}

/* 🔙 返回与搜索区域 */
.list-section {
  padding: 20rpx 32rpx;
  position: relative;
  z-index: 1;
}

.back-button {
  display: inline-flex;
  align-items: center;
  color: #007AFF; /* iOS Blue */
  font-size: 32rpx;
  font-weight: 500;
  margin-bottom: 30rpx;
  padding: 10rpx 0;
}

.back-button text {
  margin-left: 8rpx;
}

.search-section {
  margin-bottom: 30rpx;
}

.search-section .van-search {
  padding: 0 !important;
  background: transparent !important;
}

.search-section .van-search__content {
  background: #ffffff !important;
  border-radius: 20rpx !important;
  padding: 16rpx 24rpx !important;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.03) !important;
}

/* 📋 列表头部 */
.current-category-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  margin-bottom: 24rpx;
  padding: 0 8rpx;
}

.current-category-title {
  font-size: 40rpx;
  font-weight: 800;
  color: #1d1d1f;
  display: flex;
  align-items: center;
}

.category-icon-small {
  font-size: 40rpx;
  margin-right: 16rpx;
}

.current-category-count {
  font-size: 26rpx;
  color: #86868b;
  font-weight: 500;
  margin-bottom: 6rpx;
}

/* ✈️ 机型列表 */
.aircraft-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.aircraft-item {
  background: #ffffff;
  border-radius: 24rpx;
  padding: 32rpx;
  box-shadow: 
    0 4rpx 16rpx rgba(0, 0, 0, 0.04), 
    0 1rpx 2rpx rgba(0, 0, 0, 0.02);
  transition: all 0.2s ease;
  border: 1rpx solid rgba(0,0,0,0.02);
}

.aircraft-item:active {
  transform: scale(0.98);
  background: #fdfdfd;
}

.aircraft-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
  padding-bottom: 16rpx;
  border-bottom: 1rpx solid #f0f0f5;
}

.aircraft-model {
  font-size: 36rpx;
  font-weight: 700;
  color: #1d1d1f;
}

.aircraft-code {
  font-size: 24rpx;
  font-weight: 600;
  color: #86868b;
  background: #f5f5f7;
  padding: 6rpx 12rpx;
  border-radius: 8rpx;
}

.aircraft-info {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16rpx;
  margin-bottom: 20rpx;
}

.info-item {
  background: #F5F5F7;
  border-radius: 16rpx;
  padding: 16rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.info-icon {
  font-size: 28rpx;
  margin-bottom: 8rpx;
}

.info-content .label {
  font-size: 20rpx;
  color: #86868b;
  margin-bottom: 4rpx;
}

.info-content .value {
  font-size: 24rpx;
  font-weight: 600;
  color: #1d1d1f;
}

.aircraft-footer {
  display: flex;
  justify-content: center;
  margin-top: 16rpx;
}

.view-details {
  font-size: 26rpx;
  color: #007AFF;
  font-weight: 600;
  display: flex;
  align-items: center;
}

.view-details::after {
  content: '→';
  margin-left: 8rpx;
  font-family: sans-serif;
  transition: transform 0.2s ease;
}

.aircraft-item:active .view-details::after {
  transform: translateX(6rpx);
}

/* 📝 详情弹窗 - Apple Maps Sheet Style */
.detail-popup {
  background: #ffffff;
  height: 100%;
  display: flex;
  flex-direction: column;
}

.detail-header {
  padding: 40rpx;
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(20px);
  position: sticky;
  top: 0;
  z-index: 10;
  border-bottom: 1rpx solid rgba(0,0,0,0.05);
}

.detail-icon {
  font-size: 64rpx;
  margin-right: 24rpx;
}

.detail-title {
  font-size: 48rpx;
  font-weight: 800;
  color: #1d1d1f;
  line-height: 1.1;
  margin-bottom: 8rpx;
}

.detail-subtitle {
  font-size: 28rpx;
  color: #86868b;
  font-weight: 500;
}

.close-button {
  width: 60rpx;
  height: 60rpx;
  background: #F2F2F7;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #8e8e93;
}

.detail-content {
  flex: 1;
  padding: 40rpx;
  background: #ffffff;
}

.param-section {
  margin-bottom: 50rpx;
}

.section-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #1d1d1f;
  margin: 0 0 24rpx 0;
  display: flex;
  align-items: center;
}

.param-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
}

.param-item {
  background: #F5F5F7;
  border-radius: 20rpx;
  padding: 24rpx;
  display: flex;
  flex-direction: column;
}

.param-label {
  font-size: 24rpx;
  color: #86868b;
  margin-bottom: 12rpx;
}

.param-value {
  font-size: 30rpx;
  font-weight: 600;
  color: #1d1d1f;
}

/* 广告 Banner */
.ad-banner-container {
  margin: 32rpx 32rpx 0;
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.05);
}