/* 🛩️ 飞机参数页面 - 精美UX设计 */

/* 🌟 页面容器 */
.page-content {
  min-height: 100vh;
  background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 50%, #cbd5e1 100%);
  position: relative;
  overflow-x: hidden;
}

.page-content::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background:
    radial-gradient(circle at 20% 20%, rgba(59, 130, 246, 0.1) 0%, transparent 50%),
    radial-gradient(circle at 80% 40%, rgba(139, 92, 246, 0.08) 0%, transparent 50%),
    radial-gradient(circle at 40% 80%, rgba(16, 185, 129, 0.06) 0%, transparent 50%);
  animation: breathe 8s ease-in-out infinite;
  pointer-events: none;
  z-index: 0;
}

.page-content::after {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image:
    radial-gradient(2rpx 2rpx at 20rpx 30rpx, rgba(59, 130, 246, 0.3), transparent),
    radial-gradient(2rpx 2rpx at 40rpx 70rpx, rgba(139, 92, 246, 0.3), transparent),
    radial-gradient(1rpx 1rpx at 90rpx 40rpx, rgba(16, 185, 129, 0.3), transparent),
    radial-gradient(1rpx 1rpx at 130rpx 80rpx, rgba(59, 130, 246, 0.3), transparent),
    radial-gradient(2rpx 2rpx at 160rpx 30rpx, rgba(139, 92, 246, 0.3), transparent);
  background-repeat: repeat;
  background-size: 200rpx 100rpx;
  animation: sparkle 15s linear infinite;
  pointer-events: none;
  z-index: 0;
}

@keyframes breathe {
  0%, 100% { opacity: 0.6; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.05); }
}

@keyframes sparkle {
  0% { transform: translateY(0); }
  100% { transform: translateY(-200rpx); }
}

@keyframes float {
  0%, 100% { transform: translate(0, 0) rotate(0deg); }
  33% { transform: translate(-30rpx, -30rpx) rotate(120deg); }
  66% { transform: translate(30rpx, -20rpx) rotate(240deg); }
}

/* 📱 页面头部 */
.page-header {
  text-align: center;
  padding: 60rpx 40rpx 40rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 0 0 40rpx 40rpx;
  margin-bottom: 40rpx;
  box-shadow: 0 8rpx 32rpx rgba(59, 130, 246, 0.1);
  position: relative;
  overflow: hidden;
  z-index: 2;
}

.page-header::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.05) 0%, rgba(139, 92, 246, 0.05) 100%);
  z-index: -1;
}

.header-icon {
  font-size: 64rpx;
  margin-bottom: 20rpx;
  animation: float 3s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10rpx); }
}

.header-title {
  font-size: 48rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 50%, #10b981 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 16rpx;
  letter-spacing: 2rpx;
}

.header-subtitle {
  font-size: 28rpx;
  color: #64748b;
  font-weight: 500;
  opacity: 0.8;
}

/* 🎯 分类选择区域 */
.category-section {
  padding: 0 32rpx 40rpx;
  position: relative;
  z-index: 2;
}

.section-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1e293b;
  margin-bottom: 32rpx;
  text-align: center;
}

/* 分类卡片网格 */
.category-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24rpx;
}

/* 分类卡片 */
.category-card {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 24rpx;
  padding: 48rpx 32rpx;
  text-align: center;
  border: 3rpx solid;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.08);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.category-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, transparent, rgba(59, 130, 246, 0.05));
  opacity: 0;
  transition: opacity 0.3s ease;
}

.category-card:active {
  transform: translateY(-8rpx) scale(0.98);
  box-shadow: 0 16rpx 48rpx rgba(59, 130, 246, 0.2);
}

.category-card:active::before {
  opacity: 1;
}

.category-icon {
  font-size: 72rpx;
  margin-bottom: 20rpx;
  display: block;
  animation: float 3s ease-in-out infinite;
}

.category-name {
  font-size: 36rpx;
  font-weight: 700;
  color: #1e293b;
  margin-bottom: 12rpx;
}

.category-count {
  font-size: 24rpx;
  font-weight: 600;
}

/* 📋 列表区域 */
.list-section {
  position: relative;
  z-index: 2;
}

/* 返回按钮 */
.back-button {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 20rpx 32rpx;
  margin: 0 32rpx 24rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(59, 130, 246, 0.1);
  font-size: 28rpx;
  color: #3b82f6;
  font-weight: 600;
  transition: all 0.3s ease;
}

.back-button:active {
  transform: scale(0.96);
  background: rgba(59, 130, 246, 0.1);
}

/* 当前分类头部 */
.current-category-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 32rpx;
  margin: 0 32rpx 24rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(59, 130, 246, 0.1);
}

.current-category-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1e293b;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.category-icon-small {
  font-size: 36rpx;
}

.current-category-count {
  font-size: 24rpx;
  color: #64748b;
  font-weight: 600;
}

/* 飞机列表 */
.aircraft-list {
  padding: 0 32rpx 40rpx;
  position: relative;
  z-index: 2;
}

.aircraft-item {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 24rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.2);
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.06);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.aircraft-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 6rpx;
  background: linear-gradient(90deg, #3b82f6, #8b5cf6, #06b6d4);
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.4s ease;
}

.aircraft-item:active {
  transform: translateY(-8rpx) scale(0.98);
  box-shadow: 0 16rpx 48rpx rgba(59, 130, 246, 0.15);
}

.aircraft-item:active::before {
  transform: scaleX(1);
}

/* 🏷️ 飞机头部 */
.aircraft-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.aircraft-model {
  font-size: 36rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #1e293b 0%, #475569 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  letter-spacing: 0.5rpx;
  flex: 1;
}

.aircraft-code {
  font-size: 22rpx;
  color: #ffffff;
  background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  font-weight: 600;
  letter-spacing: 0.5rpx;
  box-shadow: 0 4rpx 12rpx rgba(59, 130, 246, 0.3);
  position: relative;
  overflow: hidden;
}

.aircraft-code::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.6s ease;
}

.aircraft-item:active .aircraft-code::before {
  left: 100%;
}

/* 📊 飞机信息网格 */
.aircraft-info {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.info-item {
  display: flex;
  align-items: center;
  padding: 20rpx 16rpx;
  background: rgba(59, 130, 246, 0.05);
  border-radius: 16rpx;
  border: 1rpx solid rgba(59, 130, 246, 0.1);
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.info-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(59, 130, 246, 0.1), transparent);
  transition: left 0.5s ease;
}

.info-item:hover {
  background: rgba(59, 130, 246, 0.08);
  transform: translateY(-2rpx);
}

.info-item:hover::before {
  left: 100%;
}

.info-icon {
  font-size: 28rpx;
  margin-right: 12rpx;
  opacity: 0.8;
}

.info-content {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.info-content .label {
  font-size: 20rpx;
  color: #64748b;
  font-weight: 500;
  margin-bottom: 4rpx;
}

.info-content .value {
  font-size: 26rpx;
  color: #1e293b;
  font-weight: 600;
  line-height: 1.2;
}

/* 🔗 飞机底部 */
.aircraft-footer {
  display: flex;
  justify-content: flex-end;
  align-items: center;
}

.view-details {
  font-size: 24rpx;
  color: #3b82f6;
  font-weight: 500;
  padding: 8rpx 16rpx;
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.view-details:active {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.2) 0%, rgba(139, 92, 246, 0.2) 100%);
  transform: scale(0.95);
}

/* 🎨 详情弹窗 - 现代化设计 */
.detail-popup {
  height: 100%;
  display: flex;
  flex-direction: column;
  background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
  position: relative;
  overflow: hidden;
}

.detail-popup::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 8rpx;
  background: linear-gradient(90deg, #3b82f6, #8b5cf6, #06b6d4, #10b981);
  z-index: 1;
}

.detail-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 40rpx 40rpx 32rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-bottom: 2rpx solid rgba(59, 130, 246, 0.1);
  position: relative;
  z-index: 2;
}

.detail-header-content {
  display: flex;
  align-items: center;
  flex: 1;
}

.detail-icon {
  font-size: 48rpx;
  margin-right: 20rpx;
  animation: float 3s ease-in-out infinite;
}

.detail-title-group {
  flex: 1;
}

.detail-title {
  font-size: 40rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #1e293b 0%, #3b82f6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  letter-spacing: 0.5rpx;
  margin-bottom: 8rpx;
}

.detail-subtitle {
  font-size: 24rpx;
  color: #64748b;
  font-weight: 500;
}

.close-button {
  width: 72rpx;
  height: 72rpx;
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.1) 0%, rgba(220, 38, 38, 0.1) 100%);
  border: 2rpx solid rgba(239, 68, 68, 0.2);
  border-radius: 50%;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
  box-shadow: 0 4rpx 16rpx rgba(239, 68, 68, 0.1);
}

.close-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.05) 0%, rgba(220, 38, 38, 0.05) 100%);
  border-radius: 50%;
  transform: scale(0);
  transition: transform 0.3s ease;
}

.close-button:hover::before {
  transform: scale(1);
}

.close-button:active {
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.2) 0%, rgba(220, 38, 38, 0.2) 100%);
  border-color: rgba(239, 68, 68, 0.4);
  transform: scale(0.92);
  box-shadow: 0 2rpx 8rpx rgba(239, 68, 68, 0.2);
}

.close-button .van-icon {
  transition: all 0.3s ease;
  z-index: 1;
}

.close-button:active .van-icon {
  transform: rotate(90deg);
}



.detail-content {
  flex: 1;
  padding: 32rpx 40rpx 80rpx;
  position: relative;
  z-index: 2;
  box-sizing: border-box;
  min-height: auto;
}

/* 📊 参数区域 */
.param-section {
  margin-bottom: 48rpx;
  position: relative;
}

.section-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1e293b;
  margin-bottom: 24rpx;
  padding: 16rpx 24rpx;
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
  border-radius: 16rpx;
  border-left: 6rpx solid #3b82f6;
  position: relative;
  overflow: hidden;
}

.section-title::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.05), rgba(139, 92, 246, 0.05));
  z-index: -1;
}

/* 参数网格 */
.param-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280rpx, 1fr));
  gap: 20rpx;
}

/* 参数卡片 */
.param-item {
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(20rpx);
  border-radius: 20rpx;
  padding: 24rpx;
  border: 2rpx solid rgba(59, 130, 246, 0.1);
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.05);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.param-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(90deg, #3b82f6, #8b5cf6);
  opacity: 0.5;
}

/* 参数标签 */
.param-label {
  display: block;
  font-size: 24rpx;
  color: #64748b;
  margin-bottom: 12rpx;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5rpx;
}

/* 参数值 */
.param-value {
  display: block;
  font-size: 28rpx;
  color: #1e293b;
  font-weight: 700;
  word-break: break-all;
  line-height: 1.3;
  position: relative;
}

































/* 响应式设计 */
@media (max-width: 750rpx) {
  .param-grid {
    grid-template-columns: 1fr;
  }
  
  .aircraft-info {
    flex-direction: column;
    gap: 8rpx;
  }
}

