/**
 * 应用全局配置
 * 包含广告、API等配置信息
 */

var AppConfig = {
  // 广告配置（已废弃 - 激励视频广告功能已于2025-10-19移除）
  // 当前使用的广告类型：横幅广告（Banner Ad）、格子广告（Grid Ad）
  // 广告位ID直接在各页面的WXML中配置，无需在此处管理
  ad: {
    // 保留空对象以避免代码引用报错
  },

  // 其他配置...
};

module.exports = AppConfig;