/* 缩写查询页面样式 - Enhanced Apple Design Style */

/* ========================================
   🎨 核心设计系统 (Enhanced Apple Design System)
   ======================================== */
page {
  --system-background: #F2F4F8;
  --card-background: #FFFFFF;
  --text-primary: #1C1C1E;
  --text-secondary: #8E8E93;
  --text-tertiary: #C7C7CC;
  --separator: #3C3C432E;
  
  /* 增强的系统色 - 更饱和鲜艳 */
  --system-blue: #007AFF;
  --system-blue-light: #5AC8FA;
  --system-indigo: #5856D6;
  --system-indigo-light: #7D7AFF;
  --system-teal: #30D5C8;
  --system-teal-light: #64D2FF;
  --system-orange: #FF9500;
  --system-orange-light: #FFB340;
  --system-pink: #FF2D55;
  --system-pink-light: #FF6482;
  --system-purple: #AF52DE;
  --system-green: #34C759;
  --system-red: #FF3B30;
  --system-yellow: #FFCC00;
  
  /* 分类专属渐变色 */
  --gradient-aip: linear-gradient(135deg, #007AFF 0%, #5AC8FA 100%);
  --gradient-airbus: linear-gradient(135deg, #5856D6 0%, #7D7AFF 100%);
  --gradient-boeing: linear-gradient(135deg, #30D5C8 0%, #64D2FF 100%);
  --gradient-comac: linear-gradient(135deg, #FF9500 0%, #FFB340 100%);
  --gradient-jeppesen: linear-gradient(135deg, #FF2D55 0%, #FF6482 100%);
  
  /* 增强的阴影系统 */
  --shadow-card: 0 8rpx 28rpx rgba(0, 0, 0, 0.08);
  --shadow-floating: 0 12rpx 36rpx rgba(0, 0, 0, 0.15);
  --shadow-pressed: 0 2rpx 8rpx rgba(0, 0, 0, 0.12);
  --shadow-glow-blue: 0 8rpx 24rpx rgba(0, 122, 255, 0.15);
  --shadow-glow-indigo: 0 8rpx 24rpx rgba(88, 86, 214, 0.15);
  --shadow-glow-teal: 0 8rpx 24rpx rgba(48, 213, 200, 0.15);
  --shadow-glow-orange: 0 8rpx 24rpx rgba(255, 149, 0, 0.15);
  --shadow-glow-pink: 0 8rpx 24rpx rgba(255, 45, 85, 0.15);
}

.container {
  min-height: 100vh;
  background: linear-gradient(180deg, #E8EDF5 0%, #F2F4F8 100%);
  padding-bottom: calc(env(safe-area-inset-bottom) + 20rpx);
  position: relative;
}

/* 顶部装饰渐变 - 更柔和的过渡 */
.container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 400rpx;
  background: radial-gradient(ellipse at top, rgba(0, 122, 255, 0.08) 0%, transparent 70%);
  z-index: 0;
  pointer-events: none;
}

/* ========================================
   🏷️ 标签导航 (Enhanced Segmented Control)
   ======================================== */
.modern-tabs-container {
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  padding: 16rpx 24rpx;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.06);
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.04);
}

.tabs-wrapper {
  display: flex;
  background: linear-gradient(135deg, #F8F9FA 0%, #F2F2F7 100%);
  padding: 8rpx;
  border-radius: 24rpx;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  gap: 6rpx;
  box-shadow: inset 0 2rpx 4rpx rgba(0, 0, 0, 0.04);
}

/* 隐藏滚动条 */
.tabs-wrapper::-webkit-scrollbar {
  display: none;
}

.modern-tab {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 18rpx 4rpx;
  border-radius: 20rpx;
  transition: all 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  min-width: 120rpx;
  position: relative;
  overflow: hidden;
}

/* 为每个标签添加渐变背景 */
.modern-tab.active {
  background: #FFFFFF;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1),
              0 2rpx 6rpx rgba(0, 0, 0, 0.06);
  transform: scale(1.02);
}

/* 渐变装饰条 */
.modern-tab.active::before {
  content: '';
  position: absolute;
  bottom: 0;
  left: 20%;
  right: 20%;
  height: 4rpx;
  border-radius: 4rpx 4rpx 0 0;
  background: var(--system-blue);
  opacity: 0.8;
  transition: all 0.3s;
}

/* 不同分类的装饰条颜色 */
.modern-tab[data-tab="all"].active::before {
  background: linear-gradient(90deg, #007AFF 0%, #5AC8FA 100%);
}
.modern-tab[data-tab="AIP"].active::before {
  background: var(--gradient-aip);
}
.modern-tab[data-tab="Airbus"].active::before {
  background: var(--gradient-airbus);
}
.modern-tab[data-tab="Boeing"].active::before {
  background: var(--gradient-boeing);
}
.modern-tab[data-tab="COMAC"].active::before {
  background: var(--gradient-comac);
}
.modern-tab[data-tab="Jeppesen"].active::before {
  background: var(--gradient-jeppesen);
}

.tab-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
  position: relative;
  z-index: 1;
}

.tab-icon {
  font-size: 32rpx;
  margin-bottom: 6rpx;
  opacity: 1;
  color: #8E8E93;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  filter: grayscale(0.3);
}

.modern-tab.active .tab-icon {
  color: var(--system-blue);
  filter: grayscale(0);
  transform: scale(1.1);
}

/* 不同分类的图标颜色 */
.modern-tab[data-tab="AIP"].active .tab-icon {
  color: var(--system-blue);
}
.modern-tab[data-tab="Airbus"].active .tab-icon {
  color: var(--system-indigo);
}
.modern-tab[data-tab="Boeing"].active .tab-icon {
  color: var(--system-teal);
}
.modern-tab[data-tab="COMAC"].active .tab-icon {
  color: var(--system-orange);
}
.modern-tab[data-tab="Jeppesen"].active .tab-icon {
  color: var(--system-pink);
}

.tab-title {
  font-size: 22rpx;
  font-weight: 500;
  color: #8E8E93;
  transition: all 0.2s;
}

.modern-tab.active .tab-title {
  color: var(--text-primary);
  font-weight: 600;
}

.tab-count {
  position: absolute;
  top: 6rpx;
  right: 6rpx;
  background-color: var(--system-red);
  color: white;
  font-size: 18rpx;
  padding: 2rpx 10rpx;
  border-radius: 100rpx;
  font-weight: 600;
  min-width: 24rpx;
  text-align: center;
  z-index: 2;
  box-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.1);
}

/* ========================================
   🔍 搜索区域 (Enhanced Search Bar)
   ======================================== */
.search-section {
  padding: 16rpx 24rpx 24rpx;
  position: relative;
  z-index: 1;
}

.abbreviation-search {
  background: transparent;
  padding: 0;
}

.van-search {
  padding: 0 !important;
  background: transparent !important;
}

.van-search__content {
  background: linear-gradient(135deg, #FFFFFF 0%, #F8F9FA 100%) !important;
  border-radius: 24rpx !important;
  border: 2rpx solid rgba(0, 122, 255, 0.1);
  box-shadow: 0 4rpx 16rpx rgba(0, 122, 255, 0.06),
              0 2rpx 8rpx rgba(0, 0, 0, 0.03);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.van-search__content--focused {
  border-color: rgba(0, 122, 255, 0.3);
  box-shadow: 0 6rpx 20rpx rgba(0, 122, 255, 0.12),
              0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  transform: translateY(-2rpx);
}

/* ========================================
   📋 列表与卡片 (Enhanced Card Design)
   ======================================== */
.abbreviations-section {
  padding: 0 24rpx 30rpx;
  position: relative;
  z-index: 1;
}

.modern-list {
  display: flex;
  flex-direction: column;
  gap: 28rpx;
}

.modern-abbreviation-card {
  background: linear-gradient(135deg, #FFFFFF 0%, #FAFBFC 100%);
  border-radius: 32rpx;
  padding: 32rpx;
  box-shadow: var(--shadow-card);
  position: relative;
  overflow: hidden;
  transition: all 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  border: 2rpx solid rgba(255, 255, 255, 0.8);
}

.modern-abbreviation-card:active {
  transform: scale(0.97) translateY(2rpx);
  box-shadow: var(--shadow-pressed);
}

/* 卡片背景渐变装饰 */
.modern-abbreviation-card::after {
  content: '';
  position: absolute;
  top: -50%;
  right: -20%;
  width: 200rpx;
  height: 200rpx;
  border-radius: 50%;
  opacity: 0.06;
  pointer-events: none;
  transition: all 0.3s;
}

.modern-abbreviation-card.AIP::after {
  background: radial-gradient(circle, var(--system-blue-light) 0%, transparent 70%);
}
.modern-abbreviation-card.Airbus::after {
  background: radial-gradient(circle, var(--system-indigo-light) 0%, transparent 70%);
}
.modern-abbreviation-card.Boeing::after {
  background: radial-gradient(circle, var(--system-teal-light) 0%, transparent 70%);
}
.modern-abbreviation-card.COMAC::after {
  background: radial-gradient(circle, var(--system-orange-light) 0%, transparent 70%);
}
.modern-abbreviation-card.Jeppesen::after {
  background: radial-gradient(circle, var(--system-pink-light) 0%, transparent 70%);
}

/* 卡片装饰：左侧渐变彩色条 */
.modern-abbreviation-card::before {
  content: '';
  position: absolute;
  left: 0;
  top: 24rpx;
  bottom: 24rpx;
  width: 10rpx;
  background: var(--system-blue);
  border-radius: 0 8rpx 8rpx 0;
  box-shadow: 0 0 20rpx currentColor;
  opacity: 1;
  transition: all 0.3s;
}

.modern-abbreviation-card.AIP::before { 
  background: var(--gradient-aip);
  box-shadow: 4rpx 0 16rpx rgba(0, 122, 255, 0.25);
}
.modern-abbreviation-card.Airbus::before { 
  background: var(--gradient-airbus);
  box-shadow: 4rpx 0 16rpx rgba(88, 86, 214, 0.25);
}
.modern-abbreviation-card.Boeing::before { 
  background: var(--gradient-boeing);
  box-shadow: 4rpx 0 16rpx rgba(48, 213, 200, 0.25);
}
.modern-abbreviation-card.COMAC::before { 
  background: var(--gradient-comac);
  box-shadow: 4rpx 0 16rpx rgba(255, 149, 0, 0.25);
}
.modern-abbreviation-card.Jeppesen::before { 
  background: var(--gradient-jeppesen);
  box-shadow: 4rpx 0 16rpx rgba(255, 45, 85, 0.25);
}

/* 卡片头部 */
.card-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 24rpx;
  position: relative;
  z-index: 1;
}

.primary-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.abbreviation-title {
  font-family: -apple-system, "SF Pro Display", "Helvetica Neue", sans-serif;
  font-size: 52rpx;
  font-weight: 800;
  background: var(--gradient-aip);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  letter-spacing: -1rpx;
  line-height: 1.2;
  transition: all 0.3s;
}

/* 不同分类的标题颜色 */
.modern-abbreviation-card.AIP .abbreviation-title {
  background: var(--gradient-aip);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.modern-abbreviation-card.Airbus .abbreviation-title {
  background: var(--gradient-airbus);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.modern-abbreviation-card.Boeing .abbreviation-title {
  background: var(--gradient-boeing);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.modern-abbreviation-card.COMAC .abbreviation-title {
  background: var(--gradient-comac);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.modern-abbreviation-card.Jeppesen .abbreviation-title {
  background: var(--gradient-jeppesen);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* 应急/特殊标记 */
.priority-indicators {
  display: flex;
  gap: 12rpx;
  margin-top: 4rpx;
}

.priority-badge {
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  font-size: 20rpx;
  font-weight: 600;
  text-transform: uppercase;
}

.priority-badge.emergency {
  background-color: rgba(255, 59, 48, 0.1);
  color: var(--system-red);
}

.quick-actions { display: none; }

/* 内容区域 */
.card-content {
  margin-bottom: 28rpx;
  position: relative;
  z-index: 1;
}

.chinese-primary {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 12rpx;
  line-height: 1.5;
  letter-spacing: 0.5rpx;
}

.english-secondary {
  font-size: 26rpx;
  color: var(--text-secondary);
  font-weight: 400;
  line-height: 1.6;
  font-style: italic;
  opacity: 0.9;
}

/* 底部区域 */
.card-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 24rpx;
  border-top: 1rpx solid rgba(60, 60, 67, 0.06);
  position: relative;
  z-index: 1;
}

.category-info {
  display: flex;
  align-items: center;
}

.category-badge {
  display: inline-flex;
  align-items: center;
  gap: 10rpx;
  padding: 10rpx 24rpx;
  border-radius: 100rpx;
  font-size: 24rpx;
  font-weight: 700;
  background: #F2F2F7;
  color: var(--text-secondary);
  position: relative;
  overflow: hidden;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.category-badge.aip { 
  background: linear-gradient(135deg, rgba(0, 122, 255, 0.12) 0%, rgba(90, 200, 250, 0.08) 100%);
  color: var(--system-blue);
  border: 1rpx solid rgba(0, 122, 255, 0.15);
}
.category-badge.airbus { 
  background: linear-gradient(135deg, rgba(88, 86, 214, 0.12) 0%, rgba(125, 122, 255, 0.08) 100%);
  color: var(--system-indigo);
  border: 1rpx solid rgba(88, 86, 214, 0.15);
}
.category-badge.boeing { 
  background: linear-gradient(135deg, rgba(48, 213, 200, 0.12) 0%, rgba(100, 210, 255, 0.08) 100%);
  color: var(--system-teal);
  border: 1rpx solid rgba(48, 213, 200, 0.15);
}
.category-badge.comac { 
  background: linear-gradient(135deg, rgba(255, 149, 0, 0.12) 0%, rgba(255, 179, 64, 0.08) 100%);
  color: var(--system-orange);
  border: 1rpx solid rgba(255, 149, 0, 0.15);
}

.category-icon { 
  font-size: 26rpx;
  filter: drop-shadow(0 2rpx 4rpx rgba(0, 0, 0, 0.1));
}
.category-name { 
  font-size: 24rpx;
  letter-spacing: 0.5rpx;
}

.footer-actions {
  display: flex;
  gap: 16rpx;
}

.pronunciation-btn, .more-btn {
  width: 68rpx;
  height: 68rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #F8F9FA 0%, #F2F2F7 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.06);
  border: 1rpx solid rgba(0, 0, 0, 0.04);
}

.pronunciation-btn:active {
  background: linear-gradient(135deg, rgba(0, 122, 255, 0.15) 0%, rgba(90, 200, 250, 0.1) 100%);
  transform: scale(0.92);
  box-shadow: 0 1rpx 4rpx rgba(0, 0, 0, 0.08);
}

.more-btn:active {
  background: linear-gradient(135deg, #E5E5EA 0%, #D1D1D6 100%);
  transform: scale(0.92);
  box-shadow: 0 1rpx 4rpx rgba(0, 0, 0, 0.08);
}

.pronunciation-icon {
  font-size: 32rpx;
  color: var(--system-blue);
  filter: drop-shadow(0 2rpx 4rpx rgba(0, 122, 255, 0.2));
}

.more-icon {
  color: var(--text-secondary);
  font-weight: 900;
  font-size: 32rpx;
  margin-top: -8rpx;
}

/* ========================================
   🔄 加载状态与空状态
   ======================================== */
.load-more-section, .loading-section, .no-more-section {
  padding: 40rpx 0;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 16rpx;
  z-index: 1;
  position: relative;
}

.load-more-btn {
  display: inline-flex;
  align-items: center;
  gap: 16rpx;
  padding: 20rpx 48rpx;
  background: linear-gradient(135deg, #FFFFFF 0%, #F8F9FA 100%);
  border-radius: 100rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 122, 255, 0.12);
  border: 2rpx solid rgba(0, 122, 255, 0.1);
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.load-more-btn:active {
  transform: scale(0.95);
  box-shadow: 0 2rpx 8rpx rgba(0, 122, 255, 0.15);
}

.load-more-text {
  font-size: 28rpx;
  font-weight: 600;
  background: var(--gradient-aip);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.load-more-icon {
  font-size: 28rpx;
  filter: drop-shadow(0 2rpx 4rpx rgba(0, 122, 255, 0.2));
}

.load-more-tip, .loading-text, .no-more-text {
  font-size: 24rpx;
  color: var(--text-secondary);
  font-weight: 500;
}

.loading-spinner {
  width: 48rpx;
  height: 48rpx;
  border: 4rpx solid #E5E5EA;
  border-top: 4rpx solid var(--system-blue);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
  box-shadow: 0 2rpx 8rpx rgba(0, 122, 255, 0.1);
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.no-more-icon {
  font-size: 32rpx;
  filter: drop-shadow(0 2rpx 4rpx rgba(52, 199, 89, 0.2));
}

/* 无结果 */
.no-results {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 40rpx;
  text-align: center;
  z-index: 1;
  position: relative;
}

.no-results-icon {
  font-size: 96rpx;
  margin-bottom: 32rpx;
  opacity: 0.2;
  filter: grayscale(0.5);
}

.no-results-text {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 16rpx;
  letter-spacing: 0.5rpx;
}

.no-results-tip {
  font-size: 28rpx;
  color: var(--text-secondary);
  line-height: 1.5;
}

/* ========================================
   📱 弹窗样式 (Enhanced Modal)
   ======================================== */
.detail-popup {
  height: 100%;
  display: flex;
  flex-direction: column;
  background: linear-gradient(180deg, #E8EDF5 0%, #F2F4F8 100%);
}

.popup-header {
  background: linear-gradient(135deg, #FFFFFF 0%, #F8F9FA 100%);
  padding: 36rpx 32rpx 28rpx;
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.06);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.04);
}

.popup-title {
  font-size: 52rpx;
  font-weight: 800;
  background: var(--gradient-aip);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  margin-bottom: 8rpx;
  letter-spacing: -1rpx;
}

.popup-subtitle {
  font-size: 28rpx;
  color: var(--text-secondary);
  font-weight: 600;
}

.popup-close-btn {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #F8F9FA 0%, #F2F2F7 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-secondary);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.06);
  transition: all 0.3s;
}

.popup-close-btn:active {
  transform: scale(0.92);
  background: linear-gradient(135deg, #E5E5EA 0%, #D1D1D6 100%);
}

.popup-content {
  flex: 1;
  padding: 32rpx 24rpx;
}

.detail-section {
  background: linear-gradient(135deg, #FFFFFF 0%, #FAFBFC 100%);
  border-radius: 24rpx;
  padding: 28rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.04);
  border: 1rpx solid rgba(255, 255, 255, 0.8);
}

.detail-label {
  font-size: 22rpx;
  text-transform: uppercase;
  color: var(--system-blue);
  margin-bottom: 16rpx;
  letter-spacing: 2rpx;
  font-weight: 700;
  opacity: 0.8;
}

.detail-content {
  font-size: 32rpx;
  color: var(--text-primary);
  line-height: 1.6;
  font-weight: 500;
}

.abbreviation-content {
  font-size: 44rpx;
  font-weight: 800;
  background: var(--gradient-aip);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.english-content {
  font-style: italic;
  color: #48484A;
  line-height: 1.7;
}

.pronunciation-content {
  font-family: "SF Mono", "Menlo", "Courier New", monospace;
  background: linear-gradient(135deg, #F8F9FA 0%, #F2F2F7 100%);
  padding: 20rpx;
  border-radius: 16rpx;
  border: 1rpx solid rgba(0, 0, 0, 0.04);
  font-size: 28rpx;
  letter-spacing: 1rpx;
}

/* 广告容器样式 */
.ad-banner-container {
  margin: 20rpx 0;
  border-radius: 20rpx;
  overflow: hidden;
  box-shadow: var(--shadow-card);
  position: relative;
  z-index: 1;
}
