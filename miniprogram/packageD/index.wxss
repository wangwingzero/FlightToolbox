/* 权威定义页面样式 - 机场数据样式 */

/* 页面容器 */
.container {
  min-height: 100vh;
  background: linear-gradient(180deg, #f9fafb 0%, #f3f4f6 100%);
  padding: 0;
  position: relative;
}

.page-content {
  position: relative;
  z-index: 1;
}

/* 分类筛选容器 - 置顶sticky定位 */
.category-filter-container {
  background: rgba(255, 255, 255, 0.98);
  backdrop-filter: blur(40rpx) saturate(180%);
  position: -webkit-sticky; /* iOS Safari兼容 */
  position: sticky;
  top: 0;
  z-index: 100;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.06);
  box-shadow: 0 2rpx 16rpx rgba(0, 0, 0, 0.04);
}

.category-scroll {
  height: 140rpx;
  white-space: nowrap;
}

.category-tags {
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx;
  gap: 24rpx;
  min-width: max-content;
}

/* 分类标签 - 圆形徽章样式 */
.category-tag {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding: 20rpx 24rpx;
  border-radius: 24rpx;
  transition: all 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  min-width: 88rpx;
  background: transparent;
}

.category-tag.active {
  background: linear-gradient(135deg, rgba(255,255,255,0.95) 0%, rgba(248,250,252,0.95) 100%);
  transform: scale(1.08) translateY(-2rpx);
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,0.12), 0 2rpx 8rpx rgba(0,0,0,0.08);
}

.category-badge {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #e2e8f0 0%, #cbd5e1 100%);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  font-weight: 700;
  transition: all 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.08);
}

/* Category Tag Active States */
.category-tag.active .category-badge {
  transform: scale(1.15);
  box-shadow: 0 6rpx 20rpx rgba(0, 0, 0, 0.2), 0 2rpx 8rpx rgba(0, 0, 0, 0.12);
}

.category-title {
  font-size: 24rpx;
  color: #64748b;
  font-weight: 500;
  text-align: center;
  line-height: 1.2;
  transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.category-tag.active .category-title {
  font-weight: 600;
}

/* Specific Colors for Active Tags */
.category-tag.active.tag-all .category-badge { 
  background: linear-gradient(135deg, #1c1c1e 0%, #2c2c2e 100%); 
}
.category-tag.active.tag-all .category-title { color: #1c1c1e; }

.category-tag.active.tag-ccar .category-badge { 
  background: linear-gradient(135deg, #007AFF 0%, #0051D5 100%); 
}
.category-tag.active.tag-ccar .category-title { color: #007AFF; }

.category-tag.active.tag-ac .category-badge { 
  background: linear-gradient(135deg, #34C759 0%, #248A3D 100%); 
}
.category-tag.active.tag-ac .category-title { color: #34C759; }

.category-tag.active.tag-icao .category-badge { 
  background: linear-gradient(135deg, #5856D6 0%, #3634A3 100%); 
}
.category-tag.active.tag-icao .category-title { color: #5856D6; }

.category-tag.active.tag-law .category-badge { 
  background: linear-gradient(135deg, #FF3B30 0%, #D70015 100%); 
}
.category-tag.active.tag-law .category-title { color: #FF3B30; }

.category-tag.active.tag-standard .category-badge { 
  background: linear-gradient(135deg, #30B0C7 0%, #0891B2 100%); 
}
.category-tag.active.tag-standard .category-title { color: #30B0C7; }

.category-tag.active.tag-other .category-badge { 
  background: linear-gradient(135deg, #8E8E93 0%, #636366 100%); 
}
.category-tag.active.tag-other .category-title { color: #8E8E93; }

/* 搜索区域 */
.search-section {
  padding: 28rpx 32rpx 36rpx;
  background: transparent;
  position: relative;
  overflow: visible;
}

/* 高级搜索框容器 */
.premium-search-box {
  display: flex;
  align-items: center;
  background: #ffffff;
  backdrop-filter: blur(24px) saturate(180%);
  border-radius: 52rpx;
  padding: 24rpx 32rpx;
  box-shadow:
    0 4rpx 24rpx rgba(0, 0, 0, 0.08),
    0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  border: 2rpx solid rgba(0, 0, 0, 0.06);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.premium-search-box.focused {
  transform: translateY(-2rpx);
  box-shadow:
    0 8rpx 32rpx rgba(0, 122, 255, 0.15),
    0 4rpx 16rpx rgba(0, 122, 255, 0.1);
  border-color: rgba(0, 122, 255, 0.3);
}

/* 搜索图标包装器 */
.search-icon-wrapper {
  margin-right: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 搜索输入框 */
.premium-search-input {
  flex: 1;
  font-size: 32rpx;
  color: #1c1c1e;
  line-height: 1.5;
  background: transparent;
  border: none;
  outline: none;
  font-weight: 500;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

/* 搜索框占位符样式 */
.premium-search-placeholder {
  color: #86868b;
  font-size: 30rpx;
  font-weight: 400;
}

/* 清除按钮 */
.search-clear-btn {
  margin-left: 16rpx;
  padding: 10rpx;
  border-radius: 50%;
  background: #f5f5f7;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
}

.search-clear-btn:active {
  background: #e5e5e7;
  transform: scale(0.92);
}

/* 搜索统计 */
.search-stats {
  margin-top: 20rpx;
  text-align: center;
  animation: slideInUp 0.3s ease-out;
}

.stats-content {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 122, 255, 0.08);
  backdrop-filter: blur(16rpx);
  border-radius: 100rpx;
  padding: 14rpx 28rpx;
  border: 2rpx solid rgba(0, 122, 255, 0.15);
  box-shadow: 0 2rpx 8rpx rgba(0, 122, 255, 0.1);
}

.stats-icon {
  font-size: 24rpx;
  margin-right: 8rpx;
}

.stats-text {
  font-size: 28rpx;
  color: #007AFF;
  font-weight: 700;
  letter-spacing: 0.2rpx;
}

@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 定义列表容器 */
.definitions-container {
  background: transparent;
  border-radius: 0;
  margin: 0;
  padding: 0 24rpx 32rpx;
  min-height: calc(100vh - 400rpx);
}

.definitions-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

/* 广告容器样式（格子广告） */
.ad-container {
  margin: 24rpx 0;
  padding: 0;
  border-radius: 12rpx;
  overflow: hidden;
  background: transparent;
}

/* 高端优雅定义卡片 - Apple Style Redesign */
.definition-card {
  background: #ffffff;
  border-radius: 28rpx;
  padding: 0;
  margin-bottom: 24rpx;
  position: relative;
  box-shadow: 
    0 4rpx 24rpx rgba(0, 0, 0, 0.06),
    0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  border: 1rpx solid rgba(0, 0, 0, 0.04);
  overflow: hidden;
  transform: translateZ(0);
}

.definition-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 8rpx;
  z-index: 1;
}

.definition-card:active {
  transform: scale(0.985);
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

/* Category Color Coding - 使用顶部渐变色条 */
.card-ccar::before { 
  background: linear-gradient(90deg, #007AFF 0%, #0051D5 100%); 
}
.card-ac::before { 
  background: linear-gradient(90deg, #34C759 0%, #248A3D 100%); 
}
.card-icao::before { 
  background: linear-gradient(90deg, #5856D6 0%, #3634A3 100%); 
}
.card-law::before { 
  background: linear-gradient(90deg, #FF3B30 0%, #D70015 100%); 
}
.card-standard::before { 
  background: linear-gradient(90deg, #30B0C7 0%, #0891B2 100%); 
}
.card-other::before { 
  background: linear-gradient(90deg, #8E8E93 0%, #636366 100%); 
}

/* 卡片头部 - 垂直布局 */
.card-header {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 16rpx;
  gap: 20rpx;
  position: relative;
  z-index: 2;
  padding: 32rpx 32rpx 0;
}

.card-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #1c1c1e;
  line-height: 1.35;
  flex: 1;
  letter-spacing: -0.3rpx;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.source-tag {
  font-size: 22rpx;
  padding: 8rpx 18rpx;
  border-radius: 100rpx;
  white-space: nowrap;
  font-weight: 700;
  letter-spacing: 0.3rpx;
  flex-shrink: 0;
  margin-top: 4rpx;
  backdrop-filter: blur(8rpx);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.06);
}

/* Tag Color Styles */
.card-ccar .source-tag { background: rgba(0, 122, 255, 0.1); color: #007AFF; }
.card-ac .source-tag { background: rgba(52, 199, 89, 0.1); color: #34C759; }
.card-icao .source-tag { background: rgba(88, 86, 214, 0.1); color: #5856D6; }
.card-law .source-tag { background: rgba(255, 59, 48, 0.1); color: #FF3B30; }
.card-standard .source-tag { background: rgba(48, 176, 199, 0.1); color: #30B0C7; }
.card-other .source-tag { background: rgba(142, 142, 147, 0.1); color: #8E8E93; }

/* 英文名称 - 精致样式 */
.english-name {
  font-size: 28rpx;
  color: #86868b;
  font-style: italic;
  margin-bottom: 20rpx;
  line-height: 1.4;
  font-weight: 500;
  letter-spacing: 0.2rpx;
  position: relative;
  z-index: 2;
  padding: 0 32rpx;
  font-family: -apple-system, "SF Pro Text", "Helvetica Neue", sans-serif;
}

/* 定义内容区域 - 简洁风格 */
.definition-box {
  background: #f8f9fa;
  border-radius: 18rpx;
  padding: 28rpx;
  margin: 0 32rpx 24rpx;
  position: relative;
  border: 1rpx solid rgba(0, 0, 0, 0.03);
  box-shadow: inset 0 1rpx 2rpx rgba(0, 0, 0, 0.02);
}

.definition-box::before, .definition-box::after {
  display: none;
}

.definition-text {
  font-size: 30rpx;
  color: #3a3a3c;
  line-height: 1.7;
  word-break: break-all;
  font-weight: 400;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 3;
  overflow: hidden;
  text-overflow: ellipsis;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

/* 操作提示 - 精致交互 */
.action-hint {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  opacity: 0.5;
  transition: all 0.3s ease;
  position: relative;
  z-index: 2;
  padding: 0 32rpx 32rpx;
}

.definition-card:active .action-hint {
  opacity: 0.9;
}

.hint-icon {
  display: none;
}

.hint-text {
  font-size: 24rpx;
  color: #86868b;
  font-weight: 600;
  letter-spacing: 0.2rpx;
}

/* 加载更多 */
.load-more-container {
  display: flex;
  justify-content: center;
  padding: 40rpx 0;
}

.load-more-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  background: #ffffff;
  border-radius: 100rpx;
  padding: 20rpx 40rpx;
  box-shadow: 
    0 4rpx 20rpx rgba(0, 0, 0, 0.06),
    0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  border: 2rpx solid rgba(0, 0, 0, 0.04);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.load-more-btn:active {
  transform: scale(0.96);
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.04);
}

.load-more-text {
  margin-left: 12rpx;
  font-size: 28rpx;
  color: #636366;
  font-weight: 600;
}

/* 加载完成提示 */
.load-complete {
  display: flex;
  justify-content: center;
  padding: 40rpx 0;
}

.load-complete-text {
  font-size: 26rpx;
  color: #86868b;
  padding: 18rpx 32rpx;
  background: #f5f5f7;
  border-radius: 100rpx;
  font-weight: 500;
  letter-spacing: 0.2rpx;
}

/* 功能说明区域 */
.tips-section {
  background: white;
  border-radius: 24rpx;
  padding: 36rpx;
  margin: 40rpx 24rpx 32rpx;
  box-shadow: 
    0 4rpx 24rpx rgba(0, 0, 0, 0.06),
    0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  border: 1rpx solid rgba(0, 0, 0, 0.04);
}

.tips-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1c1c1e;
  margin-bottom: 24rpx;
  display: flex;
  align-items: center;
  letter-spacing: -0.3rpx;
}

.tips-content {
  line-height: 1.7;
}

.tip-item {
  font-size: 28rpx;
  color: #636366;
  margin-bottom: 16rpx;
  padding-left: 28rpx;
  position: relative;
  line-height: 1.6;
}

.tip-item:last-child {
  margin-bottom: 0;
}

.tip-item::before {
  content: '';
  position: absolute;
  left: 8rpx;
  top: 50%;
  transform: translateY(-50%);
  width: 6rpx;
  height: 6rpx;
  background: linear-gradient(135deg, #007AFF 0%, #0051D5 100%);
  border-radius: 50%;
}

/* 详情弹窗样式 - 参考IOSA设计 */
.detail-popup-container {
  height: 100%;
  display: flex;
  flex-direction: column;
  background: #f9fafb;
  position: relative;
  overflow: hidden;
}

/* 弹窗头部 */
.detail-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 40rpx 48rpx;
  background: rgba(255, 255, 255, 0.98);
  backdrop-filter: blur(40rpx) saturate(180%);
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.06);
  box-shadow: 0 2rpx 16rpx rgba(0, 0, 0, 0.04);
  position: sticky;
  top: 0;
  z-index: 50;
}

.header-left, .header-right {
  width: 88rpx;
  display: flex;
  justify-content: center;
}

.close-btn {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: #f5f5f7;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.close-btn:active {
  background: #e5e5e7;
  transform: scale(0.9);
}

.close-icon {
  font-size: 36rpx;
  color: #636366;
  font-weight: 600;
  transition: color 0.2s ease;
}

.detail-title {
  flex: 1;
  text-align: center;
  font-size: 36rpx;
  font-weight: 700;
  color: #1c1c1e;
  line-height: 1.3;
  padding: 0 24rpx;
  letter-spacing: -0.3rpx;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

/* 滚动内容区域 */
.detail-content {
  flex: 1;
  height: 0;
  padding: 0;
  background: transparent;
  position: relative;
  z-index: 1;
}

/* 详情内容 */
.definition-detail {
  padding: 24rpx 0 80rpx;
}

.detail-section {
  margin-bottom: 32rpx;
}

/* 现代化卡片容器 */
.description-content {
  padding: 32rpx;
  background: #f8f9fa;
  margin: 0 32rpx;
  border-radius: 20rpx;
  transition: all 0.3s ease;
  position: relative;
  border: 1rpx solid rgba(0, 0, 0, 0.03);
}

/* 定义内容区域 - 可点击复制 */
.definition-content-box {
  padding: 32rpx 32rpx 48rpx;
}

.definition-content-box::after {
  content: '💡 点击复制内容';
  position: absolute;
  bottom: 12rpx;
  right: 20rpx;
  font-size: 20rpx;
  color: #94a3b8;
  opacity: 0.6;
  transition: opacity 0.3s ease;
}

.definition-content-box:active {
  background: #f1f5f9;
  transform: scale(0.995);
}

.definition-content-box:active::after {
  opacity: 1;
  color: #667eea;
}

.description-text {
  font-size: 32rpx;
  color: #3a3a3c;
  line-height: 1.75;
  word-break: break-all;
  font-weight: 400;
  letter-spacing: 0.2rpx;
  text-align: justify;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.source-text {
  font-size: 28rpx;
  color: #636366;
  line-height: 1.6;
  font-weight: 400;
}

/* 术语信息卡片 */
.detail-section .van-cell-group {
  margin: 0 32rpx;
  border-radius: 20rpx;
  overflow: hidden;
  background: #ffffff;
  border: 1rpx solid rgba(0, 0, 0, 0.04) !important;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.detail-section .van-cell-group::before,
.detail-section .van-cell-group::after {
  display: none !important;
}

.detail-section .van-cell-group__title {
  padding: 24rpx 32rpx 16rpx;
  font-size: 30rpx;
  font-weight: 700;
  color: #007AFF;
  letter-spacing: -0.2rpx;
}

.detail-section .van-cell {
  background: transparent;
  padding: 20rpx 32rpx;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.04);
  transition: all 0.3s ease;
  border-left: none !important;
  border-right: none !important;
}

.detail-section .van-cell::before,
.detail-section .van-cell::after {
  display: none !important;
}

.detail-section .van-cell:last-child {
  border-bottom: none;
}

.detail-section .van-cell__title {
  font-size: 30rpx;
  font-weight: 600;
  color: #636366;
  letter-spacing: 0.2rpx;
}

.detail-section .van-cell__value {
  font-size: 30rpx;
  color: #1c1c1e;
  font-weight: 500;
  text-align: right;
  word-wrap: break-word;
  line-height: 1.5;
}

/* 底部操作栏 */
.detail-footer {
  padding: 36rpx 48rpx 42rpx;
  background: rgba(255, 255, 255, 0.98);
  backdrop-filter: blur(40rpx) saturate(180%);
  border-top: 1rpx solid rgba(0, 0, 0, 0.06);
  box-shadow: 0 -2rpx 16rpx rgba(0, 0, 0, 0.04);
  position: relative;
}

.footer-buttons {
  display: flex;
  gap: 32rpx;
  align-items: center;
}

.back-button, .close-button {
  flex: 1;
  height: 96rpx;
  border-radius: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  font-size: 32rpx;
  font-weight: 700;
  letter-spacing: -0.2rpx;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.back-button {
  background: #f5f5f7;
  border: 2rpx solid rgba(0, 0, 0, 0.06);
  color: #1c1c1e;
}

.back-button:active {
  background: #e5e5e7;
  transform: scale(0.98);
}

.close-button {
  background: #007AFF;
  border: none;
  color: #ffffff;
  box-shadow: 
    0 4rpx 20rpx rgba(0, 122, 255, 0.3),
    0 2rpx 8rpx rgba(0, 122, 255, 0.2);
}

.close-button:active {
  background: #0051D5;
  transform: scale(0.98);
  box-shadow: 
    0 2rpx 12rpx rgba(0, 122, 255, 0.25),
    0 1rpx 4rpx rgba(0, 122, 255, 0.15);
}

/* 单按钮样式 */
.close-button-single {
  height: 96rpx;
  border-radius: 28rpx;
  background: #007AFF;
  border: none;
  color: #ffffff;
  box-shadow: 
    0 4rpx 20rpx rgba(0, 122, 255, 0.3),
    0 2rpx 8rpx rgba(0, 122, 255, 0.2);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  font-size: 32rpx;
  font-weight: 700;
  letter-spacing: -0.2rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.close-button-single:active {
  background: #0051D5;
  transform: scale(0.98);
  box-shadow: 
    0 2rpx 12rpx rgba(0, 122, 255, 0.25),
    0 1rpx 4rpx rgba(0, 122, 255, 0.15);
}

.button-icon {
  font-size: 32rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 按钮文字 */
.back-button text,
.close-button text,
.close-button-single text {
  line-height: 1;
  display: flex;
  align-items: center;
}

/* 精美的术语高亮标签 */
.term-highlight {
  display: inline-block;
  background: rgba(0, 122, 255, 0.08);
  color: #007AFF;
  padding: 4rpx 12rpx;
  border-radius: 10rpx;
  border: 1rpx solid rgba(0, 122, 255, 0.15);
  font-size: 28rpx;
  font-weight: 600;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  letter-spacing: 0.2rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 122, 255, 0.08);
  word-break: break-all;
  overflow-wrap: break-word;
  white-space: normal;
  line-height: 1.4;
}

.clickable-term:active {
  background: #007AFF;
  color: #ffffff;
  transform: scale(0.96);
  border-color: #007AFF;
  box-shadow: 0 4rpx 16rpx rgba(0, 122, 255, 0.3);
}

/* 返回按钮样式 */
.back-btn {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: #f5f5f7;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.back-btn:active {
  background: #e5e5e7;
  transform: scale(0.9);
}

.back-icon {
  font-size: 36rpx;
  color: #636366;
  font-weight: 600;
  transition: color 0.2s ease;
}

/* 弹窗容器动画 */
@keyframes popupFadeIn {
  0% {
    opacity: 0;
    transform: translateY(80rpx) scale(0.95);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.detail-popup-container {
  animation: popupFadeIn 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

/* 优化长文本显示效果 */
.description-text {
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
}

/* 优化滚动条样式 */
.detail-content::-webkit-scrollbar {
  display: none;
}

/* 响应式优化 */
@media screen and (max-width: 320px) {
  .detail-title {
    font-size: 30rpx;
  }

  .description-text {
    font-size: 28rpx;
  }
}

@media screen and (min-width: 768px) {
  .detail-header {
    padding: 40rpx 56rpx;
  }

  .detail-footer {
    padding: 40rpx 56rpx 48rpx;
  }

  .description-content {
    margin: 0 48rpx;
    padding: 48rpx 44rpx;
  }
}

/* 响应式优化 - 小屏幕设备 */
@media (max-width: 640rpx) {
  .card-header {
    padding: 28rpx 28rpx 0;
    gap: 16rpx;
  }
  
  .card-title {
    font-size: 32rpx;
  }
  
  .english-name {
    font-size: 26rpx;
    padding: 0 28rpx;
  }
  
  .definition-box {
    margin: 0 28rpx 20rpx;
    padding: 24rpx;
  }
  
  .definition-text {
    font-size: 28rpx;
    line-height: 1.65;
  }
  
  .action-hint {
    padding: 0 28rpx 28rpx;
  }
  
  .source-tag {
    font-size: 20rpx;
    padding: 6rpx 14rpx;
  }
  
  .category-badge {
    width: 52rpx;
    height: 52rpx;
    font-size: 24rpx;
  }
  
  .category-tag {
    padding: 16rpx 20rpx;
    min-width: 80rpx;
  }
}

/* 极小屏幕适配 */
@media (max-width: 480rpx) {
  .card-header {
    padding: 24rpx 24rpx 0;
  }
  
  .card-title {
    font-size: 30rpx;
  }
  
  .english-name {
    font-size: 24rpx;
    padding: 0 24rpx;
  }

  .definition-box {
    margin: 0 24rpx 16rpx;
    padding: 20rpx;
  }
  
  .definition-text {
    font-size: 26rpx;
  }
  
  .action-hint {
    padding: 0 24rpx 24rpx;
  }

  .source-tag {
    font-size: 18rpx;
    padding: 6rpx 12rpx;
  }
  
  .hint-text {
    font-size: 22rpx;
  }
  
  .category-badge {
    width: 48rpx;
    height: 48rpx;
    font-size: 22rpx;
  }
  
  .category-tag {
    padding: 14rpx 18rpx;
    min-width: 72rpx;
  }
  
  .category-title {
    font-size: 22rpx;
  }
}

/* 搜索结果高亮效果 */
.definition-card.search-result {
  border: 2rpx solid #667eea;
  box-shadow: 0 4rpx 20rpx rgba(102, 126, 234, 0.15);
}

/* 空状态样式调整 */
.van-empty {
  padding: 60rpx 0;
}

/* 动画效果 */
@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.definition-card {
  animation: slideInUp 0.3s ease-out;
}

/* 搜索中状态 */
.definitions-list.searching .definition-card {
  opacity: 0.6;
  pointer-events: none;
}

/* 定义内容样式增强 */
.definition-text {
  position: relative;
}


/* 横幅广告容器样式 */
.ad-banner-container {
  position: relative;
  z-index: 10;
  padding: 20rpx 24rpx 40rpx;
  display: flex;
  justify-content: center;
  align-items: center;
}

.ad-banner-container ad {
  width: 100%;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
}
