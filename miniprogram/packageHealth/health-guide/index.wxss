/* 健康指南页面样式 - 基于体检标准页面设计 */

/* 分类标签菜单样式 */
.category-tabs-container {
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(32rpx) saturate(180%);
  border-bottom: 2rpx solid rgba(34, 197, 94, 0.15);
  box-shadow: 0 2rpx 16rpx rgba(34, 197, 94, 0.08);
}

.category-tabs-scroll {
  white-space: nowrap;
  overflow-x: auto;
  overflow-y: hidden;
  scroll-behavior: smooth;
  -webkit-overflow-scrolling: touch;
}

.category-tabs-wrapper {
  display: flex;
  padding: 0 24rpx;
  min-width: 100%;
}

.category-tab-item {
  position: relative;
  flex-shrink: 0;
  margin-right: 8rpx;
  padding: 20rpx 24rpx 16rpx;
  border-radius: 16rpx 16rpx 0 0;
  transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  cursor: pointer;
  min-width: 100rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  background: transparent;
}

.category-tab-item:last-child {
  margin-right: 0;
}

.category-tab-item.active {
  background: linear-gradient(135deg, 
    rgba(34, 197, 94, 0.08) 0%, 
    rgba(16, 185, 129, 0.08) 50%, 
    rgba(5, 150, 105, 0.08) 100%);
  transform: translateY(-2rpx);
}

.category-tab-item .category-badge {
  background: linear-gradient(135deg, #f59e0b 0%, #f97316 100%);
  color: white;
  font-size: 18rpx;
  font-weight: 600;
  padding: 4rpx 8rpx;
  border-radius: 20rpx;
  min-width: 32rpx;
  height: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 2rpx 8rpx rgba(245, 158, 11, 0.3);
  transform: scale(0.9);
  transition: all 0.3s ease;
  margin-bottom: 8rpx;
}

.category-tab-item.active .category-badge {
  background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
  transform: scale(1);
  box-shadow: 0 4rpx 12rpx rgba(34, 197, 94, 0.4);
}

.category-tab-item .category-title {
  font-size: 24rpx;
  font-weight: 500;
  color: #64748b;
  transition: all 0.3s ease;
  line-height: 1.2;
  text-align: center;
}

.category-tab-item.active .category-title {
  color: #22c55e;
  font-weight: 600;
}

/* 加载更多按钮样式 */
.load-more-container {
  padding: 40rpx 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
}

.load-more-btn {
  background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
  color: white;
  border: none;
  border-radius: 32rpx;
  padding: 24rpx 48rpx;
  font-size: 28rpx;
  font-weight: 600;
  box-shadow: 
    0 8rpx 24rpx rgba(34, 197, 94, 0.3),
    0 12rpx 48rpx rgba(34, 197, 94, 0.15);
  transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  display: flex;
  align-items: center;
  gap: 12rpx;
  min-width: 200rpx;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.load-more-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.6s ease;
}

.load-more-btn:hover::before {
  left: 100%;
}

.load-more-btn:active:not(.loading) {
  transform: scale(0.95);
  box-shadow: 
    0 4rpx 16rpx rgba(34, 197, 94, 0.4),
    0 8rpx 32rpx rgba(34, 197, 94, 0.2);
}

.load-more-btn.loading {
  background: linear-gradient(135deg, #9ca3af 0%, #6b7280 100%);
  box-shadow: 
    0 4rpx 16rpx rgba(156, 163, 175, 0.2),
    0 8rpx 32rpx rgba(156, 163, 175, 0.1);
  cursor: not-allowed;
}

.loading-icon {
  display: flex;
  align-items: center;
}

.load-more-text {
  font-size: 28rpx;
  font-weight: 600;
  color: inherit;
}

.remaining-count {
  font-size: 24rpx;
  color: #6b7280;
  background: rgba(34, 197, 94, 0.1);
  padding: 12rpx 24rpx;
  border-radius: 20rpx;
  border: 2rpx solid rgba(34, 197, 94, 0.2);
  backdrop-filter: blur(8rpx);
}

/* 页面容器优化 */
.container {
  background: 
    linear-gradient(135deg, #f0fdf4 0%, #ecfdf5 20%, #d1fae5 40%, #f0fdf4 60%, #f0f9ff 80%, #f0fdf4 100%),
    radial-gradient(circle at 30% 40%, rgba(34, 197, 94, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 70% 60%, rgba(59, 130, 246, 0.03) 0%, transparent 50%);
  min-height: 100vh;
  animation: containerFadeIn 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

@keyframes containerFadeIn {
  0% {
    opacity: 0;
    transform: translateY(20rpx);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 卡片包装器动画 */
.context7-card-wrapper {
  animation: cardsSlideIn 0.6s ease-out;
}

@keyframes cardsSlideIn {
  0% {
    opacity: 0;
    transform: translateY(40rpx);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 健康指南卡片样式 */
.health-guide-card {
  background: rgba(255, 255, 255, 0.98);
  backdrop-filter: blur(40rpx) saturate(200%);
  border-radius: 28rpx;
  padding: 36rpx;
  box-shadow: 
    0 12rpx 48rpx rgba(34, 197, 94, 0.15),
    0 24rpx 96rpx rgba(34, 197, 94, 0.1),
    inset 0 2rpx 0 rgba(255, 255, 255, 0.95),
    inset 0 -2rpx 0 rgba(34, 197, 94, 0.05);
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  border: 3rpx solid rgba(34, 197, 94, 0.12);
  position: relative;
  overflow: hidden;
  margin-bottom: 32rpx;
}

.health-guide-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 6rpx;
  background: linear-gradient(135deg, #22c55e 0%, #3b82f6 50%, #8b5cf6 100%);
  opacity: 0.8;
}

.health-guide-card:hover {
  transform: translateY(-8rpx) scale(1.02);
  box-shadow: 
    0 20rpx 64rpx rgba(34, 197, 94, 0.2),
    0 32rpx 128rpx rgba(34, 197, 94, 0.15),
    inset 0 2rpx 0 rgba(255, 255, 255, 0.95);
  border-color: rgba(34, 197, 94, 0.2);
}

.health-guide-card:active {
  transform: translateY(-4rpx) scale(0.99);
  box-shadow: 
    0 8rpx 32rpx rgba(34, 197, 94, 0.18),
    0 16rpx 64rpx rgba(34, 197, 94, 0.12);
}

/* 类型徽章样式 */
.type-badge {
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 6rpx;
  position: relative;
  overflow: hidden;
  backdrop-filter: blur(16rpx);
}

.type-badge::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  transition: left 0.6s ease;
}

.type-badge:hover::before {
  left: 100%;
}

.type-badge.comprehensive {
  background: linear-gradient(135deg, #22c55e 0%, #16a34a 50%, #059669 100%);
  color: white;
  box-shadow: 
    0 4rpx 16rpx rgba(34, 197, 94, 0.4),
    0 8rpx 32rpx rgba(34, 197, 94, 0.2),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.3);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.type-badge.quick {
  background: linear-gradient(135deg, #f59e0b 0%, #d97706 50%, #b45309 100%);
  color: white;
  box-shadow: 
    0 4rpx 16rpx rgba(245, 158, 11, 0.4),
    0 8rpx 32rpx rgba(245, 158, 11, 0.2),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.3);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

/* 高亮标识 */
.item-highlight {
  display: flex;
  align-items: center;
  gap: 10rpx;
  margin-top: 20rpx;
  padding: 16rpx 20rpx;
  background: 
    linear-gradient(135deg, rgba(34, 197, 94, 0.15) 0%, rgba(34, 197, 94, 0.08) 100%),
    radial-gradient(circle at 20% 50%, rgba(34, 197, 94, 0.1) 0%, transparent 50%);
  border-radius: 16rpx;
  border: 2rpx solid rgba(34, 197, 94, 0.25);
  position: relative;
  overflow: hidden;
  backdrop-filter: blur(8rpx);
}

.item-highlight::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2rpx;
  background: linear-gradient(90deg, #22c55e, #3b82f6, #22c55e);
  animation: shimmer 2s ease-in-out infinite;
}

@keyframes shimmer {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}

.item-highlight text {
  font-size: 26rpx;
  color: #059669;
  font-weight: 600;
  text-shadow: 0 1rpx 2rpx rgba(5, 150, 105, 0.2);
}

/* 分类徽章优化 */
.category-badge {
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 6rpx;
  backdrop-filter: blur(16rpx);
  position: relative;
  overflow: hidden;
  transition: all 0.3s ease;
}

.category-badge::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.5s ease;
}

.category-badge:hover::before {
  left: 100%;
}

.category-badge[data-category="生理学"] {
  background: linear-gradient(135deg, #ef4444 0%, #dc2626 50%, #b91c1c 100%);
  color: white;
  box-shadow: 0 4rpx 16rpx rgba(239, 68, 68, 0.3);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.category-badge[data-category="身体健康"] {
  background: linear-gradient(135deg, #22c55e 0%, #16a34a 50%, #059669 100%);
  color: white;
  box-shadow: 0 4rpx 16rpx rgba(34, 197, 94, 0.3);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.category-badge[data-category="环境因素"] {
  background: linear-gradient(135deg, #06b6d4 0%, #0891b2 50%, #0e7490 100%);
  color: white;
  box-shadow: 0 4rpx 16rpx rgba(6, 182, 212, 0.3);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.category-badge[data-category="药物酒精"] {
  background: linear-gradient(135deg, #f59e0b 0%, #d97706 50%, #b45309 100%);
  color: white;
  box-shadow: 0 4rpx 16rpx rgba(245, 158, 11, 0.3);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

.category-badge[data-category="视听健康"] {
  background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 50%, #6d28d9 100%);
  color: white;
  box-shadow: 0 4rpx 16rpx rgba(139, 92, 246, 0.3);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

/* 搜索区域优化 */
.search-section {
  padding: 32rpx 24rpx;
  position: relative;
}

.search-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 24rpx;
  right: 24rpx;
  height: 4rpx;
  background: linear-gradient(135deg, #22c55e 0%, #3b82f6 50%, #8b5cf6 100%);
  border-radius: 0 0 12rpx 12rpx;
  animation: searchGlow 3s ease-in-out infinite;
}

@keyframes searchGlow {
  0%, 100% { opacity: 0.6; transform: scaleX(1); }
  50% { opacity: 1; transform: scaleX(1.02); }
}


/* 无结果状态优化 */
.no-results {
  text-align: center;
  padding: 80rpx 40rpx;
  animation: fadeInUp 0.6s ease-out;
}

@keyframes fadeInUp {
  0% {
    opacity: 0;
    transform: translateY(40rpx);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

.no-results-animation {
  position: relative;
  display: inline-block;
  margin-bottom: 32rpx;
}

.no-results-icon {
  font-size: 80rpx;
  animation: bounce 2s ease-in-out infinite;
}

@keyframes bounce {
  0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
  40% { transform: translateY(-20rpx); }
  60% { transform: translateY(-10rpx); }
}

.no-results-sparkles {
  position: absolute;
  top: -10rpx;
  right: -10rpx;
  font-size: 32rpx;
  animation: sparkle 1.5s ease-in-out infinite;
}

@keyframes sparkle {
  0%, 100% { opacity: 0; transform: scale(0.8) rotate(0deg); }
  50% { opacity: 1; transform: scale(1.2) rotate(180deg); }
}

.no-results-text {
  font-size: 32rpx;
  color: #6b7280;
  font-weight: 600;
  margin-bottom: 16rpx;
}

.no-results-tip {
  font-size: 28rpx;
  color: #9ca3af;
  margin-bottom: 24rpx;
}

.no-results-suggestions {
  padding: 20rpx 32rpx;
  background: rgba(34, 197, 94, 0.1);
  border-radius: 20rpx;
  border: 2rpx solid rgba(34, 197, 94, 0.2);
  display: inline-block;
}

.no-results-suggestions text {
  font-size: 26rpx;
  color: #059669;
  font-weight: 500;
}

/* 通用健康模块内容样式 */
.general-health-content {
  padding: 20rpx 0;
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #2c3e50;
  margin: 30rpx 0 20rpx 0;
  padding-bottom: 10rpx;
  border-bottom: 2rpx solid #3498db;
}


.concept-item, .symptom-item, .prevention-item, .treatment-item {
  padding: 12rpx 0;
  line-height: 1.6;
  font-size: 28rpx;
  color: #555;
}


.recommendation-item {
  padding: 12rpx 0;
  line-height: 1.6;
  font-size: 28rpx;
  margin-left: 20rpx;
}

.do-item {
  color: #27ae60;
}

.dont-item {
  color: #e74c3c;
}


.scenario-item, .regulation-item {
  margin-bottom: 24rpx;
  padding: 20rpx;
  background-color: #f8f9fa;
  border-radius: 12rpx;
  border-left: 4rpx solid #3498db;
}


.scenario-title, .regulation-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #2c3e50;
  margin-bottom: 12rpx;
}


.scenario-content, .regulation-content {
  font-size: 28rpx;
  line-height: 1.6;
  color: #555;
}


.illusion-item {
  margin-bottom: 24rpx;
  padding: 20rpx;
  background-color: #fff3cd;
  border-radius: 12rpx;
  border-left: 4rpx solid #ffc107;
}


.illusion-name {
  font-size: 30rpx;
  font-weight: 600;
  color: #856404;
  margin-bottom: 12rpx;
}


.illusion-description {
  font-size: 28rpx;
  line-height: 1.6;
  color: #856404;
}


/* 表格样式 */
.table-section {
  margin: 30rpx 0;
}

.table-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #2c3e50;
  margin-bottom: 20rpx;
  text-align: center;
}


.table-wrapper {
  border: 2rpx solid #ddd;
  border-radius: 12rpx;
  overflow: hidden;
  background-color: white;
}


.table-header {
  display: flex;
  background-color: #f8f9fa;
  font-weight: 600;
}


.table-row {
  display: flex;
  border-top: 1rpx solid #eee;
}


.table-cell {
  flex: 1;
  padding: 20rpx 16rpx;
  font-size: 26rpx;
  text-align: center;
  border-right: 1rpx solid #eee;
  word-wrap: break-word;
}


.table-cell:last-child {
  border-right: none;
}

.header-cell {
  color: #2c3e50;
  font-weight: 600;
}


/* 要点列表样式 */
.point-item {
  padding: 16rpx 0;
  line-height: 1.8;
  font-size: 28rpx;
  color: #555;
  border-bottom: 1rpx solid #f0f0f0;
}


.point-item:last-child {
  border-bottom: none;
}

.container {
  min-height: 100vh;
  padding-bottom: 120rpx;
}

/* 页面内容默认样式 */
.page-content {
  background-color: #f5f5f5;
  color: #333;
}


/* 搜索区域 */
.search-section {
  padding: 20rpx;
  background-color: white;
  margin-bottom: 20rpx;
}


/* 健康指南列表区域 */
.guides-section {
  padding: 0 20rpx;
}

.context7-content-container {
  margin-bottom: 40rpx;
}

.context7-card-wrapper {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

/* 健康指南卡片 */
.health-item-card {
  background-color: white;
  border-radius: 16rpx;
  padding: 32rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
  border: 1rpx solid #f0f0f0;
  /* 使用flexbox布局，让底部标签固定在底部 */
  display: flex;
  flex-direction: column;
  min-height: 200rpx;
}


.health-item-card:active {
  transform: scale(0.98);
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.12);
}

/* 卡片头部 */
.item-card-header {
  margin-bottom: 24rpx;
}

.item-primary {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-bottom: 12rpx;
}

.item-name-zh {
  font-size: 36rpx;
  font-weight: 600;
  color: #1a1a1a;
  line-height: 1.4;
}


.item-badges {
  display: flex;
  gap: 16rpx;
  flex-wrap: wrap;
}

.category-badge {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 8rpx 16rpx;
  border-radius: 24rpx;
  font-size: 24rpx;
  font-weight: 500;
}

.category-badge[data-category="飞行体能"] {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
}

.category-badge[data-category="太阳眼镜"] {
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  color: white;
}

.category-badge[data-category="听力保护"] {
  background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
  color: white;
}

.publication-badge {
  padding: 8rpx 16rpx;
  background: rgba(102, 126, 234, 0.1);
  border-radius: 24rpx;
  font-size: 22rpx;
  color: #667eea;
  font-weight: 500;
}


.item-secondary {
  margin-top: 8rpx;
}

.item-name-en {
  font-size: 28rpx;
  color: #666;
  font-style: italic;
}


/* 卡片内容 */
.item-card-content {
  margin-bottom: 24rpx;
}

.item-summary {
  font-size: 28rpx;
  color: #4a4a4a;
  line-height: 1.6;
  margin-bottom: 16rpx;
}


.item-source {
  display: flex;
  align-items: flex-start;
  gap: 12rpx;
  flex-wrap: wrap;
}

.source-label {
  font-size: 24rpx;
  color: #888;
  flex-shrink: 0;
}


.source-text {
  font-size: 24rpx;
  color: #666;
  flex: 1;
}


/* 卡片底部 */
.item-card-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: auto;
  padding-top: 16rpx;
}

.item-meta {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 24rpx;
  color: #999;
}


.item-arrow {
  color: #ccc;
}


/* 无结果状态 */
.no-results {
  text-align: center;
  padding: 120rpx 40rpx;
  color: #999;
}


.no-results-icon {
  font-size: 120rpx;
  margin-bottom: 32rpx;
}

.no-results-text {
  font-size: 32rpx;
  margin-bottom: 16rpx;
  color: #666;
}


.no-results-tip {
  font-size: 28rpx;
  color: #999;
}


/* 统计信息 */
.stats-section {
  padding: 32rpx 24rpx 64rpx;
  background: transparent;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24rpx;
}

.stats-card {
  display: flex;
  justify-content: center;
  gap: 24rpx;
  flex-wrap: wrap;
  width: 100%;
  max-width: 600rpx;
}

.stats-item {
  display: flex;
  align-items: center;
  gap: 18rpx;
  padding: 28rpx 36rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(40rpx) saturate(200%);
  border-radius: 28rpx;
  box-shadow: 
    0 8rpx 40rpx rgba(34, 197, 94, 0.15),
    0 16rpx 80rpx rgba(34, 197, 94, 0.1),
    inset 0 2rpx 0 rgba(255, 255, 255, 0.95),
    inset 0 -1rpx 0 rgba(34, 197, 94, 0.08);
  border: 3rpx solid rgba(34, 197, 94, 0.12);
  flex: 1;
  min-width: 180rpx;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  overflow: hidden;
}

.stats-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(135deg, #22c55e 0%, #3b82f6 50%, #8b5cf6 100%);
  opacity: 0.7;
  border-radius: 28rpx 28rpx 0 0;
}

.stats-item:hover {
  transform: translateY(-6rpx) scale(1.02);
  box-shadow: 
    0 12rpx 56rpx rgba(34, 197, 94, 0.2),
    0 24rpx 112rpx rgba(34, 197, 94, 0.15),
    inset 0 2rpx 0 rgba(255, 255, 255, 0.95);
  border-color: rgba(34, 197, 94, 0.2);
}

.stats-item:hover::before {
  opacity: 1;
}

.stats-icon {
  font-size: 36rpx;
  line-height: 1;
  filter: drop-shadow(0 3rpx 12rpx rgba(34, 197, 94, 0.4));
  transition: all 0.3s ease;
  animation: iconFloat 3s ease-in-out infinite;
}

@keyframes iconFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-4rpx); }
}

.stats-item:hover .stats-icon {
  transform: scale(1.1) translateY(-2rpx);
  filter: drop-shadow(0 4rpx 16rpx rgba(34, 197, 94, 0.5));
}

.stats-content {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.stats-number {
  font-size: 36rpx;
  font-weight: 700;
  color: #059669;
  line-height: 1;
  background: linear-gradient(135deg, #059669 0%, #22c55e 50%, #34d399 100%);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.stats-label {
  font-size: 24rpx;
  color: #6b7280;
  font-weight: 500;
  line-height: 1;
}

.stats-description {
  padding: 32rpx 40rpx;
  background: 
    linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(255, 255, 255, 0.8) 100%),
    radial-gradient(circle at 50% 50%, rgba(34, 197, 94, 0.05) 0%, transparent 70%);
  backdrop-filter: blur(24rpx) saturate(200%);
  border-radius: 24rpx;
  border: 3rpx solid rgba(34, 197, 94, 0.15);
  max-width: 600rpx;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.stats-description::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3rpx;
  background: linear-gradient(90deg, #22c55e, #3b82f6, #8b5cf6, #22c55e);
  background-size: 200% 100%;
  animation: gradientMove 3s ease-in-out infinite;
}

@keyframes gradientMove {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

.stats-description text {
  font-size: 26rpx;
  color: #6b7280;
  line-height: 1.5;
  font-weight: 400;
}






/* 详情弹窗 */
.detail-popup {
  height: 100%;
  display: flex;
  flex-direction: column;
  background: linear-gradient(135deg, #f0fdf4 0%, #ecfdf5 100%);
  border-radius: 32rpx 32rpx 0 0;
  overflow: hidden;
}

/* 弹窗头部 - 强制样式 */
.detail-popup .popup-header {
  background: linear-gradient(135deg, #22c55e 0%, #16a34a 30%, #059669 60%, #047857 100%) !important;
  color: white !important;
  padding: 24rpx 20rpx !important;
  display: flex !important;
  justify-content: space-between !important;
  align-items: flex-start !important;
  position: relative !important;
  overflow: hidden !important;
  border-radius: 32rpx 32rpx 0 0 !important;
  z-index: 1 !important;
  min-height: 120rpx !important;
}

.popup-header-decoration {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(90deg, #22c55e, #3b82f6, #8b5cf6, #22c55e);
  background-size: 200% 100%;
  animation: gradientMove 3s ease-in-out infinite;
  z-index: 0;
}

.detail-popup .popup-main-content {
  flex: 1 !important;
  padding-right: 16rpx !important;
  min-width: 0 !important;
  max-width: calc(100% - 60rpx) !important;
}

.detail-popup .popup-title {
  font-size: 32rpx !important;
  font-weight: 700 !important;
  margin-bottom: 6rpx !important;
  text-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.2) !important;
  line-height: 1.2 !important;
  word-break: break-word !important;
  color: white !important;
}

.detail-popup .popup-subtitle {
  font-size: 24rpx !important;
  opacity: 0.9 !important;
  font-style: italic !important;
  margin-bottom: 12rpx !important;
  text-shadow: 0 1rpx 4rpx rgba(0, 0, 0, 0.1) !important;
  color: white !important;
}

.detail-popup .popup-badges {
  display: flex !important;
  gap: 8rpx !important;
  flex-wrap: wrap !important;
  z-index: 3 !important;
  position: relative !important;
  margin-top: 8rpx !important;
}

.detail-popup .popup-category-badge,
.detail-popup .popup-type-badge {
  padding: 4rpx 8rpx !important;
  border-radius: 12rpx !important;
  font-size: 18rpx !important;
  font-weight: 500 !important;
  display: inline-flex !important;
  align-items: center !important;
  gap: 3rpx !important;
  background: rgba(255, 255, 255, 0.25) !important;
  border: 1rpx solid rgba(255, 255, 255, 0.4) !important;
  white-space: nowrap !important;
  color: white !important;
}

.detail-popup .popup-close-btn {
  padding: 10rpx !important;
  border-radius: 50% !important;
  background: rgba(255, 255, 255, 0.25) !important;
  border: 1rpx solid rgba(255, 255, 255, 0.4) !important;
  transition: all 0.3s ease !important;
  z-index: 10 !important;
  position: relative !important;
  flex-shrink: 0 !important;
  width: 44rpx !important;
  height: 44rpx !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
}

.popup-close-btn:active {
  transform: scale(0.95);
  background: rgba(255, 255, 255, 0.3);
}

.popup-content {
  flex: 1;
  padding: 32rpx 24rpx;
  background: 
    linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%),
    radial-gradient(circle at 50% 50%, rgba(34, 197, 94, 0.03) 0%, transparent 70%);
  /* 确保滚动视图可以正常工作 */
  box-sizing: border-box;
}



/* 确保弹窗内容可滚动 */
.detail-popup {
  position: relative;
  overflow: hidden;
}

.popup-content {
  position: relative;
  z-index: 1;
}

/* 信息卡片 */
.info-cards {
  display: flex;
  gap: 16rpx;
  margin-bottom: 32rpx;
  flex-wrap: wrap;
}

.info-card {
  flex: 1;
  min-width: 200rpx;
  padding: 24rpx 20rpx;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(16rpx) saturate(180%);
  border-radius: 20rpx;
  border: 2rpx solid rgba(34, 197, 94, 0.1);
  display: flex;
  align-items: center;
  gap: 16rpx;
  transition: all 0.3s ease;
  box-shadow: 0 4rpx 20rpx rgba(34, 197, 94, 0.08);
}

.info-card:hover {
  transform: translateY(-4rpx);
  box-shadow: 0 8rpx 32rpx rgba(34, 197, 94, 0.15);
  border-color: rgba(34, 197, 94, 0.2);
}

.info-icon {
  font-size: 32rpx;
  filter: drop-shadow(0 2rpx 8rpx rgba(34, 197, 94, 0.3));
}

.info-content {
  flex: 1;
}

.info-label {
  font-size: 24rpx;
  color: #6b7280;
  font-weight: 500;
  margin-bottom: 4rpx;
}

.info-value {
  font-size: 28rpx;
  color: #1f2937;
  font-weight: 600;
}

/* 摘要区域 */
.summary-section {
  margin-bottom: 32rpx;
  padding: 28rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx) saturate(180%);
  border-radius: 24rpx;
  border: 2rpx solid rgba(34, 197, 94, 0.15);
  position: relative;
  overflow: hidden;
}

.summary-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(135deg, #22c55e 0%, #3b82f6 50%, #8b5cf6 100%);
  opacity: 0.8;
}

.section-header {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.section-icon {
  font-size: 28rpx;
  filter: drop-shadow(0 2rpx 6rpx rgba(34, 197, 94, 0.3));
}

.section-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1f2937;
  background: linear-gradient(135deg, #059669 0%, #22c55e 100%);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.summary-content {
  font-size: 30rpx;
  color: #374151;
  line-height: 1.7;
  text-align: justify;
}

/* 详情内容区域 */
.detail-section {
  margin-bottom: 32rpx;
  padding: 28rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx) saturate(180%);
  border-radius: 24rpx;
  border: 2rpx solid rgba(34, 197, 94, 0.15);
  position: relative;
  overflow: hidden;
}

.detail-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(135deg, #22c55e 0%, #3b82f6 50%, #8b5cf6 100%);
  opacity: 0.8;
}

.detail-content {
  font-size: 30rpx;
  color: #374151;
  line-height: 1.7;
}







/* 内容详情样式 */
.content-display {
  width: 100%;
  min-height: 200rpx; /* 确保有足够的内容高度 */
}

.content-paragraph {
  margin-bottom: 32rpx;
  line-height: 1.8;
  font-size: 30rpx;
  color: #374151;
  text-align: justify;
  padding: 24rpx;
  background: rgba(248, 250, 252, 0.8);
  border-radius: 16rpx;
  border-left: 4rpx solid #22c55e;
}

.subsection-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #059669;
  margin: 32rpx 0 16rpx 0;
  padding-bottom: 12rpx;
  border-bottom: 2rpx solid rgba(34, 197, 94, 0.2);
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.subsection-title::before {
  content: '▶';
  font-size: 24rpx;
  color: #22c55e;
}

.concepts-section,
.points-section {
  margin-bottom: 32rpx;
}

.concept-item,
.point-item {
  margin-bottom: 16rpx;
  padding: 16rpx 20rpx;
  background: rgba(34, 197, 94, 0.05);
  border-radius: 12rpx;
  border-left: 3rpx solid #22c55e;
  line-height: 1.7;
  font-size: 28rpx;
  color: #374151;
}

/* 组成部分 */
.components-section {
  margin-bottom: 32rpx;
}

.component-item {
  margin-bottom: 28rpx;
  padding: 24rpx;
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(8rpx);
  border-radius: 16rpx;
  border: 2rpx solid rgba(34, 197, 94, 0.1);
  box-shadow: 0 4rpx 16rpx rgba(34, 197, 94, 0.08);
}

.component-name {
  font-size: 32rpx;
  font-weight: 700;
  color: #059669;
  margin-bottom: 16rpx;
  padding-bottom: 8rpx;
  border-bottom: 1rpx solid rgba(34, 197, 94, 0.2);
}

.component-description {
  margin-bottom: 20rpx;
  line-height: 1.7;
  font-size: 28rpx;
  color: #374151;
}

.component-examples {
  margin-top: 16rpx;
  padding: 16rpx;
  background: rgba(34, 197, 94, 0.05);
  border-radius: 12rpx;
}

.examples-title {
  font-weight: 600;
  margin-bottom: 12rpx;
  color: #059669;
  font-size: 26rpx;
}

.example-item {
  padding: 8rpx 0;
  padding-left: 20rpx;
  line-height: 1.6;
  font-size: 26rpx;
  color: #4b5563;
}

/* 子章节 */
.subsections {
  margin-bottom: 32rpx;
}

.subsection-item {
  margin-bottom: 28rpx;
  padding: 24rpx;
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(8rpx);
  border-radius: 16rpx;
  border: 2rpx solid rgba(59, 130, 246, 0.1);
  box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.08);
}

.subsection-content {
  line-height: 1.7;
  font-size: 28rpx;
  color: #374151;
  margin-top: 16rpx;
}









/* 要点列表 */
.points-section .point-item {
  margin-bottom: 12rpx;
  padding-left: 16rpx;
  line-height: 1.6;
}

.point-term {
  font-weight: 600;
  color: #333;
  margin-bottom: 8rpx;
}


.point-description {
  line-height: 1.6;
}

/* 材料信息 */
.materials-section .material-item {
  margin-bottom: 24rpx;
  padding: 20rpx;
  background: #f8f9fa;
  border-radius: 8rpx;
}


.material-name {
  font-size: 30rpx;
  font-weight: 600;
  color: #f5576c;
  margin-bottom: 12rpx;
}

.material-description {
  line-height: 1.6;
}

/* 表格样式 */
.table-section {
  margin: 24rpx 0;
}

.table-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 16rpx;
  text-align: center;
}


.table-wrapper {
  border: 1rpx solid #e0e0e0;
  border-radius: 8rpx;
  overflow: hidden;
}


.table-header {
  display: flex;
  background: #f8f9fa;
}


.table-row {
  display: flex;
  border-top: 1rpx solid #e0e0e0;
}


.table-cell {
  flex: 1;
  padding: 16rpx 12rpx;
  font-size: 26rpx;
  text-align: center;
  border-right: 1rpx solid #e0e0e0;
}


.table-cell:last-child {
  border-right: none;
}

.header-cell {
  font-weight: 600;
  color: #333;
}


/* 子章节 */
.subsections .subsection-item {
  margin-bottom: 20rpx;
}

.subsection-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #00f2fe;
  margin-bottom: 12rpx;
}

.subsection-content {
  line-height: 1.6;
}

/* 噪音类型 */
.types-section .type-item {
  margin-bottom: 20rpx;
  padding: 16rpx;
  background: #f8f9fa;
  border-radius: 8rpx;
}


.type-name {
  font-weight: 600;
  color: #00f2fe;
  margin-bottom: 8rpx;
}

.type-description {
  line-height: 1.6;
}

/* 影响效果 */
.effects-section .effect-category {
  margin-bottom: 24rpx;
}

.effect-category-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #667eea;
  margin-bottom: 12rpx;
}

.effect-point {
  margin-bottom: 8rpx;
  padding-left: 16rpx;
  line-height: 1.6;
}

/* 保护方法 */
.methods-section .method-item {
  margin-bottom: 32rpx;
  padding: 24rpx;
  background: #f8f9fa;
  border-radius: 12rpx;
}


.method-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #667eea;
  margin-bottom: 16rpx;
}

.method-details {
  margin-bottom: 20rpx;
  line-height: 1.6;
}

.method-table {
  margin: 20rpx 0;
}

/* 设备信息 */
.devices-section .device-item {
  margin-bottom: 20rpx;
  padding: 16rpx;
  background: rgba(102, 126, 234, 0.05);
  border-radius: 8rpx;
  border-left: 4rpx solid #667eea;
}


.device-name {
  font-weight: 600;
  color: #667eea;
  margin-bottom: 8rpx;
}

.device-description {
  line-height: 1.6;
}

/* 横幅广告容器 */
.ad-banner-container {
  margin: 20rpx;
  padding: 10rpx;
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(10px);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
  border: 1rpx solid rgba(255, 255, 255, 0.3);
}

.ad-banner-container ad-custom {
  border-radius: 12rpx;
  overflow: hidden;
}

/* 底部广告样式 */
.ad-banner-container.bottom-ad {
  margin: 40rpx 20rpx 20rpx;
  background: rgba(255, 255, 255, 0.25);
  box-shadow: 0 8rpx 24rpx rgba(34, 197, 94, 0.15);
}