/* 飞行计算页面 - 整合三个计算页面的统一样式 */

/* 页面容器 */
.container {
  min-height: 100vh;
  background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 50%, #2563eb 100%);
  position: relative;
  overflow-x: hidden;
  padding: 0;
  margin: 0;
}

/* 动态背景效果 */
.container::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: 
    radial-gradient(circle at 20% 80%, rgba(59, 130, 246, 0.3) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(37, 99, 235, 0.3) 0%, transparent 50%),
    radial-gradient(circle at 40% 40%, rgba(29, 78, 216, 0.2) 0%, transparent 50%);
  animation: backgroundFloat 20s ease-in-out infinite;
  pointer-events: none;
  z-index: 0;
}

@keyframes backgroundFloat {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  33% { transform: translateY(-20px) rotate(1deg); }
  66% { transform: translateY(10px) rotate(-1deg); }
}

/* 页面标题 */
.page-header {
  text-align: center;
  padding: 40rpx 32rpx 30rpx 32rpx;
  position: relative;
  z-index: 10;
}

.header-title {
  font-size: 48rpx;
  font-weight: 800;
  color: #ffffff;
  text-shadow: 
    0 2rpx 4rpx rgba(0, 0, 0, 0.3),
    0 0 20rpx rgba(255, 255, 255, 0.5);
  margin-bottom: 8rpx;
  animation: titleGlow 3s ease-in-out infinite alternate;
}

.header-subtitle {
  font-size: 28rpx;
  font-weight: 500;
  color: rgba(255, 255, 255, 0.9);
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.2);
}

@keyframes titleGlow {
  0% {
    text-shadow: 
      0 2rpx 4rpx rgba(0, 0, 0, 0.3),
      0 0 20rpx rgba(255, 255, 255, 0.5);
  }
  100% {
    text-shadow: 
      0 2rpx 4rpx rgba(0, 0, 0, 0.3),
      0 0 30rpx rgba(255, 255, 255, 0.8),
      0 0 40rpx rgba(59, 130, 246, 0.3);
  }
}

/* 广告容器 */
.calculator-ad-container {
  margin: 20rpx 16rpx;
  border-radius: 16rpx;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(10rpx);
  box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.1);
}

/* 主页面样式 */
.main-page {
  position: relative;
  z-index: 10;
}

/* 功能分区样式 */
.section {
  margin-bottom: 40rpx;
}

.section-header {
  display: flex;
  align-items: center;
  padding: 20rpx 24rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  margin: 0 16rpx 20rpx 16rpx;
  border-radius: 20rpx;
  box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.1);
}

.section-icon {
  font-size: 40rpx;
  margin-right: 16rpx;
  animation: iconFloat 3s ease-in-out infinite;
}

.section-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #1e293b;
  margin-right: 16rpx;
}

.section-desc {
  font-size: 24rpx;
  color: #64748b;
  flex: 1;
}

/* 分类网格 */
.categories-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20rpx;
  padding: 20rpx;
  position: relative;
  z-index: 10;
}

/* 分类卡片 */
.category-card {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 24rpx;
  padding: 40rpx 32rpx;
  text-align: center;
  box-shadow: 
    0 8rpx 32rpx rgba(0, 0, 0, 0.1),
    0 0 0 1rpx rgba(255, 255, 255, 0.2);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  animation: cardSlideIn 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.category-card:nth-child(1) { animation-delay: 0.1s; }
.category-card:nth-child(2) { animation-delay: 0.2s; }
.category-card:nth-child(3) { animation-delay: 0.3s; }

@keyframes cardSlideIn {
  0% {
    opacity: 0;
    transform: translateY(30rpx) scale(0.9);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.category-card:hover {
  transform: translateY(-8rpx) scale(1.02);
  box-shadow: 
    0 16rpx 48rpx rgba(59, 130, 246, 0.2),
    0 0 0 2rpx rgba(59, 130, 246, 0.1);
}

.category-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.6s;
}

.category-card:active::before {
  left: 100%;
}

.category-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
  animation: iconFloat 3s ease-in-out infinite;
}

@keyframes iconFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-8rpx); }
}

.category-title {
  font-size: 40rpx;
  font-weight: 700;
  color: #1e293b;
  margin-bottom: 12rpx;
}

.category-desc {
  font-size: 28rpx;
  color: #64748b;
  line-height: 1.4;
  margin-bottom: 16rpx;
}

.category-count {
  font-size: 24rpx;
  color: #3b82f6;
  font-weight: 600;
  background: rgba(59, 130, 246, 0.1);
  padding: 8rpx 16rpx;
  border-radius: 12rpx;
  display: inline-block;
}

/* 分类页面和模块页面样式 */
.category-page,
.calculation-page {
  position: relative;
  z-index: 10;
  animation: pageSlideIn 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes pageSlideIn {
  0% {
    opacity: 0;
    transform: translateX(30rpx);
  }
  100% {
    opacity: 1;
    transform: translateX(0);
  }
}

/* 模块头部 */
.module-header {
  display: flex;
  align-items: center;
  padding: 20rpx 24rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  margin: 0 16rpx 20rpx 16rpx;
  border-radius: 20rpx;
  box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.1);
}

.back-btn {
  font-size: 32rpx;
  color: #3b82f6;
  font-weight: 600;
  padding: 8rpx 16rpx;
  border-radius: 12rpx;
  background: rgba(59, 130, 246, 0.1);
  transition: all 0.3s;
}

.back-btn:active {
  background: rgba(59, 130, 246, 0.2);
  transform: scale(0.95);
}

.module-header .module-title {
  flex: 1;
  text-align: center;
  font-size: 36rpx;
  font-weight: 700;
  color: #1e293b;
  margin: 0;
}

/* 功能模块网格 */
.modules-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
  padding: 20rpx;
  position: relative;
  z-index: 10;
}

/* 功能模块卡片 */
.module-card {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 24rpx;
  padding: 32rpx 24rpx;
  text-align: center;
  box-shadow: 
    0 8rpx 32rpx rgba(0, 0, 0, 0.1),
    0 0 0 1rpx rgba(255, 255, 255, 0.2);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  animation: cardSlideIn 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.module-card:nth-child(1) { animation-delay: 0.1s; }
.module-card:nth-child(2) { animation-delay: 0.2s; }
.module-card:nth-child(3) { animation-delay: 0.3s; }
.module-card:nth-child(4) { animation-delay: 0.4s; }
.module-card:nth-child(5) { animation-delay: 0.5s; }
.module-card:nth-child(6) { animation-delay: 0.6s; }

.module-card:hover {
  transform: translateY(-8rpx) scale(1.02);
  box-shadow: 
    0 16rpx 48rpx rgba(59, 130, 246, 0.2),
    0 0 0 2rpx rgba(59, 130, 246, 0.1);
}

.module-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.6s;
}

.module-card:active::before {
  left: 100%;
}

.module-icon {
  font-size: 64rpx;
  margin-bottom: 16rpx;
  animation: iconFloat 3s ease-in-out infinite;
}

/* 模块积分标签样式 */
.module-cost {
  position: absolute;
  bottom: 16rpx;
  left: 16rpx;
  z-index: 10;
}

.module-cost .van-tag {
  padding: 8rpx 16rpx !important;
  font-size: 22rpx !important;
  line-height: 28rpx !important;
  border-radius: 12rpx !important;
  font-weight: 600 !important;
}

.module-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1e293b;
  margin-bottom: 8rpx;
}

.module-desc {
  font-size: 24rpx;
  color: #64748b;
  line-height: 1.4;
}

/* 计算内容区域 */
.calculation-content {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 20rpx;
  margin: 0 16rpx 20rpx 16rpx;
  padding: 40rpx 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.1);
  text-align: center;
}

.placeholder-text {
  font-size: 32rpx;
  color: #64748b;
  font-weight: 500;
}

/* 计算模块容器 */
.calculation-module {
  position: relative;
  z-index: 10;
  animation: moduleSlideIn 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes moduleSlideIn {
  0% {
    opacity: 0;
    transform: translateX(30rpx);
  }
  100% {
    opacity: 1;
    transform: translateX(0);
  }
}

/* 输入区域 */
.input-section {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 20rpx;
  margin: 0 16rpx 20rpx 16rpx;
  padding: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.1);
}

.input-group {
  margin-bottom: 24rpx;
}

.input-group:last-child {
  margin-bottom: 0;
}

.input-label {
  font-size: 30rpx;
  font-weight: 600;
  color: #374151;
  margin-bottom: 12rpx;
  padding-left: 8rpx;
}

/* 输入框美化 */
.van-field {
  background: rgba(248, 250, 252, 0.8) !important;
  border-radius: 16rpx !important;
  border: 2rpx solid rgba(59, 130, 246, 0.1) !important;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1) !important;
}

.van-field:hover {
  border-color: rgba(59, 130, 246, 0.3) !important;
  transform: translateY(-2rpx) !important;
}

.van-field--focus {
  border-color: #3b82f6 !important;
  box-shadow: 0 0 0 4rpx rgba(59, 130, 246, 0.1) !important;
  transform: translateY(-2rpx) !important;
}

.van-field__input {
  font-size: 32rpx !important;
  font-weight: 500 !important;
  color: #1f2937 !important;
}

/* 单位标签 */
.unit-label {
  background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%) !important;
  color: #ffffff !important;
  padding: 8rpx 16rpx !important;
  border-radius: 12rpx !important;
  font-size: 24rpx !important;
  font-weight: 600 !important;
  box-shadow: 0 2rpx 8rpx rgba(59, 130, 246, 0.3) !important;
}

/* 操作按钮 */
.action-buttons {
  padding: 0 16rpx 20rpx 16rpx;
  display: flex;
  gap: 16rpx;
}

.van-button {
  flex: 1 !important;
  border-radius: 16rpx !important;
  font-weight: 700 !important;
  font-size: 32rpx !important;
  padding: 24rpx !important;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1) !important;
  position: relative !important;
  overflow: hidden !important;
}

.van-button[type="primary"] {
  background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%) !important;
  border: none !important;
  box-shadow: 0 8rpx 24rpx rgba(59, 130, 246, 0.3) !important;
}

.van-button[type="primary"]:active {
  transform: translateY(2rpx) scale(0.98) !important;
}

.van-button[type="default"] {
  background: rgba(255, 255, 255, 0.9) !important;
  border: 2rpx solid rgba(59, 130, 246, 0.2) !important;
  color: #3b82f6 !important;
}

.van-button[type="default"]:active {
  background: rgba(59, 130, 246, 0.1) !important;
  transform: translateY(2rpx) scale(0.98) !important;
}

/* 结果区域 */
.result-section {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx);
  border-radius: 20rpx;
  margin: 0 16rpx 20rpx 16rpx;
  padding: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(16, 185, 129, 0.1);
  border-left: 6rpx solid #10b981;
  animation: resultSlideIn 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes resultSlideIn {
  0% {
    opacity: 0;
    transform: translateY(20rpx);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

.result-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #065f46;
  margin-bottom: 20rpx;
  text-align: center;
}

.result-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16rpx;
}

.result-item {
  background: rgba(16, 185, 129, 0.05);
  border-radius: 12rpx;
  padding: 16rpx;
  text-align: center;
  border: 1rpx solid rgba(16, 185, 129, 0.1);
}

.result-label {
  font-size: 24rpx;
  color: #064e3b;
  margin-bottom: 8rpx;
  font-weight: 500;
}

.result-value {
  font-size: 28rpx;
  font-weight: 700;
  color: #059669;
}

/* 罗盘区域 */
.compass-section {
  margin-top: 24rpx;
  text-align: center;
}

.compass-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #065f46;
  margin-bottom: 16rpx;
}

.compass-container {
  position: relative;
  width: 200rpx;
  height: 200rpx;
  margin: 0 auto 16rpx auto;
  border-radius: 50%;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(248, 250, 252, 0.8) 100%);
  backdrop-filter: blur(10rpx);
  box-shadow: 
    0 8rpx 24rpx rgba(59, 130, 246, 0.15),
    0 0 0 2rpx rgba(255, 255, 255, 0.3);
  border: 4rpx solid rgba(59, 130, 246, 0.1);
}

.compass-needle {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 4rpx;
  height: 80rpx;
  transform-origin: bottom center;
  border-radius: 2rpx;
  transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  margin-left: -2rpx;
  margin-top: -80rpx;
}

.wind-needle {
  background: linear-gradient(to top, #ef4444 0%, #dc2626 100%);
  z-index: 3;
}

.heading-needle {
  background: linear-gradient(to top, #3b82f6 0%, #1d4ed8 100%);
  z-index: 2;
  height: 70rpx;
  margin-top: -70rpx;
}

.track-needle {
  background: linear-gradient(to top, #10b981 0%, #059669 100%);
  z-index: 1;
  height: 60rpx;
  margin-top: -60rpx;
}

.compass-degrees {
  position: absolute;
  font-size: 20rpx;
  font-weight: 600;
  color: #374151;
}

.compass-degrees.north { top: 8rpx; left: 50%; transform: translateX(-50%); }
.compass-degrees.east { top: 50%; right: 8rpx; transform: translateY(-50%); }
.compass-degrees.south { bottom: 8rpx; left: 50%; transform: translateX(-50%); }
.compass-degrees.west { top: 50%; left: 8rpx; transform: translateY(-50%); }

.compass-center {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 12rpx;
  height: 12rpx;
  background: #374151;
  border-radius: 50%;
  transform: translate(-50%, -50%);
  z-index: 4;
}

.compass-legend {
  display: flex;
  justify-content: center;
  gap: 24rpx;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 24rpx;
  color: #374151;
}

.legend-color {
  width: 16rpx;
  height: 16rpx;
  border-radius: 2rpx;
}

.wind-color { background: #ef4444; }
.heading-color { background: #3b82f6; }
.track-color { background: #10b981; }

/* 计算详情 */
.calculation-details {
  margin-top: 24rpx;
  background: rgba(59, 130, 246, 0.05);
  border-radius: 12rpx;
  padding: 16rpx;
  border: 1rpx solid rgba(59, 130, 246, 0.1);
}

.details-title {
  font-size: 26rpx;
  font-weight: 600;
  color: #1e40af;
  margin-bottom: 12rpx;
}

.details-content {
  font-size: 24rpx;
  color: #1e3a8a;
  line-height: 1.6;
  white-space: pre-line;
}


/* 响应式设计 */
@media screen and (max-width: 750rpx) {
  .modules-grid {
    grid-template-columns: 1fr;
    gap: 16rpx;
  }
  
  .module-card {
    padding: 24rpx 20rpx;
  }
  
  .module-icon {
    font-size: 48rpx;
  }
  
  .module-title {
    font-size: 28rpx;
  }
  
  .module-desc {
    font-size: 22rpx;
  }
  
  .category-icon {
    font-size: 64rpx;
  }
  
  .category-title {
    font-size: 32rpx;
  }
  
  .category-desc {
    font-size: 24rpx;
  }
  
  .result-grid {
    grid-template-columns: 1fr;
    gap: 12rpx;
  }
  
  .action-buttons {
    flex-direction: column;
  }
  
  .compass-container {
    width: 160rpx;
    height: 160rpx;
  }
  
  .compass-needle {
    height: 60rpx;
    margin-top: -60rpx;
  }
  
  .heading-needle {
    height: 50rpx;
    margin-top: -50rpx;
  }
  
  .track-needle {
    height: 40rpx;
    margin-top: -40rpx;
  }
}

/* ========== 特殊计算模块样式 ========== */

/* 节标题样式 */
.section-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #374151;
  margin-bottom: 20rpx;
  padding-left: 8rpx;
  border-left: 6rpx solid #3b82f6;
}

/* 开关组样式 */
.switch-group {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 16rpx;
  background: rgba(248, 250, 252, 0.8);
  border-radius: 16rpx;
  margin-bottom: 16rpx;
  border: 2rpx solid rgba(59, 130, 246, 0.1);
}

.switch-label {
  font-size: 28rpx;
  font-weight: 600;
  color: #374151;
  flex: 1;
}

.switch-desc {
  font-size: 24rpx;
  color: #6b7280;
  margin-top: 4rpx;
}

/* 选择器组样式 */
.picker-group {
  margin-bottom: 24rpx;
}

.picker-label {
  font-size: 30rpx;
  font-weight: 600;
  color: #374151;
  margin-bottom: 12rpx;
  padding-left: 8rpx;
}

.picker-value {
  background: rgba(248, 250, 252, 0.8);
  border: 2rpx solid rgba(59, 130, 246, 0.1);
  border-radius: 16rpx;
  padding: 24rpx 20rpx;
  font-size: 28rpx;
  color: #1f2937;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
}

.picker-value:active {
  background: rgba(59, 130, 246, 0.05);
  border-color: rgba(59, 130, 246, 0.3);
  transform: scale(0.98);
}

.picker-value::after {
  content: '▼';
  position: absolute;
  right: 20rpx;
  top: 50%;
  transform: translateY(-50%);
  color: #6b7280;
  font-size: 24rpx;
}

/* 结果列表样式 */
.result-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.result-name {
  font-size: 28rpx;
  font-weight: 700;
  color: #065f46;
  margin-bottom: 12rpx;
  padding: 8rpx 16rpx;
  background: rgba(16, 185, 129, 0.1);
  border-radius: 8rpx;
  border-left: 4rpx solid #10b981;
}

.result-detail {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  padding: 0 16rpx;
}

.result-original,
.result-correction,
.result-final {
  font-size: 26rpx;
  padding: 8rpx 12rpx;
  border-radius: 8rpx;
}

.result-original {
  color: #6b7280;
  background: rgba(107, 114, 128, 0.1);
}

.result-correction {
  color: #059669;
  background: rgba(16, 185, 129, 0.1);
}

.result-final {
  color: #065f46;
  background: rgba(16, 185, 129, 0.15);
  font-weight: 600;
}

.result-warning {
  margin-top: 12rpx;
  padding: 16rpx;
  background: rgba(245, 158, 11, 0.1);
  border-radius: 12rpx;
  border-left: 4rpx solid #f59e0b;
}

.warning-title {
  font-size: 26rpx;
  font-weight: 600;
  color: #92400e;
  margin-bottom: 8rpx;
}

.warning-message {
  font-size: 24rpx;
  color: #b45309;
  line-height: 1.5;
}

/* 错误信息样式 */
.error-section {
  background: rgba(239, 68, 68, 0.05);
  border: 2rpx solid rgba(239, 68, 68, 0.2);
  border-radius: 16rpx;
  padding: 20rpx;
  margin: 20rpx 16rpx;
}

.error-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #dc2626;
  margin-bottom: 8rpx;
}

.error-message {
  font-size: 26rpx;
  color: #b91c1c;
  line-height: 1.5;
}

/* 信息区域样式 */
.info-section {
  background: rgba(59, 130, 246, 0.05);
  border: 2rpx solid rgba(59, 130, 246, 0.2);
  border-radius: 16rpx;
  padding: 20rpx;
  margin: 20rpx 16rpx;
}

.info-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #1d4ed8;
  margin-bottom: 12rpx;
}

.info-list {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.info-item {
  font-size: 26rpx;
  color: #1e40af;
  padding: 8rpx 12rpx;
  background: rgba(59, 130, 246, 0.08);
  border-radius: 8rpx;
}

/* 警告项和普通项样式 */
.warning-item {
  background: rgba(245, 158, 11, 0.1) !important;
  border-color: rgba(245, 158, 11, 0.2) !important;
}

.warning-item .result-value {
  color: #d97706 !important;
  font-weight: 700;
}

.normal-item {
  background: rgba(16, 185, 129, 0.05) !important;
  border-color: rgba(16, 185, 129, 0.1) !important;
}

.normal-item .result-value {
  color: #059669 !important;
}


/* ========== ACR-PCR 模块专用样式 ========== */
.acr-module {
  position: relative;
}

.acr-result {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.05) 0%, rgba(16, 185, 129, 0.05) 100%);
  border: 2rpx solid rgba(59, 130, 246, 0.2);
  border-radius: 20rpx;
  padding: 24rpx;
  margin: 20rpx 16rpx;
}

.acr-result .result-title {
  font-size: 32rpx;
  font-weight: 800;
  color: #1e40af;
  margin-bottom: 20rpx;
  text-align: center;
}

.acr-result .result-card {
  background: rgba(248, 250, 252, 0.9);
  border: 2rpx solid rgba(59, 130, 246, 0.1);
  border-radius: 16rpx;
  padding: 20rpx;
  margin-bottom: 16rpx;
}

.acr-result .card-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #3730a3;
  margin-bottom: 16rpx;
  padding-bottom: 8rpx;
  border-bottom: 2rpx solid rgba(59, 130, 246, 0.2);
}

.acr-result .operation-status {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 16rpx 20rpx;
  border-radius: 12rpx;
  margin-bottom: 12rpx;
}

.acr-result .success-status {
  background: rgba(34, 197, 94, 0.1);
  border: 2rpx solid rgba(34, 197, 94, 0.3);
}

.acr-result .error-status {
  background: rgba(239, 68, 68, 0.1);
  border: 2rpx solid rgba(239, 68, 68, 0.3);
}

.acr-result .status-icon {
  font-size: 32rpx;
}

.acr-result .status-text {
  font-size: 28rpx;
  font-weight: 700;
  color: #1f2937;
}

.acr-result .status-reason {
  font-size: 24rpx;
  color: #6b7280;
  line-height: 1.5;
  margin-top: 8rpx;
}

.acr-result .success-text {
  color: #16a34a !important;
  font-weight: 700;
}

.acr-result .error-text {
  color: #dc2626 !important;
  font-weight: 700;
}

.acr-error-section {
  background: rgba(239, 68, 68, 0.05);
  border: 2rpx solid rgba(239, 68, 68, 0.2);
  border-radius: 16rpx;
  padding: 20rpx;
  margin: 20rpx 16rpx;
}

.loading-section {
  text-align: center;
  padding: 40rpx 20rpx;
  color: #6b7280;
}

.loading-title {
  font-size: 28rpx;
  font-weight: 600;
  margin-bottom: 8rpx;
}

.loading-desc {
  font-size: 24rpx;
  color: #9ca3af;
}

/* ========== GPWS 模块专用样式 ========== */
.gpws-module {
  position: relative;
}

.gpws-mode-selector {
  margin: 20rpx 16rpx;
  background: rgba(248, 250, 252, 0.9);
  border: 2rpx solid rgba(59, 130, 246, 0.1);
  border-radius: 16rpx;
  padding: 20rpx;
}

.gpws-mode-selector .section-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #1e40af;
  margin-bottom: 16rpx;
  text-align: center;
}

.mode-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  justify-content: center;
}

.mode-tab {
  background: rgba(148, 163, 184, 0.1);
  border: 2rpx solid rgba(148, 163, 184, 0.2);
  border-radius: 12rpx;
  padding: 16rpx 24rpx;
  font-size: 26rpx;
  font-weight: 600;
  color: #64748b;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  cursor: pointer;
}

.mode-tab.active {
  background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
  border-color: #1d4ed8;
  color: #ffffff;
  transform: translateY(-2rpx);
  box-shadow: 0 8rpx 20rpx rgba(59, 130, 246, 0.3);
}

.mode-tab:active {
  transform: scale(0.95);
}

.gpws-mode-content {
  background: rgba(248, 250, 252, 0.9);
  border: 2rpx solid rgba(59, 130, 246, 0.1);
  border-radius: 16rpx;
  padding: 24rpx;
  margin: 20rpx 16rpx;
}

.mode-header {
  text-align: center;
  margin-bottom: 24rpx;
  padding-bottom: 16rpx;
  border-bottom: 2rpx solid rgba(59, 130, 246, 0.2);
}

.mode-title {
  font-size: 32rpx;
  font-weight: 800;
  color: #1e40af;
  margin-bottom: 8rpx;
}

.mode-desc {
  font-size: 24rpx;
  color: #6b7280;
  line-height: 1.5;
}

.gpws-result {
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.05) 0%, rgba(245, 158, 11, 0.05) 100%);
  border: 2rpx solid rgba(239, 68, 68, 0.2);
  border-radius: 16rpx;
  padding: 20rpx;
  margin: 20rpx 0;
}

.gpws-result .result-title {
  font-size: 30rpx;
  font-weight: 800;
  color: #dc2626;
  margin-bottom: 16rpx;
  text-align: center;
}

.gpws-status {
  padding: 16rpx 20rpx;
  border-radius: 12rpx;
  margin-bottom: 16rpx;
  text-align: center;
}

.gpws-status.safe {
  background: rgba(34, 197, 94, 0.1);
  border: 2rpx solid rgba(34, 197, 94, 0.3);
}

.gpws-status.warning {
  background: rgba(245, 158, 11, 0.1);
  border: 2rpx solid rgba(245, 158, 11, 0.3);
}

.gpws-status.danger {
  background: rgba(239, 68, 68, 0.1);
  border: 2rpx solid rgba(239, 68, 68, 0.3);
}

.gpws-status .status-text {
  font-size: 28rpx;
  font-weight: 800;
  margin-bottom: 8rpx;
}

.gpws-status.safe .status-text {
  color: #16a34a;
}

.gpws-status.warning .status-text {
  color: #d97706;
}

.gpws-status.danger .status-text {
  color: #dc2626;
}

.gpws-status .status-message {
  font-size: 24rpx;
  line-height: 1.5;
  color: #6b7280;
}

.switch-group {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.switch-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16rpx 20rpx;
  background: rgba(248, 250, 252, 0.8);
  border: 2rpx solid rgba(59, 130, 246, 0.1);
  border-radius: 12rpx;
}

.switch-item .switch-label {
  font-size: 26rpx;
  font-weight: 600;
  color: #374151;
}

.result-detail {
  background: rgba(59, 130, 246, 0.05);
  border: 2rpx solid rgba(59, 130, 246, 0.1);
  border-radius: 12rpx;
  padding: 16rpx;
  margin-top: 16rpx;
}

.result-detail .detail-title {
  font-size: 26rpx;
  font-weight: 700;
  color: #1e40af;
  margin-bottom: 8rpx;
}

.result-detail .detail-text {
  font-size: 24rpx;
  color: #475569;
  line-height: 1.5;
  margin-bottom: 8rpx;
}

.result-detail .detail-text:last-child {
  margin-bottom: 0;
}

.mode-info {
  font-size: 24rpx;
  color: #6b7280;
  text-align: center;
  margin-bottom: 16rpx;
  padding: 12rpx;
  background: rgba(59, 130, 246, 0.05);
  border-radius: 8rpx;
}

/* ========== 常用换算模块专用样式 ========== */

/* 确保按钮在常用换算中有特殊的视觉效果 */
.calculation-page .van-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.6s;
}

.calculation-page .van-button:active::before {
  left: 100%;
}

/* 换算模块的特殊强调样式 */
.calculation-page .unit-label {
  animation: unitLabelPulse 2s ease-in-out infinite;
}

@keyframes unitLabelPulse {
  0%, 100% {
    box-shadow: 0 2rpx 8rpx rgba(59, 130, 246, 0.3);
  }
  50% {
    box-shadow: 0 4rpx 16rpx rgba(59, 130, 246, 0.5);
  }
}

/* 结果显示增强动画 */
.calculation-page .result-item {
  animation: resultItemSlideIn 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes resultItemSlideIn {
  0% {
    opacity: 0;
    transform: translateX(-20rpx);
  }
  100% {
    opacity: 1;
    transform: translateX(0);
  }
}

/* 输入框聚焦时的增强效果 */
.calculation-page .van-field--focus {
  transform: translateY(-4rpx) !important;
  box-shadow: 0 8rpx 24rpx rgba(59, 130, 246, 0.15) !important;
}

/* 换算按钮的特殊渐变效果 */
.calculation-page .van-button[type="primary"] {
  background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 50%, #2563eb 100%) !important;
  position: relative !important;
  overflow: hidden !important;
}

.calculation-page .van-button[type="primary"]::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.1) 50%, transparent 70%);
  transform: translateX(-100%);
  transition: transform 0.6s;
}

.calculation-page .van-button[type="primary"]:active::after {
  transform: translateX(100%);
}

