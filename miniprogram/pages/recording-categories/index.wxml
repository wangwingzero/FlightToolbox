<view class="container">
  <!-- 页面头部 -->
  <view class="page-header">
    <view class="header-country">
      <text class="country-flag-large">{{ regionFlag }}</text>
      <view class="country-text">
        <text class="country-title">{{ regionName }}</text>
        <text class="country-subtitle">真实陆空通话录音</text>
      </view>
    </view>
    <view class="header-guide">
      <text class="guide-icon">👇</text>
      <text class="guide-text">选择通信类型开始学习</text>
    </view>
  </view>

  <!-- 分类卡片列表 -->
  <view class="categories-list">
    <view wx:for="{{ recordingCategories }}" wx:key="id" 
      class="category-item" 
      bind:tap="selectCategory" 
      data-category="{{ item.id }}">
      <!-- 图标区域 -->
      <view class="category-icon-area">
        <view class="icon-circle" style="background: {{ item.color }}15;">
          <text class="category-emoji">{{ item.icon }}</text>
        </view>
      </view>
      
      <!-- 信息区域 -->
      <view class="category-content">
        <text class="category-title">{{ item.name }}</text>
        <text class="category-description">
          {{ item.clips.length }} 段录音可供学习
        </text>
      </view>
      
      <!-- 操作区域 -->
      <view class="category-arrow">
        <text class="arrow-icon">▶</text>
      </view>
    </view>
  </view>

  <!-- 横幅广告 -->
  <view wx:if="{{ !isAdFree }}" class="ad-section">
    <ad unit-id="adunit-3a1bf3800fa937a2" ad-type="banner" ad-intervals="30"></ad>
  </view>
  
  <!-- 底部安全区域 -->
  <view class="safe-area-bottom"></view>
</view>