<view class="detail-container {{ categoryType }}">
  <!-- 页面标题 -->
  <view class="page-header">
    <view class="header-title">{{ categoryTitle }}</view>
    <view class="header-subtitle">直接展开展示全部内容，点击条目可复制</view>
  </view>

  <!-- 横幅广告 -->
  <view wx:if="{{ !isAdFree }}" class="ad-container">
    <ad unit-id="adunit-3a1bf3800fa937a2" ad-intervals="30" bindload="adLoad" binderror="adError" bindclose="adClose"></ad>
  </view>

  <!-- 加载状态 -->
  <view wx:if="{{ loading }}" class="loading-container">
    <van-loading type="spinner" size="24px">加载中...</van-loading>
  </view>

  <!-- 内容区域 - 直接展开所有章节 -->
  <view wx:else class="content-container">
    <!-- 章节列表 -->
    <view wx:for="{{ filteredData }}" wx:key="id" class="section-card">
      <!-- 章节标题 -->
      <view class="section-header">
        <view class="section-icon">{{ item.icon }}</view>
        <view class="section-info">
          <view class="section-title">{{ item.title }}</view>
        </view>
      </view>

      <!-- 章节内容 -->
      <view class="section-content">
        
        <!-- 文本类型 -->
        <view wx:if="{{ item.type === 'text' }}" class="text-content">
          <view wx:for="{{ item.content }}" wx:key="index" class="text-item" bind:tap="copyContent" data-content="{{ item }}">
            {{ item }}
          </view>
        </view>

        <!-- 列表类型 -->
        <view wx:if="{{ item.type === 'list' }}" class="list-content">
          <view wx:for="{{ item.content }}" wx:key="index" class="list-item" bind:tap="copyContent" data-content="{{ item }}">
            <view class="list-number">{{ index + 1 }}</view>
            <view class="list-text">{{ item }}</view>
          </view>
        </view>

        <!-- 通话结构类型 -->
        <view wx:if="{{ item.type === 'structure' }}" class="structure-content">
          <view wx:for="{{ item.content }}" wx:key="scenario" class="structure-item">
            <view class="structure-scenario">{{ item.scenario }}</view>
            <view class="structure-party">{{ item.party }}</view>
            <view class="structure-format" bind:tap="copyContent" data-content="{{ item.structure_zh }}">
              <text class="format-label">中文：</text>
              <text class="format-text">{{ item.structure_zh }}</text>
            </view>
            <view wx:if="{{ item.structure_en }}" class="structure-format" bind:tap="copyContent" data-content="{{ item.structure_en }}">
              <text class="format-label">英文：</text>
              <text class="format-text">{{ item.structure_en }}</text>
            </view>
            <view wx:if="{{ item.note }}" class="structure-note">
              <text class="note-label">注意：</text>
              <text class="note-text">{{ item.note }}</text>
            </view>
          </view>
        </view>

        <!-- 数字发音类型 -->
        <view wx:if="{{ item.type === 'numbers' }}" class="numbers-content">
          <!-- 标准数字表 -->
          <view wx:if="{{ item.content.standard }}" class="numbers-section">
            <view class="subsection-title">标准数字发音</view>
            <view wx:if="{{ item.content.standard.note }}" class="note-text">{{ item.content.standard.note }}</view>
            <view class="numbers-table">
              <view wx:for="{{ item.content.standard.table }}" wx:key="digit" class="number-row">
                <view class="number-digit">{{ item.digit }}</view>
                <view class="number-zh" bind:tap="copyContent" data-content="{{ item.pronunciation_zh }}">{{ item.pronunciation_zh }}</view>
                <view class="number-en" bind:tap="copyContent" data-content="{{ item.pronunciation_en }}">{{ item.pronunciation_en }}</view>
              </view>
            </view>
          </view>

          <!-- 数字组合 -->
          <view wx:if="{{ item.content.combinations }}" class="numbers-section">
            <view class="subsection-title">数字组合读法</view>
            <view class="rule-text">{{ item.content.combinations.rule_zh }}</view>
            <view class="rule-text">{{ item.content.combinations.rule_en }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.combinations.examples }}" wx:key="number" class="example-item">
                <view class="example-number">{{ item.number }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>
        </view>

        <!-- ICAO字母表类型 -->
        <view wx:if="{{ item.type === 'alphabet' }}" class="alphabet-content">
          <view wx:if="{{ item.content.note }}" class="note-text">{{ item.content.note }}</view>
          <view class="alphabet-table">
            <view wx:for="{{ item.content.table }}" wx:key="letter" class="alphabet-row">
              <view class="alphabet-letter">{{ item.letter }}</view>
              <view class="alphabet-word" bind:tap="copyContent" data-content="{{ item.word }}">{{ item.word }}</view>
              <view class="alphabet-pronunciation" bind:tap="copyContent" data-content="{{ item.pronunciation }}">{{ item.pronunciation }}</view>
            </view>
          </view>
        </view>

        <!-- 标准用语类型 -->
        <view wx:if="{{ item.type === 'phrases' }}" class="phrases-content">
          <view wx:for="{{ item.content }}" wx:key="phrase" class="phrase-item">
            <view class="phrase-header">
              <view class="phrase-en" bind:tap="copyContent" data-content="{{ item.phrase }}">{{ item.phrase }}</view>
              <view class="phrase-zh" bind:tap="copyContent" data-content="{{ item.meaning_zh }}">{{ item.meaning_zh }}</view>
            </view>
            <view wx:if="{{ item.definition_zh }}" class="phrase-definition" bind:tap="copyContent" data-content="{{ item.definition_zh }}">
              <text class="definition-label">定义：</text>{{ item.definition_zh }}
            </view>
            <view wx:if="{{ item.definition_en }}" class="phrase-definition" bind:tap="copyContent" data-content="{{ item.definition_en }}">
              <text class="definition-label">Definition:</text>{{ item.definition_en }}
            </view>
            <view wx:if="{{ item.note }}" class="phrase-note">
              <text class="note-label">注意：</text>{{ item.note }}
            </view>
            <view wx:if="{{ item.details }}" class="phrase-details">
              <view wx:for="{{ item.details }}" wx:for-item="detail" wx:key="context" class="detail-item">
                <view class="detail-context">{{ detail.context_zh || detail.context }}</view>
                <view class="detail-usage" bind:tap="copyContent" data-content="{{ detail.usage_zh }}">{{ detail.usage_zh }}</view>
              </view>
            </view>
          </view>

          <!-- 查看完整 ICAO 标准对话句库入口 -->
          <view class="phrasebook-entry" bind:tap="openStandardPhraseology">
            <view class="phrasebook-title">查看 ICAO 标准对话完整句库</view>
            <view class="phrasebook-subtitle">按场景浏览全部通信用语</view>
          </view>
        </view>

        <!-- 特殊读法类型 -->
        <view wx:if="{{ item.type === 'special' }}" class="special-content">
          <!-- 高度读法 -->
          <view wx:if="{{ item.content.altitude }}" class="special-section">
            <view class="subsection-title">高度读法</view>
            
            <!-- 公制高度 -->
            <view wx:if="{{ item.content.altitude.metricSystem }}" class="metric-section">
              <view class="metric-title">公制高度</view>
              <view class="metric-description">{{ item.content.altitude.metricSystem.description }}</view>
              <view wx:if="{{ item.content.altitude.metricSystem.note }}" class="metric-note">{{ item.content.altitude.metricSystem.note }}</view>
              <view class="altitude-table">
                <view wx:for="{{ item.content.altitude.metricSystem.levels }}" wx:key="altitude" class="altitude-row">
                  <view class="altitude-value">{{ item.altitude }}</view>
                  <view class="altitude-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">{{ item.reading_zh }}</view>
                  <view class="altitude-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">{{ item.reading_en }}</view>
                </view>
              </view>
            </view>

            <!-- 英制高度 -->
            <view wx:if="{{ item.content.altitude.imperialSystem }}" class="imperial-section">
              <view class="imperial-title">英制高度</view>
              <view class="imperial-description">{{ item.content.altitude.imperialSystem.description_zh }}</view>
              <view class="examples-container">
                <view wx:for="{{ item.content.altitude.imperialSystem.examples }}" wx:key="altitude" class="example-item">
                  <view class="example-altitude">{{ item.altitude }}</view>
                  <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                    <text class="reading-label">中文：</text>{{ item.reading_zh }}
                  </view>
                  <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                    <text class="reading-label">英文：</text>{{ item.reading_en }}
                  </view>
                </view>
              </view>
            </view>
          </view>

          <!-- 气压表设定 altimeterSettings -->
          <view wx:if="{{ item.content.altimeterSettings }}" class="special-section">
            <view class="subsection-title">气压表设定</view>
            <view wx:if="{{ item.content.altimeterSettings.description }}" class="description-text">{{ item.content.altimeterSettings.description }}</view>
            <view class="rules-container">
              <view wx:for="{{ item.content.altimeterSettings.rules }}" wx:key="type" class="rule-item">
                <view class="rule-type">{{ item.type }}</view>
                <view class="rule-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="rule-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- MDA/DH -->
          <view wx:if="{{ item.content.mda_dh }}" class="special-section">
            <view class="subsection-title">最低下降高度/决断高度</view>
            <view class="mda-container">
              <view wx:for="{{ item.content.mda_dh }}" wx:key="type" class="mda-item">
                <view class="mda-type">{{ item.type }}</view>
                <view class="mda-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="mda-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 机场标高 airportElevation -->
          <view wx:if="{{ item.content.airportElevation }}" class="special-section">
            <view class="subsection-title">机场标高</view>
            <view class="elevation-reading" bind:tap="copyContent" data-content="{{ item.content.airportElevation.reading_zh }}">
              <text class="reading-label">中文：</text>{{ item.content.airportElevation.reading_zh }}
            </view>
            <view class="elevation-reading" bind:tap="copyContent" data-content="{{ item.content.airportElevation.reading_en }}">
              <text class="reading-label">英文：</text>{{ item.content.airportElevation.reading_en }}
            </view>
          </view>

          <!-- 时间 time -->
          <view wx:if="{{ item.content.time }}" class="special-section">
            <view class="subsection-title">时间</view>
            <view wx:if="{{ item.content.time.note }}" class="note-text">{{ item.content.time.note }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.time.examples }}" wx:key="time" class="example-item">
                <view class="example-time">{{ item.time }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 气压 pressure -->
          <view wx:if="{{ item.content.pressure }}" class="special-section">
            <view class="subsection-title">气压</view>
            <view class="rule-text">中文规则：{{ item.content.pressure.rules_zh }}</view>
            <view class="rule-text">英文规则：{{ item.content.pressure.rules_en }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.pressure.examples }}" wx:key="pressure" class="example-item">
                <view class="example-pressure">{{ item.pressure }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 航向 heading -->
          <view wx:if="{{ item.content.heading }}" class="special-section">
            <view class="subsection-title">航向</view>
            <view class="rule-text">中文规则：{{ item.content.heading.rules_zh }}</view>
            <view class="rule-text">英文规则：{{ item.content.heading.rules_en }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.heading.examples }}" wx:key="heading" class="example-item">
                <view class="example-heading">{{ item.heading }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 速度 speed -->
          <view wx:if="{{ item.content.speed }}" class="special-section">
            <view class="subsection-title">速度</view>
            <view class="rules-container">
              <view wx:for="{{ item.content.speed.rules }}" wx:key="unit" class="rule-item">
                <view class="rule-unit">{{ item.unit }}</view>
                <view class="rule-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="rule-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
            <view class="examples-container">
              <view wx:for="{{ item.content.speed.examples }}" wx:key="speed" class="example-item">
                <view class="example-speed">{{ item.speed }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 频率 frequency -->
          <view wx:if="{{ item.content.frequency }}" class="special-section">
            <view class="subsection-title">频率</view>
            <view class="rule-text">中文规则：{{ item.content.frequency.rules_zh }}</view>
            <view class="rule-text">英文规则：{{ item.content.frequency.rules_en }}</view>
            <view wx:if="{{ item.content.frequency.note }}" class="note-text">{{ item.content.frequency.note }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.frequency.examples }}" wx:key="frequency" class="example-item">
                <view class="example-frequency">{{ item.frequency }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 跑道 runway -->
          <view wx:if="{{ item.content.runway }}" class="special-section">
            <view class="subsection-title">跑道</view>
            <view class="rule-text">中文规则：{{ item.content.runway.rules_zh }}</view>
            <view class="rule-text">英文规则：{{ item.content.runway.rules_en }}</view>
            <view wx:if="{{ item.content.runway.note }}" class="note-text">{{ item.content.runway.note }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.runway.examples }}" wx:key="runway" class="example-item">
                <view class="example-runway">{{ item.runway }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 距离 distance -->
          <view wx:if="{{ item.content.distance }}" class="special-section">
            <view class="subsection-title">距离</view>
            <view class="rule-text">中文规则：{{ item.content.distance.rules_zh }}</view>
            <view class="rule-text">英文规则：{{ item.content.distance.rules_en }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.distance.examples }}" wx:key="distance" class="example-item">
                <view class="example-distance">{{ item.distance }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 应答机编码 squawkCode -->
          <view wx:if="{{ item.content.squawkCode }}" class="special-section">
            <view class="subsection-title">应答机编码</view>
            <view class="rule-text">中文规则：{{ item.content.squawkCode.rules_zh }}</view>
            <view class="rule-text">英文规则：{{ item.content.squawkCode.rules_en }}</view>
            <view wx:if="{{ item.content.squawkCode.note }}" class="note-text">{{ item.content.squawkCode.note }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.squawkCode.examples }}" wx:key="code" class="example-item">
                <view class="example-code">{{ item.code }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 经纬度 latLong -->
          <view wx:if="{{ item.content.latLong }}" class="special-section">
            <view class="subsection-title">经纬度</view>
            <view class="rule-text">中文规则：{{ item.content.latLong.rules_zh }}</view>
            <view class="rule-text">英文规则：{{ item.content.latLong.rules_en }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.latLong.examples }}" wx:key="coord" class="example-item">
                <view class="example-coord">{{ item.coord }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 航空器机型 aircraftType -->
          <view wx:if="{{ item.content.aircraftType }}" class="special-section">
            <view class="subsection-title">航空器机型</view>
            <view wx:if="{{ item.content.aircraftType.description }}" class="description-text">{{ item.content.aircraftType.description }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.aircraftType.examples }}" wx:key="type" class="example-item">
                <view class="example-type">{{ item.type }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <!-- 点钟方位 clockPosition -->
          <view wx:if="{{ item.content.clockPosition }}" class="special-section">
            <view class="subsection-title">点钟方位</view>
            <view class="rule-text">中文规则：{{ item.content.clockPosition.rules_zh }}</view>
            <view class="rule-text">英文规则：{{ item.content.clockPosition.rules_en }}</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.clockPosition.examples }}" wx:key="position" class="example-item">
                <view class="example-position">{{ item.position }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>
        </view>

        <!-- 管制单位呼号类型 -->
        <view wx:if="{{ item.type === 'controller' }}" class="controller-content">
          <view wx:if="{{ item.content.description }}" class="description-text">{{ item.content.description }}</view>
          <view wx:if="{{ item.content.suffixes }}" class="suffixes-table">
            <view class="table-header">
              <view class="header-cell">服务类型</view>
              <view class="header-cell">中文后缀</view>
              <view class="header-cell">英文后缀</view>
            </view>
            <view wx:for="{{ item.content.suffixes }}" wx:key="service_en" class="suffix-row">
              <view class="suffix-service">{{ item.service_zh }}</view>
              <view class="suffix-zh" bind:tap="copyContent" data-content="{{ item.suffix_zh }}">{{ item.suffix_zh }}</view>
              <view class="suffix-en" bind:tap="copyContent" data-content="{{ item.suffix_en }}">{{ item.suffix_en }}</view>
            </view>
          </view>
        </view>

        <!-- 航空器呼号类型 -->
        <view wx:if="{{ item.type === 'aircraft' }}" class="aircraft-content">
          <view wx:for="{{ item.content.types }}" wx:key="type" class="aircraft-type">
            <view class="type-title">{{ item.type }}</view>
            <view class="type-description">{{ item.description_zh }}</view>
            <view wx:if="{{ item.example1 }}" class="type-example" bind:tap="copyContent" data-content="{{ item.example1 }}">
              {{ item.example1 }}
            </view>
            <view wx:if="{{ item.example2_zh }}" class="type-example" bind:tap="copyContent" data-content="{{ item.example2_zh }}">
              {{ item.example2_zh }}
            </view>
            <view wx:if="{{ item.examples }}" class="examples-container">
              <view wx:for="{{ item.examples }}" wx:for-item="example" wx:key="callsign" class="example-item">
                <view class="example-callsign">{{ example.callsign }}</view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ example.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ example.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ example.reading_en }}">
                  <text class="reading-label">英文：</text>{{ example.reading_en }}
                </view>
              </view>
            </view>
          </view>
        </view>

        <!-- 天气报文类型 -->
        <view wx:if="{{ item.type === 'weather' }}" class="weather-content">
          <view wx:if="{{ item.content.formatCards }}" class="formats-container">
            <view wx:for="{{ item.content.formatCards }}" wx:for-item="fmt" wx:key="type" class="format-item">
              <view class="format-type">{{ fmt.type }}</view>
              <view class="format-zh" bind:tap="copyContent" data-content="{{ fmt.format_zh }}">
                <text class="format-label">中文格式：</text>{{ fmt.format_zh }}
              </view>
              <view class="format-en" bind:tap="copyContent" data-content="{{ fmt.format_en }}">
                <text class="format-label">英文格式：</text>{{ fmt.format_en }}
              </view>

              <!-- 紧跟在格式下面的示例通话 -->
              <view wx:if="{{ fmt.examples && fmt.examples.length }}" class="examples-container">
                <view wx:for="{{ fmt.examples }}" wx:for-item="example" wx:key="scenario" class="example-item">
                  <view class="example-scenario">{{ example.scenario }}</view>
                  <view wx:if="{{ example.data }}" class="example-data">{{ example.data }}</view>
                  <view class="example-reading" bind:tap="copyContent" data-content="{{ example.reading_zh }}">
                    <text class="reading-label">中文：</text>{{ example.reading_zh }}
                  </view>
                  <view class="example-reading" bind:tap="copyContent" data-content="{{ example.reading_en }}">
                    <text class="reading-label">英文：</text>{{ example.reading_en }}
                  </view>
                </view>
              </view>
            </view>
          </view>

          <!-- 云量缩写表（仅在有 cloudCover 数据时显示） -->
          <view wx:if="{{ item.content.cloudCover }}" class="cloudcover-container">
            <view class="subsection-title">云量缩写</view>
            <view class="examples-container">
              <view wx:for="{{ item.content.cloudCover }}" wx:key="abbr" class="example-item">
                <view class="example-abbr">{{ item.abbr }}</view>
                <view class="example-meaning">{{ item.meaning }}</view>
                <view wx:if="{{ item.reading_zh }}" class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_zh }}">
                  <text class="reading-label">中文：</text>{{ item.reading_zh }}
                </view>
                <view class="example-reading" bind:tap="copyContent" data-content="{{ item.reading_en }}">
                  <text class="reading-label">英文：</text>{{ item.reading_en }}
                </view>
              </view>
            </view>
          </view>

          <view wx:if="{{ item.content.note }}" class="weather-note">{{ item.content.note }}</view>
        </view>

        <!-- 通用内容类型 -->
        <view wx:if="{{ item.type === 'generic' }}" class="generic-content">
          <text>{{ item.content }}</text>
        </view>

      </view>
    </view>

    <!-- 空状态 -->
    <view wx:if="{{ filteredData.length === 0 }}" class="empty-state">
      <view class="empty-icon">🔍</view>
      <view class="empty-text">没有找到相关内容</view>
      <view class="empty-hint">请尝试其他关键词</view>
    </view>
  </view>
</view>

<van-toast id="van-toast" />