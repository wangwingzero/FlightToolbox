<view class="container">
  <!-- 个人预设表单 -->
  <van-cell-group title="个人预设信息">
    <van-field
      label="姓名"
      value="{{profile.name}}"
      placeholder="请输入姓名"
      bind:change="onFieldChange"
      data-field="name"
    />
    <van-field
      label="所属单位"
      value="{{profile.company}}"
      placeholder="请输入所属单位"
      bind:change="onFieldChange"
      data-field="company"
    />
    <van-field
      label="常用机型"
      value="{{profile.aircraftModel}}"
      placeholder="请输入常用机型"
      bind:change="onFieldChange"
      data-field="aircraftModel"
    />
  </van-cell-group>

  <!-- 填报时限设置 -->
  <van-cell-group title="填报时限设置">
    <van-field
      label="紧急事件时限"
      value="{{profile.urgentDeadline}}"
      placeholder="境内：24小时内"
      bind:change="onFieldChange"
      data-field="urgentDeadline"
    >
      <view slot="right-icon" class="deadline-hint">
        <van-tag type="danger" size="small">紧急</van-tag>
      </view>
    </van-field>
    <van-field
      label="非紧急事件时限"
      value="{{profile.nonUrgentDeadline}}"
      placeholder="48小时内填报"
      bind:change="onFieldChange"
      data-field="nonUrgentDeadline"
    >
      <view slot="right-icon" class="deadline-hint">
        <van-tag type="primary" size="small">非紧急</van-tag>
      </view>
    </van-field>
  </van-cell-group>

  <!-- 操作按钮 -->
  <van-cell-group title="操作">
    <van-cell>
      <van-button type="primary" block bind:click="saveProfile">
        保存预设
      </van-button>
    </van-cell>
    <van-cell>
      <van-button type="default" block bind:click="clearProfile">
        清除预设
      </van-button>
    </van-cell>
  </van-cell-group>

  <!-- 说明 -->
  <van-cell-group title="说明">
    <view class="help-content">
      <view class="help-text">
        设置个人预设信息后，在填写事件报告时会自动填充相关字段，提高填报效率。
      </view>
    </view>
  </van-cell-group>
</view> 