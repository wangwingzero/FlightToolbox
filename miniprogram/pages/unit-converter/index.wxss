/* 常用换算样式 - Material Design 3 美化版本 */
.container {
  padding: 0 16rpx !important;
  margin: 0 !important;
  background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
  min-height: 100vh;
  width: 100% !important;
  box-sizing: border-box !important;
  display: block !important;
  animation: fadeInUp 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes fadeInUp {
  0% {
    opacity: 0;
    transform: translateY(30rpx);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 标签页系统优化 */
.van-tabs {
  background: #ffffff;
  border-radius: 16rpx 16rpx 0 0;
  box-shadow: 0 4rpx 20rpx rgba(148, 163, 184, 0.1);
  margin: 16rpx 0 0 0;
  overflow: hidden;
}

.van-tabs__wrap {
  overflow-x: auto !important;
  background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
  border-bottom: 1px solid #e2e8f0;
}

.van-tabs__nav {
  display: flex !important;
  flex-wrap: nowrap !important;
  padding: 8rpx 16rpx;
}

.van-tab {
  flex: 1 1 auto !important;
  min-width: 100rpx !important;
  padding: 16rpx 20rpx !important;
  margin: 0 6rpx !important;
  border-radius: 12rpx !important;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1) !important;
  position: relative !important;
  background: transparent !important;
}

.van-tab--active {
  background: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%) !important;
  color: #ffffff !important;
  box-shadow: 0 4rpx 12rpx rgba(6, 182, 212, 0.3) !important;
  transform: translateY(-2rpx) !important;
}

.van-tab__text {
  font-size: 26rpx !important;
  white-space: nowrap !important;
  overflow: hidden !important;
  text-overflow: ellipsis !important;
  font-weight: 600 !important;
  color: inherit !important;
}

.van-tab:not(.van-tab--active):hover {
  background: rgba(6, 182, 212, 0.1) !important;
  color: #0891b2 !important;
}

/* 便当式分组样式增强 */
.van-cell-group {
  margin: 20rpx 0 !important;
  border-radius: 16rpx !important;
  overflow: hidden !important;
  box-shadow: 0 2rpx 12rpx rgba(148, 163, 184, 0.08) !important;
  border: 1px solid rgba(226, 232, 240, 0.6) !important;
  background: #ffffff !important;
  position: relative !important;
  animation: slideInUp 0.5s cubic-bezier(0.4, 0, 0.2, 1) !important;
}

@keyframes slideInUp {
  0% {
    opacity: 0;
    transform: translateY(20rpx);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

.van-cell-group::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(90deg, #06b6d4 0%, #0891b2 50%, #0e7490 100%);
  border-radius: 16rpx 16rpx 0 0;
}

.van-cell-group__title {
  font-weight: 700 !important;
  font-size: 32rpx !important;
  color: #1e293b !important;
  background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%) !important;
  padding: 24rpx 32rpx 16rpx 32rpx !important;
  border-radius: 16rpx 16rpx 0 0 !important;
  border-left: 6rpx solid #667eea !important;
  margin-top: 32rpx !important;
  position: relative !important;
  box-shadow: 0 2rpx 8rpx rgba(148, 163, 184, 0.1) !important;
}

.van-cell-group__title::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 32rpx;
  right: 32rpx;
  height: 2rpx;
  background: linear-gradient(90deg, #667eea 0%, transparent 100%);
}

/* 特色分组标题颜色方案 - 紫色系变化 */
.van-cell-group[title*="距离"] .van-cell-group__title {
  border-left-color: #667eea !important;
  background: linear-gradient(135deg, #f0f4ff 0%, #e0e7ff 100%) !important;
  color: #5b21b6 !important;
}

.van-cell-group[title*="重量"] .van-cell-group__title {
  border-left-color: #764ba2 !important;
  background: linear-gradient(135deg, #f0f4ff 0%, #e0e7ff 100%) !important;
  color: #6b21a8 !important;
}

.van-cell-group[title*="体积"] .van-cell-group__title {
  border-left-color: #8b5cf6 !important;
  background: linear-gradient(135deg, #f0f4ff 0%, #e0e7ff 100%) !important;
  color: #7c3aed !important;
}

.van-cell-group[title*="温度"] .van-cell-group__title {
  border-left-color: #a855f7 !important;
  background: linear-gradient(135deg, #f0f4ff 0%, #e0e7ff 100%) !important;
  color: #9333ea !important;
}

.van-cell-group[title*="压力"] .van-cell-group__title {
  border-left-color: #c084fc !important;
  background: linear-gradient(135deg, #f0f4ff 0%, #e0e7ff 100%) !important;
  color: #a855f7 !important;
}

/* 输入框优化 */
.van-field__input {
  font-size: 32rpx !important;
  color: #1e293b !important;
  font-weight: 500 !important;
  padding: 8rpx 0 !important;
}

.van-field__label {
  font-weight: 600 !important;
  color: #475569 !important;
  font-size: 30rpx !important;
  min-width: 160rpx !important;
}

.van-field__right-icon {
  color: #64748b !important;
  font-size: 28rpx !important;
  font-weight: 500 !important;
  background: rgba(226, 232, 240, 0.3) !important;
  padding: 6rpx 12rpx !important;
  border-radius: 8rpx !important;
}

/* 输入框聚焦效果 */
.van-field--focus {
  background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%) !important;
  border-bottom-color: #667eea !important;
  transform: translateY(-1rpx) !important;
  box-shadow: 0 2rpx 8rpx rgba(102, 126, 234, 0.1) !important;
}

/* Switch开关样式优化 */
.van-cell .van-switch {
  margin-right: 0 !important;
}

.van-cell__title {
  font-weight: 500 !important;
  color: #374151 !important;
}

/* 按钮容器样式 - 与特殊计算页面完全一致 */
.button-cell .van-button {
  width: 100% !important;
  max-width: none !important;
  margin: 0 auto !important;
  
  /* 垂直居中修复 */
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  line-height: 1 !important;
  
  /* 高端UX风格 - 青色主题（与顶部菜单一致） */
  height: 50px !important;
  border-radius: 25px !important;
  font-size: 16px !important;
  font-weight: 600 !important;
  border: none !important;
  background: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%) !important;
  color: #fff !important;
  box-shadow: 0 6px 20px rgba(6, 182, 212, 0.3) !important;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1) !important;
  position: relative !important;
  overflow: hidden !important;
  min-width: 140px !important;
}

/* 悬停和活动状态 */
.button-cell .van-button:hover {
  transform: translateY(-2px) !important;
  box-shadow: 0 8px 25px rgba(6, 182, 212, 0.4) !important;
}

.button-cell .van-button:active {
  transform: translateY(0) scale(0.98) !important;
  box-shadow: 0 4px 15px rgba(6, 182, 212, 0.4) !important;
}

/* 涟漪效果 */
.button-cell .van-button::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  background: rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  transition: all 0.6s ease;
  pointer-events: none;
}

.button-cell .van-button:active::before {
  width: 300px;
  height: 300px;
}

/* 确保按钮文字垂直居中 */
.button-cell .van-button__text {
  display: inline-flex !important;
  align-items: center !important;
  line-height: 1.2 !important;
  vertical-align: middle !important;
  text-align: center !important;
}

/* 块级按钮特殊处理 */
.button-cell .van-button--block {
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  width: 100% !important;
}

/* 不同尺寸按钮统一处理 */
.button-cell .van-button--large,
.button-cell .van-button--normal,
.button-cell .van-button--small,
.button-cell .van-button--mini {
  height: 50px !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  line-height: 1 !important;
}

/* 距离模块特色主题 */
.van-tab[data-name="distance"] .van-cell-group::before {
  background: linear-gradient(90deg, #06b6d4 0%, #0891b2 50%, #0e7490 100%);
}

/* QFE模块特色主题 */
.van-tab[data-name="qfe"] .van-cell-group::before {
  background: linear-gradient(90deg, #8b5cf6 0%, #7c3aed 50%, #6d28d9 100%);
}

/* 速度模块特色主题 */
.van-tab[data-name="speed"] .van-cell-group::before {
  background: linear-gradient(90deg, #10b981 0%, #059669 50%, #047857 100%);
}

/* ISA模块特色主题 */
.van-tab[data-name="isa"] .van-cell-group::before {
  background: linear-gradient(90deg, #f59e0b 0%, #d97706 50%, #b45309 100%);
}

/* 温度模块特色主题 */
.van-tab[data-name="temperature"] .van-cell-group::before {
  background: linear-gradient(90deg, #ef4444 0%, #dc2626 50%, #b91c1c 100%);
}

/* 重量模块特色主题 */
.van-tab[data-name="weight"] .van-cell-group::before {
  background: linear-gradient(90deg, #6366f1 0%, #4f46e5 50%, #4338ca 100%);
}

/* 侧风计算罗盘优化 */
.wind-compass {
  margin-top: 40rpx;
  text-align: center;
  pointer-events: none;
  background: #ffffff;
  border-radius: 16rpx;
  padding: 32rpx;
  box-shadow: 0 4rpx 20rpx rgba(148, 163, 184, 0.1);
  border: 1px solid rgba(226, 232, 240, 0.6);
  animation: fadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes fadeIn {
  0% { opacity: 0; }
  100% { opacity: 1; }
}

.compass-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1e293b;
  margin-bottom: 24rpx;
  position: relative;
}

.compass-title::before {
  content: '🧭';
  margin-right: 12rpx;
  font-size: 30rpx;
}

.compass-container {
  display: flex;
  justify-content: center;
  margin: 30rpx 0;
  pointer-events: none;
}

.compass-circle {
  position: relative;
  width: 320rpx;
  height: 320rpx;
  border: 4rpx solid #06b6d4;
  border-radius: 50%;
  background: linear-gradient(135deg, #f8fafc 0%, #f0f9ff 100%);
  box-shadow: 0 8rpx 24rpx rgba(6, 182, 212, 0.2);
  pointer-events: none;
}

/* 度数刻度优化 */
.compass-degrees {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
}

.degree-mark {
  position: absolute;
  font-size: 24rpx;
  font-weight: 700;
  color: #0891b2;
  background: linear-gradient(135deg, #ffffff 0%, #f0f9ff 100%);
  padding: 6rpx 12rpx;
  border-radius: 12rpx;
  border: 2rpx solid #06b6d4;
  box-shadow: 0 2rpx 8rpx rgba(6, 182, 212, 0.2);
}

.degree-mark.north {
  top: -12rpx;
  left: 50%;
  transform: translateX(-50%);
}

.degree-mark.east {
  right: -24rpx;
  top: 50%;
  transform: translateY(-50%);
}

.degree-mark.south {
  bottom: -12rpx;
  left: 50%;
  transform: translateX(-50%);
}

.degree-mark.west {
  left: -24rpx;
  top: 50%;
  transform: translateY(-50%);
}

/* 指针系统优化 */
.heading-indicator {
  position: absolute;
  top: 50%;
  left: 50%;
  transform-origin: center bottom;
  transform: translate(-50%, -100%);
  z-index: 5;
  pointer-events: none;
}

.heading-line {
  width: 6rpx;
  height: 120rpx;
  background: linear-gradient(180deg, #1e293b 0%, #475569 100%);
  margin: 0 auto;
  border-radius: 3rpx;
  box-shadow: 0 2rpx 8rpx rgba(30, 41, 59, 0.3);
}

.wind-arrow {
  position: absolute;
  top: 50%;
  left: 50%;
  transform-origin: center bottom;
  transform: translate(-50%, -100%);
  pointer-events: none;
}

.arrow-line {
  width: 6rpx;
  height: 120rpx;
  background: linear-gradient(180deg, #ef4444 0%, #dc2626 100%);
  margin: 0 auto;
  border-radius: 3rpx;
  box-shadow: 0 2rpx 8rpx rgba(239, 68, 68, 0.3);
}

.track-indicator {
  position: absolute;
  top: 50%;
  left: 50%;
  transform-origin: center bottom;
  transform: translate(-50%, -100%);
  z-index: 3;
  pointer-events: none;
}

.track-line {
  width: 6rpx;
  height: 120rpx;
  background: linear-gradient(180deg, #10b981 0%, #059669 100%);
  margin: 0 auto;
  border-radius: 3rpx;
  box-shadow: 0 2rpx 8rpx rgba(16, 185, 129, 0.3);
}

/* 图例系统优化 */
.compass-legend {
  display: flex;
  justify-content: center;
  gap: 32rpx;
  margin-top: 24rpx;
  padding: 20rpx;
  background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
  border-radius: 12rpx;
  border: 1px solid #e2e8f0;
  pointer-events: none;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.legend-color {
  width: 24rpx;
  height: 6rpx;
  border-radius: 3rpx;
  box-shadow: 0 1rpx 4rpx rgba(0, 0, 0, 0.1);
}

.heading-color {
  background: linear-gradient(90deg, #1e293b 0%, #475569 100%);
}

.wind-color {
  background: linear-gradient(90deg, #ef4444 0%, #dc2626 100%);
}

.track-color {
  background: linear-gradient(90deg, #10b981 0%, #059669 100%);
}

.legend-text {
  font-size: 24rpx;
  color: #475569;
  font-weight: 600;
}

/* 按钮组优化 */
.button-group {
  padding: 24rpx 0;
}

/* 响应式设计 */
@media (max-width: 750rpx) {
  .van-tab {
    min-width: 80rpx !important;
    padding: 12rpx 16rpx !important;
  }
  
  .van-tab__text {
    font-size: 24rpx !important;
  }
  
  .compass-circle {
    width: 280rpx;
    height: 280rpx;
  }
  
  .compass-legend {
    gap: 20rpx;
  }
}

/* 深色模式兼容 */
@media (prefers-color-scheme: dark) {
  .container {
    background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
  }
  
  .van-cell-group {
    background: #374151 !important;
    border-color: #4b5563 !important;
  }
  
  .van-cell-group__title {
    background: linear-gradient(135deg, #374151 0%, #4b5563 100%) !important;
    color: #f1f5f9 !important;
  }
}

/* 无障碍访问 - 限制作用域避免冲突 */
.button-cell .van-button:focus {
  outline: 3rpx solid #06b6d4;
  outline-offset: 2rpx;
}

.van-field:focus-within {
  outline: 2rpx solid #06b6d4;
  outline-offset: 1rpx;
}

/* 加载动画 */
.van-loading {
  color: #06b6d4 !important;
}

/* 错误状态 */
.van-field--error {
  border-left: 4rpx solid #ef4444 !important;
  background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%) !important;
  animation: shake 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-4rpx); }
  75% { transform: translateX(4rpx); }
}

/* 成功状态 */
.success-cell {
  background: linear-gradient(135deg, #ecfdf5 0%, #d1fae5 100%) !important;
  border-left: 4rpx solid #10b981 !important;
  animation: successPulse 1s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes successPulse {
  0% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.4); }
  70% { box-shadow: 0 0 0 10rpx rgba(16, 185, 129, 0); }
  100% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0); }
}

/* 如果上述:has选择器不兼容，使用类名方式 */
.button-cell {
  padding: 16rpx 32rpx !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
}

/* 悬停和活动状态 - 与特殊计算页面完全一致 */
.button-cell .van-button:hover {
  transform: translateY(-2px) !important;
  box-shadow: 0 8px 25px rgba(6, 182, 212, 0.4) !important;
}

.button-cell .van-button:active {
  transform: translateY(0) scale(0.98) !important;
  box-shadow: 0 4px 15px rgba(6, 182, 212, 0.4) !important;
}

/* 按钮文字垂直居中 - 与特殊计算页面完全一致 */
.button-cell .van-button__text {
  display: inline-flex !important;
  align-items: center !important;
  line-height: 1.2 !important;
  vertical-align: middle !important;
  text-align: center !important;
}

/* 确保不同尺寸的按钮都正确居中 - 与特殊计算页面完全一致 */
.button-cell .van-button--large,
.button-cell .van-button--normal,
.button-cell .van-button--small,
.button-cell .van-button--mini,
.button-cell .van-button--block {
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
}

/* 涟漪效果 - 与特殊计算页面完全一致 */
.button-cell .van-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(255, 255, 255, 0.1);
  transform: scale(0);
  border-radius: inherit;
  transition: transform 0.3s ease;
}

.button-cell .van-button:active::before {
  transform: scale(1);
}

 