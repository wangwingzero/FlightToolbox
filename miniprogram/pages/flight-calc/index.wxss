/* 页面容器样式 */
.container {
  padding: 0 16rpx !important;
  margin: 0 !important;
  background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
  min-height: 100vh;
  width: 100% !important;
  box-sizing: border-box !important;
  display: block !important;
  animation: fadeInUp 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes fadeInUp {
  0% {
    opacity: 0;
    transform: translateY(30rpx);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 标签页系统优化 */
.van-tabs {
  background: #ffffff;
  border-radius: 16rpx 16rpx 0 0;
  box-shadow: 0 4rpx 20rpx rgba(148, 163, 184, 0.1);
  margin: 16rpx 0 0 0;
  overflow: hidden;
}

.van-tabs__wrap {
  overflow-x: auto !important;
  background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
  border-bottom: 1px solid #e2e8f0;
}

.van-tabs__nav {
  display: flex !important;
  flex-wrap: nowrap !important;
  padding: 8rpx 16rpx;
}

.van-tab {
  flex: 1 1 auto !important;
  min-width: 120rpx !important;
  padding: 16rpx 20rpx !important;
  margin: 0 8rpx !important;
  border-radius: 12rpx !important;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1) !important;
  position: relative !important;
  background: transparent !important;
  color: #64748b !important;
}

.van-tab--active {
  background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%) !important;
  color: #ffffff !important;
  box-shadow: 0 4rpx 12rpx rgba(245, 158, 11, 0.3) !important;
  transform: translateY(-2rpx) !important;
}

.van-tab__text {
  font-size: 28rpx !important;
  white-space: nowrap !important;
  overflow: hidden !important;
  text-overflow: ellipsis !important;
  font-weight: 600 !important;
  color: inherit !important;
}

.van-tab:not(.van-tab--active):hover {
  background: rgba(245, 158, 11, 0.1) !important;
  color: #f59e0b !important;
}

/* 强制覆盖标签页文字颜色 */
.container .van-tabs .van-tab {
  color: #64748b !important;
}

.container .van-tabs .van-tab--active {
  color: #ffffff !important;
}

.container .van-tabs .van-tab__text {
  color: inherit !important;
  font-size: 28rpx !important;
  font-weight: 600 !important;
}

.container .van-tabs .van-tab:not(.van-tab--active) .van-tab__text {
  color: #64748b !important;
}

.container .van-tabs .van-tab--active .van-tab__text {
  color: #ffffff !important;
}

/* 额外的强制样式 */
page .container .van-tabs .van-tab {
  color: #64748b !important;
}

page .container .van-tabs .van-tab--active {
  color: #ffffff !important;
  background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%) !important;
}

page .container .van-tabs .van-tab__text {
  color: inherit !important;
}

/* 模式分组标题样式 */
.van-cell-group__title {
  font-weight: 700 !important;
  font-size: 32rpx !important;
  color: #1e293b !important;
  background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%) !important;
  padding: 24rpx 32rpx 16rpx 32rpx !important;
  border-radius: 16rpx 16rpx 0 0 !important;
  border-left: 6rpx solid #f59e0b !important;
  margin-top: 32rpx !important;
  position: relative !important;
  box-shadow: 0 2rpx 8rpx rgba(148, 163, 184, 0.1) !important;
}

.van-cell-group__title::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 32rpx;
  right: 32rpx;
  height: 2rpx;
  background: linear-gradient(90deg, #f59e0b 0%, transparent 100%);
}

/* 特色分组标题颜色方案 - 橙色系变化 */
.van-cell-group[title*="下滑"] .van-cell-group__title {
  border-left: 6rpx solid #10b981 !important;
  background: linear-gradient(135deg, #ecfdf5 0%, #d1fae5 100%) !important;
  color: #047857 !important;
}

.van-cell-group[title*="侧风"] .van-cell-group__title {
  border-left: 6rpx solid #ef4444 !important;
  background: linear-gradient(135deg, #fef2f2 0%, #fecaca 100%) !important;
  color: #991b1b !important;
}

.van-cell-group[title*="转弯"] .van-cell-group__title {
  border-left: 6rpx solid #f59e0b !important;
  background: linear-gradient(135deg, #fffbeb 0%, #fef3c7 100%) !important;
  color: #92400e !important;
}

.van-cell-group[title*="绕飞"] .van-cell-group__title {
  border-left: 6rpx solid #8b5cf6 !important;
  background: linear-gradient(135deg, #f3e8ff 0%, #e9d5ff 100%) !important;
  color: #6b21a8 !important;
}

.van-cell-group[title*="基础"] .van-cell-group__title {
  border-left: 6rpx solid #f59e0b !important;
  background: linear-gradient(135deg, #fef7cd 0%, #fde047 100%) !important;
  color: #a16207 !important;
}

.van-cell-group[title*="输入"] .van-cell-group__title {
  border-left: 6rpx solid #06b6d4 !important;
  background: linear-gradient(135deg, #ecfeff 0%, #cffafe 100%) !important;
  color: #0e7490 !important;
}

.van-cell-group[title*="计算"] .van-cell-group__title {
  border-left: 6rpx solid #f59e0b !important;
  background: linear-gradient(135deg, #fffbeb 0%, #fef3c7 100%) !important;
  color: #92400e !important;
}

.van-cell-group[title*="结果"] .van-cell-group__title {
  border-left: 6rpx solid #10b981 !important;
  background: linear-gradient(135deg, #ecfdf5 0%, #d1fae5 100%) !important;
  color: #047857 !important;
}

/* 输入框优化 */
.van-field__input {
  font-size: 32rpx !important;
  color: #1e293b !important;
  font-weight: 500 !important;
  padding: 8rpx 0 !important;
}

.van-field__label {
  font-weight: 600 !important;
  color: #475569 !important;
  font-size: 30rpx !important;
  min-width: 160rpx !important;
}

.van-field__right-icon {
  color: #64748b !important;
  font-size: 28rpx !important;
  font-weight: 500 !important;
  background: rgba(226, 232, 240, 0.3) !important;
  padding: 6rpx 12rpx !important;
  border-radius: 8rpx !important;
}

/* 输入框聚焦效果 */
.van-field--focus {
  background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%) !important;
  border-bottom-color: #f59e0b !important;
  transform: translateY(-1rpx) !important;
  box-shadow: 0 2rpx 8rpx rgba(245, 158, 11, 0.1) !important;
}

/* 错误状态 */
.van-field--error {
  border-left: 4rpx solid #ef4444 !important;
  background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%) !important;
  animation: shake 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-4rpx); }
  75% { transform: translateX(4rpx); }
}

/* 成功状态 */
.success-cell {
  background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%) !important;
  border-left: 4rpx solid #f59e0b !important;
  animation: successPulse 1s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes successPulse {
  0% { box-shadow: 0 0 0 0 rgba(245, 158, 11, 0.4); }
  70% { box-shadow: 0 0 0 10rpx rgba(245, 158, 11, 0); }
  100% { box-shadow: 0 0 0 0 rgba(245, 158, 11, 0); }
}

/* 响应式设计增强 */
@media screen and (max-width: 750rpx) {
  .van-tab {
    min-width: 100rpx !important;
    padding: 12rpx 16rpx !important;
  }
  
  .van-tab__text {
    font-size: 26rpx !important;
  }
  
  .van-cell-group__title {
    font-size: 30rpx !important;
    padding: 20rpx 24rpx 12rpx 24rpx !important;
  }
  
  .van-field {
    padding: 16rpx 24rpx !important;
  }
  
  .van-button {
    font-size: 30rpx !important;
    padding: 16rpx 24rpx !important;
  }
}

/* 无障碍访问增强 */
.van-button:focus,
.van-cell--clickable:focus,
.van-field__input:focus {
  outline: 3rpx solid rgba(245, 158, 11, 0.5) !important;
  outline-offset: 2rpx !important;
}

/* 深色模式兼容性 */
@media (prefers-color-scheme: dark) {
  .container {
    background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
  }
  
  .van-cell-group {
    background: #1e293b !important;
    border-color: #334155 !important;
  }
  
  .van-cell {
    background: #1e293b !important;
    border-bottom-color: #334155 !important;
  }
  
  .van-cell__title {
    color: #f1f5f9 !important;
  }
  
  .van-field__input {
    color: #f1f5f9 !important;
  }
}

/* 加载动画优化 */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.van-cell-group {
  animation: fadeInUp 0.6s ease-out !important;
  animation-fill-mode: both !important;
}

.van-cell-group:nth-child(1) { animation-delay: 0.1s !important; }
.van-cell-group:nth-child(2) { animation-delay: 0.2s !important; }
.van-cell-group:nth-child(3) { animation-delay: 0.3s !important; }
.van-cell-group:nth-child(4) { animation-delay: 0.4s !important; }
.van-cell-group:nth-child(5) { animation-delay: 0.5s !important; }

/* 按钮居中样式修复 */
.van-cell:has(.van-button) {
  padding: 16rpx 32rpx !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
}

/* 确保按钮在van-cell中居中 */
.van-cell .van-button {
  width: 100% !important;
  max-width: none !important;
  margin: 0 auto !important;
  display: block !important;
}

/* 增强按钮容器的居中效果 */
.van-cell__value:has(.van-button) {
  display: flex !important;
  justify-content: center !important;
  align-items: center !important;
  width: 100% !important;
}

/* 如果上述:has选择器不兼容，使用类名方式 */
.button-cell {
  padding: 16rpx 32rpx !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
}

.button-cell .van-button {
  width: 100% !important;
  max-width: none !important;
  margin: 0 auto !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  line-height: 1 !important;
}

/* 基于Context7最佳实践的按钮文字垂直居中修复 */
.van-button {
  /* 垂直居中修复 */
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  line-height: 1 !important;
  
  /* 高端UX风格 - 橙色主题 */
  height: 50px !important;
  border-radius: 25px !important;
  font-size: 16px !important;
  font-weight: 600 !important;
  border: none !important;
  background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%) !important;
  color: #fff !important;
  box-shadow: 0 6px 20px rgba(245, 158, 11, 0.3) !important;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1) !important;
  position: relative !important;
  overflow: hidden !important;
  min-width: 140px !important;
}

/* 悬停和活动状态 */
.van-button:hover {
  transform: translateY(-2px) !important;
  box-shadow: 0 8px 25px rgba(245, 158, 11, 0.4) !important;
}

.van-button:active {
  transform: translateY(0) scale(0.98) !important;
  box-shadow: 0 4px 15px rgba(245, 158, 11, 0.4) !important;
}

/* 按钮文字垂直居中 */
.van-button__text {
  display: inline-flex !important;
  align-items: center !important;
  line-height: 1.2 !important;
  vertical-align: middle !important;
  text-align: center !important;
}

/* 确保不同尺寸的按钮都正确居中 */
.van-button--large,
.van-button--normal,
.van-button--small,
.van-button--mini,
.van-button--block {
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
}

/* 涟漪效果 */
.van-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(255, 255, 255, 0.1);
  transform: scale(0);
  border-radius: inherit;
  transition: transform 0.3s ease;
}

.van-button:active::before {
  transform: scale(1);
}

/* 特殊计算页面按钮区域优化 */
.button-cell .van-button {
  width: 100% !important;
  max-width: none !important;
  margin: 0 auto !important;
}

/* 罗盘相关样式 - 保持原有功能 */
.compass-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32rpx;
  background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
  border-radius: 20rpx;
  box-shadow: 0 8rpx 32rpx rgba(148, 163, 184, 0.1);
  margin: 24rpx 16rpx;
}

.compass-circle {
  width: 400rpx;
  height: 400rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%);
  border: 4rpx solid #e2e8f0;
  position: relative;
  box-shadow: 
    0 8rpx 32rpx rgba(148, 163, 184, 0.15),
    inset 0 2rpx 8rpx rgba(255, 255, 255, 0.8);
}

.compass-legend {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 24rpx;
  margin-top: 32rpx;
  padding: 0 16rpx;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 12rpx 16rpx;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 12rpx;
  box-shadow: 0 2rpx 8rpx rgba(148, 163, 184, 0.1);
}

.legend-color {
  width: 24rpx;
  height: 24rpx;
  border-radius: 4rpx;
  box-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.1);
}

.heading-color {
  background: linear-gradient(90deg, #1e293b 0%, #475569 100%);
}

.wind-color {
  background: linear-gradient(90deg, #ef4444 0%, #dc2626 100%);
}

.track-color {
  background: linear-gradient(90deg, #10b981 0%, #059669 100%);
}

.legend-text {
  font-size: 24rpx;
  color: #475569;
  font-weight: 600;
}

/* 🎯 基于Context7最佳实践：广告区域样式 - 专业计算器风格（橙色主题） */
.calculator-ad-container {
  margin: 32rpx 16rpx;
  padding: 24rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx) saturate(150%);
  border-radius: 20rpx;
  border: 1rpx solid rgba(245, 158, 11, 0.2);
  box-shadow: 
    0 8rpx 32rpx rgba(245, 158, 11, 0.1),
    0 2rpx 8rpx rgba(245, 158, 11, 0.05),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.8);
  position: relative;
  overflow: hidden;
  animation: calculatorAdSlideIn 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.calculator-ad-container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, 
    rgba(245, 158, 11, 0.02) 0%, 
    rgba(217, 119, 6, 0.02) 100%);
  pointer-events: none;
}

/* 确保广告内容在背景之上 */
.calculator-ad-container > view,
.calculator-ad-container > ad-template {
  position: relative;
  z-index: 1;
}

/* 专业计算器主题的广告动画 */
@keyframes calculatorAdSlideIn {
  0% {
    opacity: 0;
    transform: translateY(40rpx) scale(0.95);
  }
  60% {
    opacity: 0.8;
    transform: translateY(-8rpx) scale(1.02);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* 响应式优化 - 小屏幕设备 */
@media (max-width: 375px) {
  .calculator-ad-container {
    margin: 24rpx 12rpx;
    padding: 20rpx;
    border-radius: 16rpx;
  }
}

/* 深色模式适配 */
@media (prefers-color-scheme: dark) {
  .calculator-ad-container {
    background: rgba(30, 41, 59, 0.95);
    border-color: rgba(245, 158, 11, 0.3);
    box-shadow: 
      0 8rpx 32rpx rgba(0, 0, 0, 0.3),
      0 2rpx 8rpx rgba(0, 0, 0, 0.2),
      inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
  }
  
  .calculator-ad-container::before {
    background: linear-gradient(135deg, 
      rgba(245, 158, 11, 0.05) 0%, 
      rgba(217, 119, 6, 0.05) 100%);
  }
}

/* 兼容旧的ad-section类名 */
.ad-section {
  margin: 32rpx 16rpx;
  padding: 24rpx;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(20rpx) saturate(150%);
  border-radius: 20rpx;
  border: 1rpx solid rgba(245, 158, 11, 0.2);
  box-shadow: 
    0 8rpx 32rpx rgba(245, 158, 11, 0.1),
    0 2rpx 8rpx rgba(245, 158, 11, 0.05),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.8);
  position: relative;
  overflow: hidden;
  animation: calculatorAdSlideIn 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.ad-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, 
    rgba(245, 158, 11, 0.02) 0%, 
    rgba(217, 119, 6, 0.02) 100%);
  pointer-events: none;
}

/* 确保广告内容在背景之上 */
.ad-section > view,
.ad-section > ad-template {
  position: relative;
  z-index: 1;
}

 