<view class="container page-content">
  <!-- 页面标题 -->
  <view class="page-header">
    <view class="header-title">⚡ 性能参数</view>
    <view class="header-subtitle">飞机性能计算与参数查询</view>
  </view>

  <!-- 功能卡片网格 -->
  <view class="modules-grid">
    <view 
      wx:for="{{ modules }}" 
      wx:key="id"
      class="module-card" 
      bind:tap="selectModule" 
      data-module="{{ item.id }}"
    >
      <view class="module-icon">{{ item.icon }}</view>
      <view class="module-title">{{ item.title }}</view>
      <view class="module-desc">{{ item.desc }}</view>
      <!-- 积分标签（右上角） -->
      <view class="module-points" wx:if="{{ item.pointsText }}">
        <van-tag type="{{ item.pointsType }}" size="mini">{{ item.pointsText }}</van-tag>
      </view>
      <!-- 数据量标签（底部） -->
      <view class="module-count">
        <van-tag type="{{ item.countType }}" size="medium">{{ item.count }}</van-tag>
      </view>
    </view>
  </view>

  <!-- 功能说明 -->
  <view class="tips-section">
    <view class="tips-title">💡 使用说明</view>
    <view class="tips-content">
      <view class="tip-item">• 🛩️ 飞机参数：查询各型飞机技术参数数据</view>
      <view class="tip-item">• 📊 性能详解：包含性能相关术语定义与解释</view>
      <view class="tip-item">• ✈️ 双发复飞：计算双发飞机复飞性能</view>
      <view class="tip-item">• 🔄 ACR-PCR：飞机道面承载能力对比</view>
      <view class="tip-item">• 📱 积分不足时可通过签到获取</view>
      <view class="tip-item">• 🛫 支持离线使用，数据本地存储</view>
    </view>
  </view>

</view>

<van-toast id="van-toast" />