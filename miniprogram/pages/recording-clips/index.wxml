<view class="container">
  <!-- 录音卡片网格 -->
  <view class="clips-grid">
    <view class="grid-header">
      <view class="header-title">{{ regionFlag }} {{ regionName }} - {{ categoryName }}</view>
      <view class="header-count">{{ allClips.length }}个录音</view>
    </view>
    
    
    <!-- 录音卡片网格 -->
    <van-row gutter="24">
      <van-col span="12" wx:for="{{ allClips }}" wx:key="index">
        <view class="audio-card" bind:tap="selectClip" data-index="{{ index }}">
          <view class="card-header">
            <view class="card-number">{{ index + 1 }}</view>
            <view class="learned-status" bind:tap="toggleLearnedStatus" data-index="{{ index }}">
              {{ item.isLearned ? '✅' : '❌' }}
            </view>
          </view>
          
          <view class="card-content">
            <view class="card-label">{{ item.label }}</view>
            <view class="card-title">{{ item.isLearned ? item.full_transcript : '???' }}</view>
            <view class="card-subtitle">{{ item.isLearned ? item.translation_cn : '请先学习这段录音' }}</view>
          </view>
          
          <view class="card-footer">
            <view class="play-icon">🎧</view>
            <view class="play-text">点击播放</view>
          </view>
        </view>
      </van-col>
    </van-row>
  </view>

</view>