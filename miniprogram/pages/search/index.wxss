/* 资料查询页面样式 - iOS 风格精致设计 */

.page-content {
  padding: 0;
  min-height: 100vh;
  /* iOS 系统灰色背景 */
  background: #F2F2F7;
}

/* 🚀 新增：搜索框样式 */
.search-container {
  padding: 0 20rpx 20rpx 20rpx;
  margin-top: -10rpx;
}

.search-box {
  display: flex;
  align-items: center;
  background: white;
  border-radius: 50rpx;
  padding: 20rpx 28rpx;
  box-shadow:
    0 4rpx 16rpx rgba(0, 0, 0, 0.08),
    0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  transition: all 0.3s ease;
  position: relative;
}

.search-box:focus-within {
  box-shadow:
    0 6rpx 20rpx rgba(102, 126, 234, 0.15),
    0 2rpx 10rpx rgba(102, 126, 234, 0.1),
    0 0 0 2rpx rgba(102, 126, 234, 0.2);
}

.search-icon {
  font-size: 32rpx;
  margin-right: 16rpx;
  color: #9ca3af;
}

.search-input {
  flex: 1;
  font-size: 28rpx;
  color: #1e293b;
  height: 44rpx;
  line-height: 44rpx;
}

.search-input::placeholder {
  color: #9ca3af;
}

.search-clear {
  width: 40rpx;
  height: 40rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #e5e7eb;
  border-radius: 50%;
  font-size: 24rpx;
  color: #6b7280;
  margin-left: 16rpx;
  transition: all 0.2s ease;
  cursor: pointer;
}

.search-clear:active {
  background: #d1d5db;
  transform: scale(0.9);
}

/* 资料卡片网格 */
.categories-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24rpx;
  padding: 24rpx;
  background: transparent;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes cardSlideIn {
  0% {
    opacity: 0;
    transform: translateY(30rpx) scale(0.9);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes iconFloat {
  0%, 100% { 
    transform: translateY(0); 
  }
  50% { 
    transform: translateY(-8rpx); 
  }
}

/* ===== 单个资料卡片基础样式 ===== */
.category-card {
  background: #FFFFFF;
  border-radius: 28rpx;
  padding: 32rpx 28rpx;
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 320rpx;
  /* iOS 精致阴影 */
  box-shadow: 
    0 2rpx 16rpx rgba(0, 0, 0, 0.04),
    0 0rpx 2rpx rgba(0, 0, 0, 0.02);
  transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  /* 入场动画 */
  animation: cardSlideIn 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) both;
}

/* 入场动画延迟 */
.category-card:nth-child(1) { animation-delay: 0.05s; }
.category-card:nth-child(2) { animation-delay: 0.1s; }
.category-card:nth-child(3) { animation-delay: 0.15s; }
.category-card:nth-child(4) { animation-delay: 0.2s; }
.category-card:nth-child(5) { animation-delay: 0.25s; }
.category-card:nth-child(6) { animation-delay: 0.3s; }
.category-card:nth-child(7) { animation-delay: 0.35s; }
.category-card:nth-child(8) { animation-delay: 0.4s; }

/* ===== iOS 主题色系统 ===== */

/* Blue - CCAR规章 */
.theme-blue { background: linear-gradient(135deg, #FFFFFF 0%, #F0F5FF 100%); border: 2rpx solid rgba(0, 122, 255, 0.15); }
.theme-blue .category-icon { background: linear-gradient(135deg, #007AFF 0%, #0051D5 100%); box-shadow: 0 8rpx 24rpx rgba(0, 122, 255, 0.25); }

/* Teal - ICAO出版物 */
.theme-teal { background: linear-gradient(135deg, #FFFFFF 0%, #F0FCFF 100%); border: 2rpx solid rgba(90, 200, 250, 0.15); }
.theme-teal .category-icon { background: linear-gradient(135deg, #5AC8FA 0%, #30A7D7 100%); box-shadow: 0 8rpx 24rpx rgba(90, 200, 250, 0.25); }

/* Orange - 缩写 */
.theme-orange { background: linear-gradient(135deg, #FFFFFF 0%, #FFF8F0 100%); border: 2rpx solid rgba(255, 149, 0, 0.15); }
.theme-orange .category-icon { background: linear-gradient(135deg, #FF9500 0%, #FF6D00 100%); box-shadow: 0 8rpx 24rpx rgba(255, 149, 0, 0.25); }

/* Green - 权威定义 */
.theme-green { background: linear-gradient(135deg, #FFFFFF 0%, #F0FFF4 100%); border: 2rpx solid rgba(52, 199, 89, 0.15); }
.theme-green .category-icon { background: linear-gradient(135deg, #34C759 0%, #248A3D 100%); box-shadow: 0 8rpx 24rpx rgba(52, 199, 89, 0.25); }

/* Purple - 机场数据 */
.theme-purple { background: linear-gradient(135deg, #FFFFFF 0%, #FAF5FF 100%); border: 2rpx solid rgba(175, 82, 222, 0.15); }
.theme-purple .category-icon { background: linear-gradient(135deg, #AF52DE 0%, #8E44AD 100%); box-shadow: 0 8rpx 24rpx rgba(175, 82, 222, 0.25); }

/* Indigo - 飞机性能 */
.theme-indigo { background: linear-gradient(135deg, #FFFFFF 0%, #F5F3FF 100%); border: 2rpx solid rgba(88, 86, 214, 0.15); }
.theme-indigo .category-icon { background: linear-gradient(135deg, #5856D6 0%, #4338CA 100%); box-shadow: 0 8rpx 24rpx rgba(88, 86, 214, 0.25); }

/* Red - 事件调查 */
.theme-red { background: linear-gradient(135deg, #FFFFFF 0%, #FFF5F5 100%); border: 2rpx solid rgba(255, 59, 48, 0.15); }
.theme-red .category-icon { background: linear-gradient(135deg, #FF3B30 0%, #DC2626 100%); box-shadow: 0 8rpx 24rpx rgba(255, 59, 48, 0.25); }

/* Pink - 体检标准 */
.theme-pink { background: linear-gradient(135deg, #FFFFFF 0%, #FFF0F5 100%); border: 2rpx solid rgba(255, 45, 85, 0.15); }
.theme-pink .category-icon { background: linear-gradient(135deg, #FF2D55 0%, #E91E63 100%); box-shadow: 0 8rpx 24rpx rgba(255, 45, 85, 0.25); }

/* System Red - 危险品 */
.theme-system-red { background: linear-gradient(135deg, #FFFFFF 0%, #FFF1F0 100%); border: 2rpx solid rgba(255, 69, 58, 0.15); }
.theme-system-red .category-icon { background: linear-gradient(135deg, #FF453A 0%, #FF1744 100%); box-shadow: 0 8rpx 24rpx rgba(255, 69, 58, 0.25); }

/* Yellow - 日出日落 */
.theme-yellow { background: linear-gradient(135deg, #FFFFFF 0%, #FFFBF0 100%); border: 2rpx solid rgba(255, 204, 0, 0.15); }
.theme-yellow .category-icon { background: linear-gradient(135deg, #FFCC00 0%, #FF9500 100%); box-shadow: 0 8rpx 24rpx rgba(255, 204, 0, 0.25); }

/* Cyan - IOSA审计 */
.theme-cyan { background: linear-gradient(135deg, #FFFFFF 0%, #F0FDFF 100%); border: 2rpx solid rgba(50, 173, 230, 0.15); }
.theme-cyan .category-icon { background: linear-gradient(135deg, #32ADE6 0%, #0891B2 100%); box-shadow: 0 8rpx 24rpx rgba(50, 173, 230, 0.25); }

/* Mint - 胜任力 */
.theme-mint { background: linear-gradient(135deg, #FFFFFF 0%, #F0FFF9 100%); border: 2rpx solid rgba(0, 199, 190, 0.15); }
.theme-mint .category-icon { background: linear-gradient(135deg, #00C7BE 0%, #0D9488 100%); box-shadow: 0 8rpx 24rpx rgba(0, 199, 190, 0.25); }

/* Brown - 飞机参数 */
.theme-brown { background: linear-gradient(135deg, #FFFFFF 0%, #F8F5F0 100%); border: 2rpx solid rgba(162, 132, 94, 0.15); }
.theme-brown .category-icon { background: linear-gradient(135deg, #A2845E 0%, #8B6F47 100%); box-shadow: 0 8rpx 24rpx rgba(162, 132, 94, 0.25); }

/* Gray - 绕机检查 */
.theme-gray { background: linear-gradient(135deg, #FFFFFF 0%, #F5F5F7 100%); border: 2rpx solid rgba(142, 142, 147, 0.15); }
.theme-gray .category-icon { background: linear-gradient(135deg, #8E8E93 0%, #636366 100%); box-shadow: 0 8rpx 24rpx rgba(142, 142, 147, 0.25); }

@keyframes cardSlideIn {
  0% {
    opacity: 0;
    transform: translateY(30rpx) scale(0.9);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* 卡片按下效果 */
.category-card:active {
  transform: scale(0.96);
  box-shadow: 
    0 1rpx 8rpx rgba(0, 0, 0, 0.08),
    0 0rpx 1rpx rgba(0, 0, 0, 0.04);
}

/* 卡片图标 */
.category-icon {
  width: 96rpx;
  height: 96rpx;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 56rpx;
  margin-bottom: 20rpx;
  transition: transform 0.3s ease;
}

.category-card:active .category-icon {
  transform: scale(0.92);
}

/* 卡片内容区 */
.category-content {
  width: 100%;
  text-align: center;
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.category-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #1C1C1E;
  margin-bottom: 8rpx;
  letter-spacing: 0.5rpx;
  /* iOS 字体 */
  font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Text', 'SF Pro Display', sans-serif;
}

.category-desc {
  font-size: 22rpx;
  color: #8E8E93;
  line-height: 1.4;
  font-weight: 400;
  padding: 0 8rpx;
}

/* 积分标签 - 位于右上角 */
.category-points {
  position: absolute;
  top: 16rpx;
  right: 16rpx;
  z-index: 2;
}

/* 数据量标签 - 位于底部 */
.category-count {
  display: flex;
  justify-content: center;
  margin-top: auto;
  padding-top: 16rpx;
}

/* 鼓励作者卡片特殊样式 */
.category-card.support {
  border: 2rpx solid rgba(236, 72, 153, 0.3);
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.98) 0%, rgba(253, 242, 248, 0.95) 100%);
  position: relative;
  overflow: hidden;
}

.category-card.support::before {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(236, 72, 153, 0.1) 0%, transparent 70%);
  animation: heartBeat 3s ease-in-out infinite;
  z-index: 0;
}

.category-card.support .category-icon,
.category-card.support .category-title,
.category-card.support .category-desc,
.category-card.support .category-count {
  position: relative;
  z-index: 1;
}

@keyframes heartBeat {
  0%, 100% {
    transform: scale(1);
    opacity: 0.7;
  }
  50% {
    transform: scale(1.1);
    opacity: 0.3;
  }
}

.category-card.support:hover {
  border: 2rpx solid rgba(236, 72, 153, 0.6);
  box-shadow: 
    0 12rpx 48rpx rgba(236, 72, 153, 0.2),
    0 0 0 1rpx rgba(255, 255, 255, 0.2),
    0 0 0 3rpx rgba(236, 72, 153, 0.15);
  transform: translateY(-4rpx);
}

.category-card.support .category-icon {
  animation: heartPulse 2s ease-in-out infinite;
}

@keyframes heartPulse {
  0%, 100% {
    transform: scale(1) translateY(0);
  }
  25% {
    transform: scale(1.1) translateY(-2rpx);
  }
  50% {
    transform: scale(1.05) translateY(-4rpx);
  }
  75% {
    transform: scale(1.1) translateY(-2rpx);
  }
}

/* 激励作者卡片高亮状态 - 更深的红色+小红点 */
.category-card.support.highlight-pulse {
  background: linear-gradient(135deg, #ff3333 0%, #ff0000 100%) !important;
  border: 6rpx solid #cc0000 !important;
  box-shadow:
    0 16rpx 40rpx rgba(255, 0, 0, 0.6),
    0 0 0 4rpx rgba(255, 0, 0, 0.25),
    inset 0 2rpx 10rpx rgba(255, 255, 255, 0.4) !important;
  transform: scale(1.03) !important;
}

.category-card.support.highlight-pulse .category-icon {
  color: #ffffff !important;
  font-size: 88rpx !important;
  filter: drop-shadow(0 3rpx 10rpx rgba(255, 255, 255, 0.9));
}

.category-card.support.highlight-pulse .category-title {
  color: #ffffff !important;
  font-weight: 900 !important;
  font-size: 36rpx !important;
  text-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.4);
}

.category-card.support.highlight-pulse .category-desc {
  color: rgba(255, 255, 255, 0.98) !important;
  font-weight: 700 !important;
}

/* 黄色小红点 - 脉冲动画 */
.category-card.support.highlight-pulse::before {
  content: '';
  position: absolute;
  top: 8rpx;
  left: 8rpx;
  width: 32rpx;
  height: 32rpx;
  background: #ffff00;
  border-radius: 50%;
  border: 6rpx solid #ffffff;
  z-index: 10;
  box-shadow:
    0 0 20rpx rgba(255, 255, 0, 0.9),
    0 6rpx 20rpx rgba(255, 0, 0, 0.7);
  animation: pulse-badge 1.2s ease-in-out infinite;
}

@keyframes pulse-badge {
  0%, 100% {
    transform: scale(1);
    opacity: 1;
  }
  50% {
    transform: scale(1.4);
    opacity: 0.7;
  }
}

/* 删除脉冲动画 */
@keyframes highlightPulse {
  /* 保留动画定义以防其他地方引用,但不使用 */
  0% {
    transform: scale(1);
  }
  100% {
    transform: scale(1);
  }
}

/* 广告计数器样式 */
.ad-counter {
  position: absolute;
  top: 16rpx;
  left: 16rpx;
  background: linear-gradient(135deg, #ec4899 0%, #db2777 100%);
  color: white;
  border-radius: 50%;
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20rpx;
  font-weight: bold;
  box-shadow: 0 4rpx 12rpx rgba(236, 72, 153, 0.3);
  z-index: 2;
}

.counter-number {
  line-height: 1;
}

/* 功能说明区域 */
.tips-section {
  margin: 32rpx;
  padding: 32rpx;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 20rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.05);
  animation: fadeIn 0.8s ease-out 0.4s both;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.tips-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #1e293b;
  margin-bottom: 16rpx;
}

.tips-content {
  font-size: 26rpx;
  color: #64748b;
  line-height: 1.8;
}

.tip-item {
  margin: 8rpx 0;
}

/* ===== 响应式设计 ===== */
@media (max-width: 600rpx) {
  .categories-grid {
    grid-template-columns: 1fr;
    gap: 20rpx;
    padding: 20rpx;
  }
  
  .category-card {
    padding: 28rpx 24rpx;
    min-height: 280rpx;
  }
  
  .category-icon {
    width: 80rpx;
    height: 80rpx;
    font-size: 48rpx;
  }
  
  .category-title {
    font-size: 28rpx;
  }

  .category-desc {
    font-size: 20rpx;
  }
}

@media (min-width: 1200px) {
  .categories-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* ===== 支持作者卡片 - iOS 17 风格美化 ===== */

/* 卡片入场动画 */
@keyframes supportCardIn {
  0% {
    opacity: 0;
    transform: translateY(20rpx) scale(0.96);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* 柔和呼吸动画 */
@keyframes gentlePulse {
  0%, 100% {
    box-shadow:
      0 16rpx 48rpx -8rpx rgba(244, 63, 94, 0.35),
      0 8rpx 24rpx -4rpx rgba(225, 29, 72, 0.25),
      inset 0 1rpx 2rpx rgba(255, 255, 255, 0.4);
  }
  50% {
    box-shadow:
      0 20rpx 56rpx -8rpx rgba(244, 63, 94, 0.45),
      0 12rpx 32rpx -4rpx rgba(225, 29, 72, 0.35),
      inset 0 1rpx 2rpx rgba(255, 255, 255, 0.4);
  }
}

/* 角标脉冲动画 */
@keyframes badgePulse {
  0%, 100% {
    transform: scale(1);
    box-shadow:
      0 4rpx 16rpx rgba(250, 204, 21, 0.4),
      0 0 0 0 rgba(250, 204, 21, 0.4);
  }
  50% {
    transform: scale(1.02);
    box-shadow:
      0 6rpx 20rpx rgba(250, 204, 21, 0.5),
      0 0 0 8rpx rgba(250, 204, 21, 0);
  }
}

/* 按钮微光动画 */
@keyframes buttonShine {
  0% {
    transform: translateX(-100%) skewX(-15deg);
  }
  100% {
    transform: translateX(200%) skewX(-15deg);
  }
}

/* 加载点动画 */
@keyframes loadingBounce {
  0%, 80%, 100% {
    transform: scale(0.6);
    opacity: 0.4;
  }
  40% {
    transform: scale(1);
    opacity: 1;
  }
}

.support-author-card {
  margin: 24rpx 24rpx 16rpx;
  background: linear-gradient(145deg, #ff6b8a 0%, #f43f5e 40%, #e11d48 100%);
  border-radius: 28rpx;
  overflow: hidden;
  position: relative;
  animation: supportCardIn 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) both,
             gentlePulse 4s ease-in-out 0.5s infinite;
}

/* 背景光晕装饰 */
.support-author-card::before {
  content: '';
  position: absolute;
  top: -80rpx;
  right: -80rpx;
  width: 280rpx;
  height: 280rpx;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.2) 0%, transparent 60%);
  border-radius: 50%;
  pointer-events: none;
}

.support-author-card::after {
  content: '';
  position: absolute;
  bottom: -60rpx;
  left: -60rpx;
  width: 220rpx;
  height: 220rpx;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.12) 0%, transparent 60%);
  border-radius: 50%;
  pointer-events: none;
}

/* 已免广告状态 - 绿色主题 */
.support-author-card.is-ad-free {
  background: linear-gradient(145deg, #34d399 0%, #10b981 40%, #059669 100%);
  animation: supportCardIn 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) both;
}

.support-content {
  padding: 0;
  display: flex;
  flex-direction: column;
  position: relative;
  z-index: 1;
}

/* ===== 角标样式 ===== */
.ad-free-badge {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  padding: 16rpx 28rpx;
  background: linear-gradient(135deg, rgba(254, 243, 199, 0.98) 0%, rgba(253, 230, 138, 0.98) 100%);
  display: flex;
  align-items: center;
  gap: 12rpx;
  border-bottom: 1rpx solid rgba(217, 119, 6, 0.15);
  animation: badgePulse 3s ease-in-out infinite;
}

.badge-icon {
  font-size: 32rpx;
  line-height: 1;
}

.badge-text {
  display: flex;
  align-items: baseline;
  gap: 12rpx;
}

.badge-main {
  font-size: 26rpx;
  font-weight: 800;
  color: #92400e;
  letter-spacing: 0.5rpx;
}

.badge-sub {
  font-size: 22rpx;
  font-weight: 600;
  color: #b45309;
}

/* ===== 主体内容区 ===== */
.support-main {
  padding: 100rpx 28rpx 24rpx;
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.support-icon-wrap {
  flex-shrink: 0;
}

.support-icon {
  font-size: 52rpx;
  line-height: 1;
  width: 80rpx;
  height: 80rpx;
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.25);
  backdrop-filter: blur(8rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.35);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
}

.support-info {
  flex: 1;
  min-width: 0;
  color: white;
}

.support-title {
  font-size: 30rpx;
  font-weight: 700;
  margin-bottom: 6rpx;
  text-shadow: 0 1rpx 3rpx rgba(0, 0, 0, 0.15);
  letter-spacing: 0.5rpx;
  line-height: 1.3;
}

.support-desc {
  font-size: 24rpx;
  opacity: 0.92;
  line-height: 1.4;
  font-weight: 500;
}

/* ===== 底部操作区 ===== */
.support-footer {
  padding: 0 28rpx 24rpx;
}

/* 观看广告按钮 - 渐变 + 微光 */
.watch-ad-button {
  width: 100% !important;
  background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
  color: #e11d48;
  border: none;
  padding: 24rpx 0;
  border-radius: 16rpx;
  font-size: 28rpx;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  box-shadow:
    0 8rpx 24rpx rgba(0, 0, 0, 0.12),
    0 2rpx 8rpx rgba(0, 0, 0, 0.08),
    inset 0 1rpx 0 rgba(255, 255, 255, 1);
  transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.watch-ad-button::after {
  border: none !important;
}

/* 按钮微光效果 */
.button-glow {
  position: absolute;
  top: 0;
  left: 0;
  width: 60rpx;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.6), transparent);
  animation: buttonShine 3s ease-in-out infinite;
}

.watch-ad-button:active {
  transform: scale(0.97);
  box-shadow:
    0 4rpx 12rpx rgba(0, 0, 0, 0.08),
    0 1rpx 4rpx rgba(0, 0, 0, 0.04);
  background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
}

.button-icon {
  font-size: 24rpx;
  line-height: 1;
  color: #e11d48;
  background: linear-gradient(135deg, #fecdd3 0%, #fda4af 100%);
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  padding-left: 4rpx;
}

.button-text {
  line-height: 1;
  font-size: 28rpx;
  font-weight: 700;
  letter-spacing: 0.5rpx;
}

/* ===== 加载提示 ===== */
.loading-hint {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  padding: 24rpx 0;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 16rpx;
  backdrop-filter: blur(8rpx);
}

.loading-dot {
  width: 12rpx;
  height: 12rpx;
  background: white;
  border-radius: 50%;
  animation: loadingBounce 1.4s ease-in-out infinite;
}

.loading-dot:nth-child(1) { animation-delay: 0s; }
.loading-dot:nth-child(2) { animation-delay: 0.16s; }
.loading-dot:nth-child(3) { animation-delay: 0.32s; }

.loading-text {
  color: white;
  font-size: 24rpx;
  font-weight: 600;
  margin-left: 8rpx;
  opacity: 0.95;
}

/* ===== 感谢状态样式 ===== */
.thank-you {
  flex-direction: row !important;
  padding: 28rpx 28rpx !important;
  align-items: center;
  gap: 20rpx;
}

.thank-you-icon {
  font-size: 56rpx;
  line-height: 1;
  width: 80rpx;
  height: 80rpx;
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.25);
  backdrop-filter: blur(8rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.4);
  flex-shrink: 0;
}

.thank-you-info {
  flex: 1;
  min-width: 0;
  color: white;
}

.thank-you-title {
  font-size: 30rpx;
  font-weight: 700;
  margin-bottom: 8rpx;
  text-shadow: 0 1rpx 3rpx rgba(0, 0, 0, 0.15);
}

.thank-you-time {
  display: flex;
  align-items: center;
  gap: 8rpx;
  background: rgba(255, 255, 255, 0.2);
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  width: fit-content;
}

.time-icon {
  font-size: 24rpx;
  line-height: 1;
}

.time-text {
  font-size: 24rpx;
  font-weight: 600;
  opacity: 0.95;
}

.thank-you-badge {
  background: rgba(255, 255, 255, 0.95);
  color: #059669;
  font-size: 22rpx;
  font-weight: 700;
  padding: 10rpx 20rpx;
  border-radius: 20rpx;
  flex-shrink: 0;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

/* ===== 响应式适配 ===== */
@media (max-width: 375px) {
  .support-main {
    padding: 92rpx 24rpx 20rpx;
  }
  
  .support-icon {
    width: 72rpx;
    height: 72rpx;
    font-size: 44rpx;
  }
  
  .support-title {
    font-size: 28rpx;
  }
  
  .support-desc {
    font-size: 22rpx;
  }
  
  .badge-main {
    font-size: 24rpx;
  }
  
  .badge-sub {
    font-size: 20rpx;
  }
}


/* ===== 原生模板广告样式 ===== */

/* 横版广告容器 - 激励卡片下方 */
.ad-horizontal-container {
  margin: 16rpx 24rpx 8rpx;
  border-radius: 20rpx;
  overflow: hidden;
  background: #FFFFFF;
  box-shadow: 
    0 2rpx 16rpx rgba(0, 0, 0, 0.04),
    0 0rpx 2rpx rgba(0, 0, 0, 0.02);
}

.ad-horizontal-container ad {
  width: 100%;
}

/* 竖版广告卡片 - 融入卡片网格 */
.ad-card-vertical {
  background: #FFFFFF;
  border-radius: 28rpx;
  overflow: hidden;
  min-height: 320rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 
    0 2rpx 16rpx rgba(0, 0, 0, 0.04),
    0 0rpx 2rpx rgba(0, 0, 0, 0.02);
  padding: 0 !important;
}

.ad-card-vertical ad {
  width: 100%;
  height: 100%;
}
