# 胜任力功能实施方案

## 📋 项目概述

在资料查询页面增加"胜任力"卡片，将《附件D：PLM胜任力及行为指标框架》的内容转换为可查询的数据库。

## 🎯 实施目标

1. 创建新分包 `packageCompetence`（胜任力分包）
2. 将Markdown表格数据转换为JavaScript数据文件
3. 在资料查询页面添加"胜任力"卡片入口
4. 实现胜任力查询页面（参考packageD的设计）

---

## 📊 第一步：数据转换指导（给其他AI）

### 数据源文件

- 文件路径：`D:\FlightToolbox\图片\附件 D：PLM 胜任力及行为指标框架_MinerU__20251012070325.md`
- 数据内容：包含两个大表格
  - **D-1**: 核心胜任力及行为指标框架（9个胜任力，共81个行为指标）
  - **D-2**: 检查员和教员胜任力及行为指标框架（4个胜任力）

### 目标数据结构

需要生成一个JavaScript数据文件：`D:\FlightToolbox\miniprogram\packageCompetence\competence-data.js`

#### 数据格式规范

```javascript
/**
 * 胜任力数据文件
 * 文件：competence-data.js
 * 说明：包含核心胜任力和检查员/教员胜任力数据
 */

// 核心胜任力数据（9个胜任力）
var coreCompetencies = [
  {
    // ============ 基本信息 ============
    id: 'KNO',                                    // 胜任力代码（必须）
    category: 'core',                             // 分类：'core' 或 'instructor'（必须）
    chinese_name: '知识应用',                      // 中文名称（必须）
    english_name: 'Application of Knowledge',     // 英文名称（必须）

    // ============ 描述信息 ============
    description: '展示对相关信息、运行规定、飞机系统和运行环境的知识和理解。', // 中文描述（必须）
    description_en: 'Demonstrates knowledge and understanding of relevant information, operating instructions, aircraft systems and the operating environment.', // 英文描述（必须）

    // ============ 行为指标列表 ============
    behaviors: [
      {
        id: 'OB_KNO_1',                           // 行为指标ID（必须，格式：OB_胜任力代码_序号）
        code: 'OB KNO.1',                         // 行为指标代码（必须）
        chinese: '展示有关限制和系统及相互作用的实用和适用知识', // 中文内容（必须）
        english: 'Demonstrates practical and applicable knowledge of limitations and systems and their interaction' // 英文内容（必须）
      },
      {
        id: 'OB_KNO_2',
        code: 'OB KNO.2',
        chinese: '展示所需的已公布的运行规定的知识',
        english: 'Demonstrates required knowledge of published operating instructions'
      }
      // ... 其他行为指标
    ],

    // ============ 元数据 ============
    source: '附件D：PLM胜任力及行为指标框架',      // 数据来源（必须）
    section: 'D-1',                               // 所属章节（必须）
    behavior_count: 7                             // 行为指标数量（必须）
  }
  // ... 其他8个核心胜任力
];

// 检查员和教员胜任力数据（4个胜任力）
var instructorCompetencies = [
  {
    id: 'MGMT',
    category: 'instructor',
    chinese_name: '管理学习环境',
    english_name: 'Management of the learning environment',
    description: '确保教学、评估环境合适并且安全。',
    description_en: 'Ensures that the instruction, assessment and evaluation are conducted in a suitable and safe environment',

    behaviors: [
      {
        id: 'OB_MGMT_1',
        code: 'OB MGMT.1',
        chinese: '在教学和评估中实施威胁和差错管理 (TEM)',
        english: 'Applies TEM in the context of instruction/evaluation'
      }
      // ... 其他行为指标
    ],

    // ============ 额外字段（检查员和教员专用） ============
    conditions: {
      ground_training: '地面训练（包括 CRM）',
      flight_training: '飞行训练：在飞机和FSTD中涉及训练内容：-执照 -型别等级 -转机型 -航线 -复训'
    },

    source: '附件D：PLM胜任力及行为指标框架',
    section: 'D-2',
    behavior_count: 9
  }
  // ... 其他3个检查员和教员胜任力
];

// 导出合并数组
module.exports = coreCompetencies.concat(instructorCompetencies);
```

### 转换要求

#### 必须遵守的规则

1. **编码规范**
   - 使用 `var` 声明变量（不使用 `let` 或 `const`）
   - 所有字符串使用单引号 `'`
   - 使用 `module.exports` 导出数据

2. **字段完整性**
   - 所有标记为"必须"的字段都不能缺失
   - 英文和中文内容必须一一对应
   - 行为指标ID必须唯一且符合命名规范

3. **数据清洗**
   - 移除Markdown表格标记符号
   - 处理换行符，确保文本连续
   - 移除多余的空格
   - 确保特殊字符正确转义

4. **胜任力代码映射**（D-1核心胜任力）
   - KNO = 知识应用 (Application of Knowledge)
   - PRO = 程序应用和遵守规章 (Application of procedures and compliance with regulations)
   - FPA = 自动航径管理 (Aircraft Flight Path Management, automation)
   - FPM = 人工航径管理 (Aircraft Flight Path Management, manual control)
   - COM = 沟通 (Communication)
   - LTW = 领导力与团队合作 (Leadership and Teamwork)
   - SAW = 情景意识与信息管理 (Situation awareness and management of information)
   - WLM = 工作负荷管理 (Workload Management)
   - PSD = 问题解决与决策 (Problem Solving and Decision Making)

5. **检查员和教员胜任力代码映射**（D-2）
   - MGMT = 管理学习环境 (Management of the learning environment)
   - INST = 教学 (Instruction)
   - INTR = 与学员互动 (Interaction with the trainees)
   - ASMNT = 评估 (Assessment)

### 转换示例

**输入**（Markdown表格行）：
```
| 知识应用 Application of Knowledge (KNO) | 展示对相关信息、运行规定、飞机系统和运行环境的知识和理解。 | OB KNO.1 展示有关限制和系统及相互作用的实用和适用知识 |
```

**输出**（JavaScript对象）：
```javascript
{
  id: 'KNO',
  category: 'core',
  chinese_name: '知识应用',
  english_name: 'Application of Knowledge',
  description: '展示对相关信息、运行规定、飞机系统和运行环境的知识和理解。',
  description_en: 'Demonstrates knowledge and understanding of relevant information, operating instructions, aircraft systems and the operating environment.',
  behaviors: [
    {
      id: 'OB_KNO_1',
      code: 'OB KNO.1',
      chinese: '展示有关限制和系统及相互作用的实用和适用知识',
      english: 'Demonstrates practical and applicable knowledge of limitations and systems and their interaction'
    }
    // ... 其他6个行为指标
  ],
  source: '附件D：PLM胜任力及行为指标框架',
  section: 'D-1',
  behavior_count: 7
}
```

### 质量检查清单

转换完成后，请确认：

- [ ] 文件使用 `var` 和单引号
- [ ] 共13个胜任力对象（9个core + 4个instructor）
- [ ] 每个胜任力对象包含所有必须字段
- [ ] 所有行为指标ID唯一且符合格式
- [ ] 中英文内容完整且一一对应
- [ ] 特殊字符正确转义（如引号、换行符）
- [ ] 最后使用 `module.exports` 导出合并数组
- [ ] 文件语法正确，可以在Node.js中加载

---

## 🛠️ 第二步：创建分包结构

### 目录结构

```
miniprogram/packageCompetence/
├── competence-data.js          # 胜任力数据文件（由其他AI生成）
├── index.js                    # 主页面逻辑
├── index.json                  # 页面配置
├── index.wxml                  # 页面结构
└── index.wxss                  # 页面样式
```

### 页面设计参考

参考 `packageD/index.js` 的实现：

**核心功能**：
1. 搜索功能（支持中英文搜索）
2. 分类筛选（核心胜任力 / 检查员教员胜任力）
3. 行为指标展开/折叠
4. 浮窗详情显示
5. 复制功能

**页面布局**：
- 顶部：搜索框 + 分类切换
- 中部：胜任力列表（卡片式）
- 底部：加载更多
- 浮窗：胜任力详情 + 行为指标列表

---

## 🔌 第三步：配置分包路由

### 修改 `miniprogram/app.json`

在 `subPackages` 数组中添加新分包配置：

```json
{
  "root": "packageCompetence",
  "name": "competencePackage",
  "pages": [
    "index"
  ]
}
```

### 配置预加载规则

在 `app.json` 的 `preloadRule` 中添加：

```json
"pages/search/index": {
  "network": "all",
  "packages": ["competencePackage"]
}
```

---

## 🎨 第四步：添加资料查询卡片

### 修改 `miniprogram/pages/search/index.js`

在 `allCategories` 数组中添加胜任力卡片（建议插入到第4个位置，在"缩写"之后）：

```javascript
{
  id: 'competence',
  icon: '🎯',
  title: '胜任力',
  description: 'PLM胜任力及行为指标框架',
  count: '13项胜任力',
  countType: 'primary',
  pointsRequired: 0,
  pointsType: 'success',
  pointsText: '',
  path: '/packageCompetence/index'
}
```

---

## ✅ 验证清单

完成实施后，请逐项验证：

### 数据验证
- [ ] `competence-data.js` 文件生成成功
- [ ] 数据格式符合规范
- [ ] 共13个胜任力对象（9+4）
- [ ] 所有行为指标数据完整

### 分包验证
- [ ] 分包在 `app.json` 中正确配置
- [ ] 预加载规则已添加
- [ ] 页面文件（js/json/wxml/wxss）齐全

### 功能验证
- [ ] 资料查询页面显示胜任力卡片
- [ ] 点击卡片可正常导航到胜任力页面
- [ ] 搜索功能正常工作
- [ ] 分类筛选正常工作
- [ ] 浮窗详情显示正常
- [ ] 复制功能正常工作
- [ ] 离线模式下可正常使用

### 性能验证
- [ ] 分包大小合理（建议 < 500KB）
- [ ] 页面加载速度正常
- [ ] 搜索响应速度快

---

## 📝 注意事项

1. **离线优先**：所有功能必须在飞行模式下可用
2. **使用BasePage**：页面必须继承 `base-page.js`
3. **广告集成**：参考其他页面集成 `AdManager`
4. **错误处理**：使用统一的 `handleError` 方法
5. **响应式布局**：使用 `rpx` 单位

---

## 🚀 实施步骤总结

1. **将本文档第一部分发送给其他AI**，让其生成 `competence-data.js` 文件
2. **创建分包目录和页面文件**（参考 packageD）
3. **配置 app.json**（添加分包和预加载）
4. **修改资料查询页面**（添加胜任力卡片）
5. **测试验证**（功能、离线、性能）

---

## 📚 参考文件

- 数据源：`D:\FlightToolbox\图片\附件 D：PLM 胜任力及行为指标框架_MinerU__20251012070325.md`
- 参考分包：`miniprogram\packageD\`
- 参考页面：`miniprogram\pages\search\index.js`
- 基类文件：`miniprogram\utils\base-page.js`
- 广告管理：`miniprogram\utils\ad-manager.js`

---

## 💡 扩展建议

后续可以考虑：
1. 添加胜任力收藏功能
2. 支持胜任力评估记录
3. 生成胜任力评估报告
4. 与训练记录关联
