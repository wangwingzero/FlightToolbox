# 航线录音分包完整管理指南

**文档版本**: v1.0.0  
**最后更新**: 2025-10-25  
**用途**: 新增机场航线录音的完整操作规范

---

## 📋 目录

1. [项目架构概述](#项目架构概述)
2. [航线录音分包结构](#航线录音分包结构)
3. [预加载规则系统](#预加载规则系统)
4. [新增机场完整流程](#新增机场完整流程)
5. [配置文件详解](#配置文件详解)
6. [数据验证清单](#数据验证清单)
7. [常见问题解决](#常见问题解决)
8. [最佳实践建议](#最佳实践建议)

---

## 项目架构概述

### 核心约束

#### 1. 微信小程序限制
```
⚠️ 重要限制：
- 单个页面 preloadRule 最大预加载: 2MB
- 总分包大小建议: < 20MB（性能考虑）
- 音频文件格式: MP3（推荐 32-48kbps 压缩）
```

#### 2. 离线优先设计
```
✅ 必须满足：
- 所有音频分包必须支持离线播放
- 预加载完成后，飞行模式下可正常使用
- 数据文件本地存储（miniprogram/data/regions/）
```

#### 3. TabBar 主页禁止加载音频
```
❌ 禁止在以下页面预加载音频：
- pages/search/index（资料查询）
- pages/flight-calculator/index（计算工具）
- pages/cockpit/index（驾驶舱）
- pages/operations/index（通信）
- pages/home/index（我的首页）
```

---

## 航线录音分包结构

### 目录结构规范

```
miniprogram/
├── package{CountryCode}/          # 音频分包目录
│   ├── index.js                   # 分包入口文件（必需）
│   └── *.mp3                      # 音频文件（建议压缩）
├── data/
│   └── regions/
│       └── {country}.js           # 数据文件（必需）
```

### 命名规范

#### 1. 分包目录命名
```javascript
// 格式：package + 国家/地区英文名（首字母大写）
packageJapan          // ✅ 日本
packageKorean         // ✅ 韩国（注意：Korea的形容词形式）
packageUK             // ✅ 英国（使用缩写）
packageAmerica        // ✅ 美国
packageUAE            // ✅ 阿联酋（使用缩写）

// 多单词国家/地区
packageSrilanka       // ✅ 斯里兰卡（连写，首字母大写）
packageAustralia      // ✅ 澳大利亚
```

#### 2. 音频文件命名
```javascript
// 格式：航班号_陆空通话内容简述.mp3
// 规则：
// - 航空公司+航班号，用连字符连接
// - 内容用连字符分隔单词
// - 首字母大写（每个单词）
// - 数字直接使用（不用 decimal）

China-Eastern-7551_Descend-FL250-level-by-LOGAN.mp3           // ✅
Hainan-439_Cleared-to-Land-RWY27L.mp3                         // ✅
Korean-Air-925_Line-up-and-wait-runway-16-left.mp3            // ✅
China-Southern-3089_Contact-Incheon-Control-124-decimal-52.mp3 // ✅

// ❌ 错误示例
china_eastern_7551_descend.mp3                                 // 小写
ChinaEastern7551-Descend-FL250.mp3                            // 无连字符
China-Eastern-7551_Descend FL250.mp3                          // 有空格
```

#### 3. 数据文件命名
```javascript
// 格式：{country}.js（全小写，英文名称）
japan.js              // ✅ 日本
korean.js             // ✅ 韩国
uk.js                 // ✅ 英国
usa.js 或 america.js  // ✅ 美国（二选一，保持一致）
philippines.js        // ✅ 菲律宾

// ❌ 错误示例
Japan.js              // 首字母不能大写
south-korea.js        // 数据文件不用连字符
韩国.js               // 不能用中文
```

---

## 预加载规则系统

### 可用预加载页面（非 TabBar）

```javascript
// 当前已分配的预加载页面及其容量
const PRELOAD_PAGES = {
  // 1. 录音播放页（容量：1.74 MB 已满）
  'pages/audio-player/index': {
    allocated: ['packageKorean'],
    currentSize: '1.74 MB',
    remaining: '0.26 MB',
    icon: '🎵'
  },

  // 2. 航线录音页（容量：1.72 MB 已满）
  'pages/airline-recordings/index': {
    allocated: ['packageJapan', 'packageAmerica'],
    currentSize: '1.72 MB',
    remaining: '0.28 MB',
    icon: '✈️'
  },

  // 3. 录音分类页（容量：1.61 MB）
  'pages/recording-categories/index': {
    allocated: ['packageUAE', 'packageItaly', 'packagePhilippines'],
    currentSize: '1.61 MB',
    remaining: '0.39 MB',
    icon: '📂'
  },

  // 4. 录音片段页（容量：1.25 MB）
  'pages/recording-clips/index': {
    allocated: ['packageThailand', 'packageSrilanka', 'packageTurkey'],
    currentSize: '1.25 MB',
    remaining: '0.75 MB',
    icon: '🎬'
  },

  // 5. 通信失效页（容量：1.71 MB）⭐ 最佳选择
  'pages/communication-failure/index': {
    allocated: ['packageRussia', 'packageAustralia', 'packageFrance', 'packageSingapore', 'packageUK'],
    currentSize: '1.71 MB',
    remaining: '0.29 MB',
    icon: '📡'
  }
};
```

### 预加载页面分配策略

#### 分配原则
```
1. ⚖️ 负载均衡：尽量平均分配各页面的预加载量
2. 📏 大小限制：单页总预加载量 < 2MB（严格）
3. 🎯 语义关联：相似地区/功能的音频分配到同一页面
4. 🔄 灵活调整：小分包可组合，大分包单独分配
```

#### 分配算法
```javascript
/**
 * 新增音频分包预加载页面分配算法
 * @param {number} packageSize - 新分包大小（MB）
 * @returns {string} 推荐的预加载页面
 */
function selectPreloadPage(packageSize) {
  // 1. 筛选剩余空间足够的页面
  const availablePages = Object.entries(PRELOAD_PAGES)
    .filter(([page, info]) => parseFloat(info.remaining) >= packageSize)
    .sort((a, b) => parseFloat(b[1].remaining) - parseFloat(a[1].remaining));
  
  // 2. 优先选择剩余空间最大的页面
  if (availablePages.length > 0) {
    return availablePages[0][0];
  }
  
  // 3. 如果所有页面空间都不够，需要重新组织
  console.error('所有预加载页面空间不足，需要重新规划！');
  return null;
}

// 使用示例
const newPackageSize = 0.6; // 新分包 0.6 MB
const recommendedPage = selectPreloadPage(newPackageSize);
// 推荐: pages/recording-clips/index (剩余 0.75 MB)
```

---

## 新增机场完整流程

### 🚀 步骤 1：准备音频文件

#### 1.1 收集音频
```bash
# 音频来源：
- LiveATC.net 真实录音
- 机场官方发布的训练材料
- 航空公司内部录音（需授权）

# 音频要求：
✅ 真实陆空通话录音
✅ 音质清晰可辨
✅ 包含完整的通话内容
❌ 避免背景噪音过大
❌ 避免重复录音
```

#### 1.2 音频压缩
```bash
# 使用 FFmpeg 压缩音频
# 目标：32-48kbps，单个文件 < 100KB

# 压缩命令（推荐）
ffmpeg -i input.mp3 -b:a 32k -ar 22050 -ac 1 output.mp3

# 批量压缩脚本
for file in *.mp3; do
  ffmpeg -i "$file" -b:a 32k -ar 22050 -ac 1 "compressed_$file"
done

# 压缩效果参考：
原文件: 500KB → 压缩后: 15-20KB (压缩率 95%+)
```

#### 1.3 文件命名与整理
```bash
# 创建分包目录
mkdir miniprogram/package{CountryName}

# 复制压缩后的音频文件
cp compressed_*.mp3 miniprogram/package{CountryName}/

# 重命名为规范格式
# 示例：
mv "compressed_china_eastern_7551.mp3" \
   "China-Eastern-7551_Descend-FL250-level-by-LOGAN.mp3"
```

---

### 🚀 步骤 2：创建分包入口文件

#### 2.1 创建 index.js
```javascript
// miniprogram/package{CountryName}/index.js

// 导出分包信息
module.exports = {
  name: '{country}AudioPackage',              // 分包内部名称
  description: '{国家}{机场}真实陆空通话录音（航空公司列表）',
  version: '1.0.0',                           // 版本号
  audioCount: XX,                             // 音频文件数量
  lastUpdated: '2025-XX-XX'                   // 最后更新日期
};
```

#### 2.2 实际示例
```javascript
// miniprogram/packageUK/index.js

module.exports = {
  name: 'ukAudioPackage',
  description: '英国伦敦希斯罗机场真实陆空通话录音（China Eastern, Hainan, Qantas等）',
  version: '1.0.0',
  audioCount: 36,
  lastUpdated: '2025-10-25'
};
```

---

### 🚀 步骤 3：创建数据文件

#### 3.1 数据文件结构
```javascript
// miniprogram/data/regions/{country}.js

const {Country}Data = {
  "clips": [
    {
      "label": "区调|进近|塔台|地面|放行",  // 管制阶段标签
      "full_transcript": "完整英文转录",
      "translation_cn": "中文翻译",
      "mp3_file": "音频文件名.mp3"
    }
  ]
};

module.exports = {Country}Data;
```

#### 3.2 标签分类规范
```javascript
// 管制阶段标签（label）
const VALID_LABELS = [
  '放行',        // Clearance Delivery
  '地面',        // Ground Control
  '塔台',        // Tower
  '离场',        // Departure
  '区调',        // Area Control / Center
  '进场',        // Arrival
  '进近',        // Approach
  '其他'         // Others
];

// 标签选择原则：
// - 以管制员视角为准（不是飞行员）
// - 优先使用主要管制阶段
// - 避免自创标签
```

#### 3.3 完整示例
```javascript
// miniprogram/data/regions/uk.js

const UKData = {
  "clips": [
    {
      "label": "区调",
      "full_transcript": "China Eastern 7551, descend flight level 250, level by LOGAN.",
      "translation_cn": "东方航空7551，下降到高度层250，在LOGAN改平。",
      "mp3_file": "China-Eastern-7551_Descend-FL250-level-by-LOGAN.mp3"
    },
    {
      "label": "进近",
      "full_transcript": "China Eastern 7551, turn right heading 240 degrees to establish localizer 27 Right.",
      "translation_cn": "东方航空7551，右转航向240度，截获27右跑道航向道。",
      "mp3_file": "China-Eastern-7551_Turn-right-heading-240-establish-localizer-27R.mp3"
    },
    {
      "label": "塔台",
      "full_transcript": "China Eastern 7551, runway 27 Right, cleared to land. Surface wind 140 degrees, 8 knots.",
      "translation_cn": "东方航空7551，跑道27右，可以落地。地面风140度8节。",
      "mp3_file": "China-Eastern-7551_Cleared-to-land-runway-27R.mp3"
    },
    {
      "label": "地面",
      "full_transcript": "China Eastern 7551, good day. Taxi south on Echo, hold at Echo 1, stand 440.",
      "translation_cn": "东方航空7551，日安。沿Echo滑行道向南滑行，在Echo 1等待，机位440。",
      "mp3_file": "China-Eastern-7551_Taxi-south-on-Echo-hold-Echo1.mp3"
    }
  ]
};

module.exports = UKData;
```

#### 3.4 翻译规范
```javascript
// 英文转录规范：
✅ 保留原始用词和语序
✅ 包含所有关键信息（高度、航向、频率等）
✅ 使用标准航空英语术语
✅ 数字使用阿拉伯数字或完整拼写

// 中文翻译规范：
✅ 准确传达英文原意
✅ 使用中国民航标准术语
✅ 保留关键数据（不转换单位）
✅ 语序可调整以符合中文习惯

// 术语对照表：
descend             → 下降
climb               → 爬升
turn left/right     → 左转/右转
cleared to land     → 可以落地
hold short          → 在...外等待
contact             → 联系
flight level        → 高度层
runway              → 跑道
taxiway             → 滑行道
```

---

### 🚀 步骤 4：统计分包大小

#### 4.1 PowerShell 命令
```powershell
# 统计音频文件数量
cd D:\FlightToolbox\miniprogram
(Get-ChildItem "package{CountryName}\*.mp3" -File).Count

# 统计分包总大小（MB）
$size = (Get-ChildItem "package{CountryName}\*.mp3" -File | 
         Measure-Object -Property Length -Sum).Sum / 1MB
[math]::Round($size, 2)

# 完整统计脚本
$packageName = "packageUK"
$count = (Get-ChildItem "$packageName\*.mp3" -File).Count
$size = (Get-ChildItem "$packageName\*.mp3" -File | 
         Measure-Object -Property Length -Sum).Sum / 1MB
Write-Output "$packageName 统计信息:"
Write-Output "音频文件数: $count"
Write-Output "总大小: $([math]::Round($size, 2)) MB"
```

#### 4.2 选择预加载页面
```javascript
// 根据分包大小选择预加载页面
if (packageSize < 0.3) {
  // 小分包：可与其他分包组合
  recommendedPage = '找剩余空间最大的页面';
} else if (packageSize < 1.0) {
  // 中等分包：建议组合或单独
  recommendedPage = '找剩余空间 > 1MB 的页面';
} else if (packageSize < 1.8) {
  // 大分包：建议单独分配
  recommendedPage = '找空闲或剩余空间充足的页面';
} else {
  // 超大分包：必须压缩或拆分
  console.error('分包过大，建议压缩音频或拆分为两个分包');
}
```

---

### 🚀 步骤 5：配置 app.json

#### 5.1 添加 subPackages
```json
// 在 miniprogram/app.json 的 subPackages 数组中添加

{
  "subPackages": [
    // ... 其他分包
    {
      "root": "package{CountryName}",
      "name": "{country}AudioPackage",
      "pages": ["index"]
    }
  ]
}
```

#### 5.2 更新 preloadRule
```json
// 在 miniprogram/app.json 的 preloadRule 中更新对应页面

{
  "preloadRule": {
    // ... 其他规则
    "pages/communication-failure/index": {
      "network": "all",
      "packages": [
        "packageRussia", 
        "packageAustralia", 
        "packageFrance", 
        "packageSingapore",
        "package{CountryName}"  // 新增
      ]
    }
  }
}
```

#### 5.3 完整配置示例
```json
// miniprogram/app.json 片段

{
  "subPackages": [
    // 音频分包示例
    {
      "root": "packageJapan",
      "name": "japanAudioPackage",
      "pages": ["index"]
    },
    {
      "root": "packageKorean",
      "name": "koreaAudioPackage",
      "pages": ["index"]
    },
    {
      "root": "packageUK",
      "name": "ukAudioPackage",
      "pages": ["index"]
    }
  ],
  
  "preloadRule": {
    "pages/audio-player/index": {
      "network": "all",
      "packages": ["packageKorean"]
    },
    "pages/airline-recordings/index": {
      "network": "all",
      "packages": ["packageJapan", "packageAmerica"]
    },
    "pages/communication-failure/index": {
      "network": "all",
      "packages": [
        "packageRussia", 
        "packageAustralia", 
        "packageFrance", 
        "packageSingapore",
        "packageUK"
      ]
    }
  }
}
```

---

### 🚀 步骤 6：配置 audio-preload-guide.js

#### 6.1 添加地区映射
```javascript
// miniprogram/utils/audio-preload-guide.js

this.preloadPageMapping = {
  // ... 其他地区
  
  '{countryId}': {
    packageName: 'package{CountryName}',
    regionName: '{国家}{城市}机场',
    flag: '{国旗emoji}',
    preloadPage: '{选择的预加载页面路径}',
    preloadPageName: '{预加载页面中文名}',
    preloadPageIcon: '{页面图标emoji}',
    description: '{国家}{城市}机场陆空通话录音将通过{页面名}页面自动预加载'
  }
};
```

#### 6.2 regionId 命名规范
```javascript
// regionId 命名规则：全小写英文，国家/地区名称
const REGION_IDS = {
  'japan': 'packageJapan',          // ✅
  'korea': 'packageKorean',         // ✅（不是 south-korea）
  'usa': 'packageAmerica',          // ✅（或 america）
  'uk': 'packageUK',                // ✅
  'uae': 'packageUAE',              // ✅
  'singapore': 'packageSingapore',  // ✅
  'philippines': 'packagePhilippines', // ✅
  
  // ❌ 错误示例
  'south-korea': '...',  // 不要用连字符
  'USA': '...',          // 不要大写
  '韩国': '...',          // 不要中文
};
```

#### 6.3 国旗 Emoji 对照表
```javascript
const FLAG_EMOJIS = {
  '中国': '🇨🇳',
  '日本': '🇯🇵',
  '韩国': '🇰🇷',
  '美国': '🇺🇸',
  '英国': '🇬🇧',
  '法国': '🇫🇷',
  '意大利': '🇮🇹',
  '俄罗斯': '🇷🇺',
  '澳大利亚': '🇦🇺',
  '新加坡': '🇸🇬',
  '泰国': '🇹🇭',
  '菲律宾': '🇵🇭',
  '阿联酋': '🇦🇪',
  '土耳其': '🇹🇷',
  '斯里兰卡': '🇱🇰'
};
```

#### 6.4 完整配置示例
```javascript
// miniprogram/utils/audio-preload-guide.js

this.preloadPageMapping = {
  'uk': {
    packageName: 'packageUK',
    regionName: '英国伦敦希斯罗机场',
    flag: '🇬🇧',
    preloadPage: 'pages/communication-failure/index',
    preloadPageName: '通信失效',
    preloadPageIcon: '📡',
    description: '英国伦敦希斯罗机场陆空通话录音将通过通信失效页面自动预加载'
  },
  
  'japan': {
    packageName: 'packageJapan',
    regionName: '日本成田机场',
    flag: '🇯🇵',
    preloadPage: 'pages/airline-recordings/index',
    preloadPageName: '航线录音',
    preloadPageIcon: '✈️',
    description: '日本成田机场陆空通话录音将通过航线录音页面自动预加载'
  }
};
```

---

## 配置文件详解

### app.json 完整结构

```json
{
  "pages": [...],           // 主包页面列表
  
  "subPackages": [          // 分包配置
    {
      "root": "packageXXX",       // 分包根目录
      "name": "xxxAudioPackage",  // 分包名称（用于 preloadRule）
      "pages": ["index"]          // 分包页面列表
    }
  ],
  
  "preloadRule": {          // 预加载规则
    "pages/xxx/index": {          // 触发预加载的页面路径
      "network": "all",           // 网络模式：all, wifi
      "packages": [               // 预加载的分包列表
        "packageXXX",
        "packageYYY"
      ]
    }
  },
  
  "window": {...},          // 窗口配置
  "tabBar": {...},          // TabBar 配置
  "permission": {...},      // 权限配置
  "requiredPrivateInfos": [...] // 隐私 API 声明
}
```

### audio-preload-guide.js 完整结构

```javascript
/**
 * 音频分包预加载引导配置管理器
 */

// TabBar 页面列表（禁止预加载音频）
var TABBAR_PAGES = [
  '/pages/search/index',
  '/pages/flight-calculator/index',
  '/pages/cockpit/index',
  '/pages/operations/index',
  '/pages/home/index'
];

function AudioPreloadGuide() {
  // 音频分包预加载页面映射配置
  this.preloadPageMapping = {
    '{regionId}': {
      packageName: 'package{CountryName}',
      regionName: '{国家}{城市}机场',
      flag: '{emoji}',
      preloadPage: '{页面路径}',
      preloadPageName: '{页面名称}',
      preloadPageIcon: '{emoji}',
      description: '{描述文案}'
    }
  };

  // 初始化本地存储系统
  this.initPreloadStorage();
}

// 核心方法
AudioPreloadGuide.prototype = {
  initPreloadStorage: function() {},      // 初始化存储
  getPreloadGuide: function(regionId) {}, // 获取引导信息
  generateGuideMessage: function(regionId) {}, // 生成引导消息
  checkPackagePreloaded: function(regionId) {}, // 检查预加载状态
  navigateToPreloadPage: function(regionId) {}, // 跳转预加载页面
  showPreloadGuideDialog: function(regionId) {}, // 显示引导弹窗
  markPackagePreloaded: function(regionId) {}, // 标记已预加载
  clearPreloadStatus: function(regionId) {} // 清除预加载状态
};

module.exports = AudioPreloadGuide;
```

---

## 数据验证清单

### ✅ 文件完整性检查

```bash
# 1. 检查音频文件数量
cd miniprogram/package{CountryName}
ls *.mp3 | wc -l

# 2. 检查数据文件记录数
grep -c '"mp3_file":' data/regions/{country}.js

# 3. 对比数量是否一致
# 音频文件数 = 数据记录数 ✅

# 4. 检查重复记录
grep '"mp3_file":' data/regions/{country}.js | sort | uniq -d

# 5. 检查缺失文件
# 对比数据文件中的 mp3_file 与实际文件列表
```

### ✅ 语法检查

```bash
# JavaScript 语法检查
cd miniprogram

# 检查分包入口文件
node -c package{CountryName}/index.js

# 检查数据文件
node -c data/regions/{country}.js

# 检查配置文件
node -c utils/audio-preload-guide.js

# 检查 app.json（使用 PowerShell）
try {
  $json = Get-Content app.json -Raw | ConvertFrom-Json
  Write-Output "✅ app.json JSON格式验证通过"
} catch {
  Write-Output "❌ app.json JSON格式错误"
}
```

### ✅ 预加载限制检查

```powershell
# 计算预加载页面总大小
$packages = @('packageRussia', 'packageAustralia', 'packageFrance', 
              'packageSingapore', 'packageUK')
$totalSize = 0
foreach ($pkg in $packages) {
  $size = (Get-ChildItem "$pkg\*.mp3" -File | 
           Measure-Object -Property Length -Sum).Sum / 1MB
  $totalSize += $size
  Write-Output "$pkg`: $([math]::Round($size, 2)) MB"
}
Write-Output "总预加载大小: $([math]::Round($totalSize, 2)) MB"

# 验证：总大小 < 2MB ✅
```

---

## 常见问题解决

### 问题 1：预加载超限（错误码 80058）

#### 症状
```
Error: 系统错误，错误码：80058
preloadRule [pages/xxx/index] source size XXX KB exceed max limit 2MB
```

#### 原因
- 单个页面预加载的分包总大小超过 2MB

#### 解决方案
```javascript
// 方案 1：压缩音频文件
ffmpeg -i input.mp3 -b:a 24k -ar 22050 -ac 1 output.mp3

// 方案 2：拆分到多个预加载页面
// 将大分包移到其他页面

// 方案 3：拆分分包
// 将一个大分包拆分为两个小分包
packageUK1 (0.8 MB) + packageUK2 (0.8 MB)
```

### 问题 2：数据文件与音频文件数量不一致

#### 检测方法
```bash
# 音频文件数量
audio_count=$(ls miniprogram/packageUK/*.mp3 | wc -l)

# 数据记录数量
data_count=$(grep -c '"mp3_file":' miniprogram/data/regions/uk.js)

# 对比
if [ $audio_count -eq $data_count ]; then
  echo "✅ 数量一致"
else
  echo "❌ 数量不一致: 音频$audio_count 数据$data_count"
fi
```

#### 解决方案
```bash
# 1. 找出重复记录
grep '"mp3_file":' data/regions/uk.js | sort | uniq -d

# 2. 找出缺失文件
# 列出数据文件中的所有 mp3_file
grep -oP '"mp3_file": "\K[^"]+' data/regions/uk.js > data_files.txt

# 列出实际音频文件
ls packageUK/*.mp3 | xargs -n 1 basename > audio_files.txt

# 对比
diff data_files.txt audio_files.txt
```

### 问题 3：音频播放时提示"分包未加载"

#### 原因
1. 用户未访问过预加载页面
2. 预加载配置错误
3. 分包名称不匹配

#### 解决方案
```javascript
// 1. 验证 regionId 与 packageName 匹配
// audio-preload-guide.js
'uk': {
  packageName: 'packageUK',  // 必须与 app.json 中的 root 一致
}

// app.json
{
  "root": "packageUK",  // 必须与 packageName 对应
  "name": "ukAudioPackage"
}

// 2. 验证预加载配置
// audio-preload-guide.js
'uk': {
  preloadPage: 'pages/communication-failure/index'  // 页面路径
}

// app.json
"pages/communication-failure/index": {
  "packages": ["packageUK"]  // 必须包含此分包
}
```

### 问题 4：JSON 格式错误

#### 常见错误
```json
// ❌ 错误 1：缺少逗号
{
  "label": "区调"
  "full_transcript": "..."  // 缺少逗号
}

// ❌ 错误 2：多余逗号
{
  "label": "区调",
  "full_transcript": "...",  // 最后一项不要逗号
}

// ❌ 错误 3：引号错误
{
  "label": '区调',  // 应该用双引号
  "mp3_file": "test.mp3"
}

// ✅ 正确格式
{
  "label": "区调",
  "full_transcript": "...",
  "translation_cn": "...",
  "mp3_file": "test.mp3"
}
```

#### 验证工具
```bash
# 使用 jq 验证 JSON 格式
cat data/regions/uk.js | grep -A 9999 "clips" | jq .

# 或使用 Node.js
node -e "require('./data/regions/uk.js')"
```

---

## 最佳实践建议

### 🎯 音频质量管理

```javascript
// 推荐的音频参数
const AUDIO_SETTINGS = {
  bitrate: '32-48 kbps',      // 比特率
  sampleRate: '22050 Hz',     // 采样率
  channels: 'mono',           // 单声道
  format: 'MP3',              // 格式
  targetSize: '< 50 KB/file'  // 单文件大小
};

// 压缩命令模板
const COMPRESS_COMMAND = `
  ffmpeg -i input.mp3 \\
    -b:a 32k \\           # 比特率
    -ar 22050 \\          # 采样率
    -ac 1 \\              # 单声道
    output.mp3
`;
```

### 🎯 数据组织规范

```javascript
// 1. 按飞行阶段排序
const PHASE_ORDER = [
  '放行',   // 1. Clearance Delivery
  '地面',   // 2. Ground Control
  '塔台',   // 3. Tower (Takeoff)
  '离场',   // 4. Departure
  '区调',   // 5. Area Control
  '进场',   // 6. Arrival
  '进近',   // 7. Approach
  '塔台',   // 8. Tower (Landing)
  '地面'    // 9. Ground Control (Taxi-in)
];

// 2. 同一航班的录音连续排列
// ✅ 推荐
China-Eastern-7551_xxx_1.mp3
China-Eastern-7551_xxx_2.mp3
China-Eastern-7551_xxx_3.mp3
Hainan-439_xxx_1.mp3
Hainan-439_xxx_2.mp3

// ❌ 不推荐
China-Eastern-7551_xxx_1.mp3
Hainan-439_xxx_1.mp3
China-Eastern-7551_xxx_2.mp3
```

### 🎯 版本管理

```javascript
// package{CountryName}/index.js 版本号规范

// 主版本号.次版本号.修订号
version: 'X.Y.Z'

// X: 主版本号（重大变更）
//    - 更换机场
//    - 完全重录
//    - 删除 >50% 文件

// Y: 次版本号（功能更新）
//    - 新增录音
//    - 更新部分录音
//    - 重新压缩

// Z: 修订号（bug修复）
//    - 修正翻译错误
//    - 修正文件名
//    - 元数据更新

// 示例
'1.0.0'  // 初始版本
'1.1.0'  // 新增10条录音
'1.1.1'  // 修正3条翻译
'2.0.0'  // 更换为另一个机场
```

### 🎯 文档化

```markdown
# 每个新增机场都应创建更新文档

## 文档结构
更新说明/
└── 新增{国家}{城市}音频分包_v{版本}.md
    ├── 音频分包信息
    ├── 航空公司覆盖
    ├── 音频内容分类
    ├── 特色陆空通话
    ├── 技术实现
    ├── 项目统计更新
    ├── 预加载配置更新
    ├── 验证清单
    └── 机场信息

## 模板
参考: 更新说明/新增英国伦敦音频分包_v2.8.1.md
```

---

## 快速参考表

### 文件路径对照

| 类型 | 路径 | 说明 |
|-----|------|------|
| 音频分包 | `miniprogram/package{CountryName}/` | 存放音频文件 |
| 分包入口 | `miniprogram/package{CountryName}/index.js` | 分包元数据 |
| 数据文件 | `miniprogram/data/regions/{country}.js` | 转录和翻译 |
| 全局配置 | `miniprogram/app.json` | 分包和预加载 |
| 引导配置 | `miniprogram/utils/audio-preload-guide.js` | 预加载映射 |
| 更新文档 | `更新说明/新增{国家}{城市}音频分包_v{版本}.md` | 版本记录 |

### 命令速查

```bash
# 统计音频数量
(Get-ChildItem "packageXXX\*.mp3" -File).Count

# 统计分包大小
$size = (Get-ChildItem "packageXXX\*.mp3" -File | 
         Measure-Object -Property Length -Sum).Sum / 1MB
[math]::Round($size, 2)

# 检查数据记录数
(Select-String -Path "data\regions\xxx.js" -Pattern '"mp3_file":').Matches.Count

# 检查重复记录
Select-String -Path "data\regions\xxx.js" -Pattern '"mp3_file": "(.*?)"' | 
  ForEach-Object { $_.Matches.Groups[1].Value } | 
  Group-Object | Where-Object { $_.Count -gt 1 }

# 语法检查
node -c packageXXX/index.js
node -c data/regions/xxx.js
```

---

## 附录

### A. 当前音频分包总览（v2.8.1）

| # | 国家/地区 | 分包名称 | 大小 | 文件数 | 预加载页面 |
|---|---------|---------|------|--------|-----------|
| 1 | 🇰🇷 韩国 | packageKorean | 1.74 MB | 121 | 录音播放 |
| 2 | 🇯🇵 日本 | packageJapan | 1.00 MB | 67 | 航线录音 |
| 3 | 🇺🇸 美国 | packageAmerica | 0.72 MB | 53 | 航线录音 |
| 4 | 🇦🇪 阿联酋 | packageUAE | 0.64 MB | 38 | 录音分类 |
| 5 | 🇬🇧 英国 | packageUK | 0.57 MB | 36 | 通信失效 |
| 6 | 🇮🇹 意大利 | packageItaly | 0.51 MB | 29 | 录音分类 |
| 7 | 🇵🇭 菲律宾 | packagePhilippines | 0.46 MB | 27 | 录音分类 |
| 8 | 🇹🇭 泰国 | packageThailand | 0.43 MB | 27 | 录音片段 |
| 9 | 🇱🇰 斯里兰卡 | packageSrilanka | 0.41 MB | 22 | 录音片段 |
| 10 | 🇹🇷 土耳其 | packageTurkey | 0.41 MB | 28 | 录音片段 |
| 11 | 🇷🇺 俄罗斯 | packageRussia | 0.38 MB | 23 | 通信失效 |
| 12 | 🇦🇺 澳大利亚 | packageAustralia | 0.35 MB | 20 | 通信失效 |
| 13 | 🇫🇷 法国 | packageFrance | 0.26 MB | 19 | 通信失效 |
| 14 | 🇸🇬 新加坡 | packageSingapore | 0.15 MB | 8 | 通信失效 |

**总计**: 14个音频分包，8.03 MB，374个音频文件

### B. 预加载页面配置总览

| 预加载页面 | 路径 | 图标 | 分包数 | 总大小 | 剩余空间 |
|-----------|------|------|--------|--------|---------|
| 录音播放 | pages/audio-player/index | 🎵 | 1 | 1.74 MB | 0.26 MB |
| 航线录音 | pages/airline-recordings/index | ✈️ | 2 | 1.72 MB | 0.28 MB |
| 录音分类 | pages/recording-categories/index | 📂 | 3 | 1.61 MB | 0.39 MB |
| 通信失效 | pages/communication-failure/index | 📡 | 5 | 1.71 MB | 0.29 MB |
| 录音片段 | pages/recording-clips/index | 🎬 | 3 | 1.25 MB | 0.75 MB |

---

**文档创建**: 2025-10-25  
**适用版本**: v2.8.1+  
**维护责任**: 开发团队  
**更新频率**: 随项目更新

